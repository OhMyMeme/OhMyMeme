# Issues — architecture-optimization

Problems and gotchas encountered during work on this plan.

---

## 2026-08-27 baseline

- `but status -fv --no-hint` cannot run in the native isolated worktree because GitButler is not configured there; it returned `No GitButler project found at .`. Main-worktree GitButler status was captured separately and remained unchanged.
- `mise run setup` failed at `python -m pip install --upgrade pip` because the auto-created uv venv has no pip. This was resolved with managed `uv pip` installation in the isolated venv only.

## 2026-08-27 adversarial verification — local projection invariants

- Verdict: `needs-fix`.
- The targeted tests pass: `mise exec -- python -m pytest tests/application/test_local_library_service.py tests/core/test_assets.py -q` reports `28 passed in 0.43s`; the wider application/core slice reports `50 passed in 1.11s`; ten sequential targeted runs each report `28 passed` with no timing or shared-path flake.
- `mise exec -- ruff check tests/application/test_local_library_service.py tests/core/test_assets.py` passes, and `mise exec -- black --check tests/application/test_local_library_service.py tests/core/test_assets.py` reports both files unchanged. The requested LSP diagnostic calls were rejected because the LSP client is rooted at the main worktree and refuses paths outside its request cwd; no target-worktree LSP result is claimed.
- Commit `bee830f` changes only `.omo/notepads/architecture-optimization/learnings.md`, `tests/application/test_local_library_service.py`, and `tests/core/test_assets.py`; native target-worktree status is clean. The protected main worktree still has only its four pre-existing unrelated files: `src/ohmymeme/presentation/desktop/window_manager.py`, `src/ohmymeme/presentation/frontend/settings/core/init.js`, `src/ohmymeme/presentation/frontend/settings/features/sync/settings.js`, and `src/webui/settings.js`.
- Defect: `tests/application/test_local_library_service.py:299-327` monkeypatches `_restore_manifest` and calls the private `_project_after_mutation()` directly. It never invokes `rename_meme()` or any other database mutation, so `db.get_by_id(meme_id)["original_name"] == "old"` only proves that no mutation occurred. The test therefore does not lock the required invariant that a successful SQLite mutation followed by projection failure and recovery failure is explicitly reported and not returned as success. The test should trigger a real mutation first and assert the resulting public return/exception plus the committed DB state and unrecovered manifest bytes.
- Cleanup receipt: all test-created SQLite databases and manifest files were under pytest `tmp_path` or a one-shot temporary directory; the failed one-line manual probe created no repository files. No production, plan, README, AGENTS, or protected files were modified.

## 2026-08-27 recovery-failure test correction

- Replaced the private `_project_after_mutation()` invocation with public `rename_meme()`. The test now proves the SQLite row changes to `renamed` before a `ValueError` projection failure, then surfaces that same exception with deterministic `OSError("restore failed")` as its cause while the unrecovered manifest bytes remain `new`.

## 2026-08-27 public projection boundary verification

- Failing-first tests initially failed for the intended reasons: `LocalLibraryService` had no `project_manifest()` and LAN `pull_manifest` accessed `_project_after_mutation()`.
- After the minimal boundary change, targeted application/sync/LAN/container tests passed `137 passed, 1 skipped`; the full suite passed `382 passed, 1 skipped`.
- Real temporary-root smoke paths independently observed Container/library projection, Sync `project_manifest`, and LAN `pull_manifest` plus `push_manifest` success. Temporary roots were removed after each probe.
- `lsp_diagnostics` could not run against this linked worktree because the LSP client rejects paths outside its request cwd; no clean LSP result is claimed.

## 2026-08-27 adversarial checkbox-3 verification

- Review scope used the available linked worktree `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `ed9d1c3`; the user-mentioned path without `-baseline` does not exist. The target worktree remained product-clean before this append; GitButler is not configured there, so native Git inspection was used.
- Positive evidence: `mise exec -- python -m pytest tests/application/test_local_library_service.py tests/test_sync.py tests/test_lan.py tests/app/test_container.py tests/integration/test_lan_server.py -q` returned `137 passed, 1 skipped in 6.84s`; full `mise exec -- python -m pytest tests/ -q` returned `382 passed, 1 skipped in 13.80s`; targeted `ruff check` passed; targeted `black --check` reported 6 files unchanged; `git diff --check` passed.
- Positive smoke evidence: a temporary `Container` imported a valid PNG and `library.project_manifest()` returned `True`; direct LAN `_cmd_push_manifest` with an empty version-3 manifest returned exactly `{'ok': True, 'local_count': 1}`. The temporary root was removed after the probe. Public projection failure/recovery was also covered by the existing real-`MemeDB` test and returned `False` while restoring old manifest bytes.
- Static private-call scan found `_project_after_mutation()` only inside `LocalLibraryService`; no `._project_after_mutation()` cross-module access remains in `src` or tests. Public `SyncService.project_manifest()` and `CommandHandlers._cmd_pull_manifest()` paths are correctly wired and their sentinel tests reject private projection bypass.
- **Blocking finding for checkbox acceptance:** `src/ohmymeme/services/lan/commands.py:146-158` still defines callable `_apply_manifest()`. When `_sync_service is None`, it directly calls `sync.service._apply_remote_order()` and `_apply_remote_collections()` at lines 154-155, bypassing the public `LocalLibraryService` apply boundary. Although current dispatch does not call this helper, the requested invariant says LAN metadata/order/collection paths only go through the public local-library boundary; a callable legacy path violates that seam and is not proven dead by a type/interface contract. An adversarial invocation reached the private adapters (the first probe failed only because the monkeypatched lambda arity did not match the adapter's two-argument forwarding; the actual source path and references are directly observable).
- **Coverage gap:** no test exercises LAN `push_manifest` success, malformed manifest, DB/application failure, projection failure/recovery, or exact `local_count` response shape. Existing LAN tests cover `pull_manifest` success and projection-failure envelope only (`tests/test_lan.py:384-413`). Sync pull/push mostly use `_FakeDb`; they do not independently prove real SQLite remote metadata/order/collection mutation through the public boundary.
- **Test-quality risks:** LAN tests use fixed port `17990` and global config/DB/server state (`tests/test_lan.py:25,128-151`), so concurrent worktrees can interfere. `tests/test_lan.py:370-380` treats `socket.timeout` as proof of server closure, making the handshake retry-limit test potentially misleading. These are review observations, not changes made here.
- `lsp_diagnostics` was attempted for all three changed source files and rejected because the LSP client root excludes this linked worktree; no LSP cleanliness is claimed. An initial full-suite attempt failed because the temporary smoke directory was removed during test collection; the follow-up full suite passed after cleanup ordering was corrected, so the first failure is recorded as a stale-state/misleading-success probe rather than a product failure.
- Verdict: **needs-fix / not confirmed**. Public projection replacement and recovery behavior are evidenced, but the callable LAN `_apply_manifest()` private planning bypass and missing LAN `push_manifest` failure/recovery evidence prevent satisfying the checkbox's strict “only public boundary” and adversarial acceptance criteria.

## 2026-08-27 follow-up verification

- Red regression `test_legacy_manifest_apply_uses_public_library_boundary` first failed because the legacy callable `_apply_manifest()` imported private sync helpers; after the fix it passed while those helpers were patched to fail, proving they are not reached.
- `_apply_manifest()` now returns the boolean result of `LocalLibraryService.apply_remote_metadata()` and has no sync-service private-helper import path.
- LAN compatibility tests now exercise real handler behavior: valid `push_manifest` returns `{"ok": True, "local_count": ...}`, malformed payload returns `{"ok": False, "error": "manifest 格式错误"}`, and public apply failure returns `{"ok": False, "error": "本地清单应用失败"}`.
- Serial targeted command `mise exec -- python -m pytest tests/test_lan.py tests/test_sync.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py tests/app/test_container.py -q` passed `140 passed, 1 skipped`; LAN-only passed `37 passed, 1 skipped`; LAN integration passed `4 passed`.
- A deliberately parallel run of fixed-port LAN suites produced unrelated global-state/port races; the required serial rerun passed. No production fix was made for that pre-existing test isolation issue.
- `mise exec -- ruff check ...` passed; `mise exec -- black --check ...` reported both changed Python files unchanged; `git diff --check` passed. LSP remained unavailable because the client rejects this linked worktree path.

## 2026-08-27 follow-up blocker fix

- Red test `test_legacy_manifest_apply_uses_public_library_boundary` initially reached the intended blocker: the callable LAN `_apply_manifest()` imported and invoked private sync helper functions when no SyncService was injected.
- `_apply_manifest()` now delegates directly to `LocalLibraryService.apply_remote_metadata()` and returns its boolean result; no LAN callable path imports or invokes `_apply_remote_order()`/`_apply_remote_collections()`.
- Added real LAN command coverage for push-manifest success (`{"ok": True, "local_count": ...}`), malformed input (`{"ok": False, "error": "manifest 格式错误"}`), and public apply failure (`{"ok": False, "error": "本地清单应用失败"}`).

## 2026-08-27 dependency convergence verification

- First full-suite run exposed the existing `_pull_worker` signature contract in `tests/application/test_import_service.py`; the public four-argument facade was restored, with new explicit dependencies confined to `_pull_worker_core`.
- Targeted verification passed `125 passed, 1 skipped`; full verification passed `389 passed, 1 skipped`. Ruff and black checks passed. LSP diagnostics were rejected for every changed path because the client request cwd is the main worktree, not this linked worktree.
- Scoped diff contains only the four requested production modules, two narrowly required regression test files, and this append-only notepad entry. No schema, manifest format, LAN wire protocol, config semantics, README, AGENTS, or protected dirty file changed.

## 2026-08-27 adversarial verification — Task 4 checkbox

- Verification was rerun in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` (the exact non-`-baseline` path remains absent). The worktree was clean before the append-only evidence update. Commit `8d840aa` is exactly `refactor: converge application-owned dependencies`; its scope is the four requested production modules plus `tests/app/test_container.py`, `tests/test_sync.py`, and the two notepad updates. No protected source/config/README/AGENTS files are in the commit.
- Positive evidence: `mise exec -- python -m pytest tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/integration/test_lan_server.py tests/application/test_local_library_service.py -q` returned `144 passed, 1 skipped in 6.85s`; `mise exec -- python -m pytest tests/ -q` returned `389 passed, 1 skipped in 14.17s`; targeted `ruff check` passed; targeted `black --check` reported 7 files unchanged; `git diff --check 8d840aa^ 8d840aa` passed.
- Positive real smoke: a temporary-root `Container` imported a valid PNG, then observed Sync and LAN command dependencies all had object identity with the Container's config/db/assets/manifest/library, DB count was 1, manifest existed, every path was under the temporary root, and cleanup removed the root. After `Container.close()`, `create_sync_service`, `create_lan_server`, `create_import_service`, and `create_local_library_service` all raised `RuntimeError("container is shut down")`.
- Positive compatibility probe: explicit falsey planning DB and falsey LAN config/db/assets/manifest/library objects were retained with `is not None` fallback checks; singleton access was patched to raise and planning completed without touching it. The first probe harness was malformed/incomplete twice (Python `SyntaxError` / missing fake `build`), then the corrected probe passed; those harness failures were not product failures.
- Blocking finding: `Container.create_hotkey()` and `Container.create_tray()` at `src/ohmymeme/app/container.py:127-131` do not call `_ensure_open()`. A direct runtime probe showed both accepted creation after `Container.close()`, while service factories rejected it. This violates the strict acceptance phrase “Container close 后不得接收新服务” if these owned factories are included, and current tests only cover `create_sync_service()`; verdict is `needs-fix`, not confirmed.
- Compatibility/coverage gap: no test or smoke exercised real `push()`/`pull()` with explicit config/db/assets/manifest while `library=None`; source still chooses `_default_library()` in those paths (`src/ohmymeme/services/sync/service.py:678-690`, `902-910`), so the stated “library=None 兼容构造” behavior is not independently proven for non-default resources. Legacy module facades remain intentionally singleton-backed, but isolation from this mixed-argument combination is unproven.
- Malformed metadata observable: a LAN `push_manifest` with a dict-shaped payload containing malformed nested metadata returned success when the injected library accepted it; only non-dict top-level input is rejected by `commands.py:146-155`. Existing full tests cover top-level malformed input and public failure, not nested schema validation. This is a residual robustness gap rather than a newly demonstrated Task 4 regression.
- LSP: all changed-path `lsp_diagnostics` requests were rejected because the LSP client request cwd is the main worktree and excludes this linked worktree; no clean LSP result is claimed. Cleanup receipt: the temporary smoke roots were removed; no production/test/plan/README/AGENTS/protected files were modified by verification.
- Final verdict for checkbox 4: **needs-fix / BLOCKED from confirmed**. Tests, lint, formatting, commit scope, real temporary identity/isolation, and covered close gates pass, but the observed post-close hotkey/tray admission is a concrete lifecycle acceptance failure; explicit `library=None` mixed-resource behavior also lacks proof.

## 2026-08-27 close-admission follow-up

- Reproduced the verifier defect before editing: a real temporary `Container` accepted both `create_hotkey()` and `create_tray(...)` after `close()`, while the other service factories raised `RuntimeError("container is shut down")`.
- Added separate failing-first tests for both platform factories. The minimal fix wraps each factory in the existing `_close_lock` and `_ensure_open()` admission pattern; no platform behavior or callback signature changed.
- Post-fix real temporary smoke observed `container is shut down` for hotkey, tray, sync, LAN, import, and local-library factories. Serial targeted suite passed `151 passed, 1 skipped`; serial full suite passed `391 passed, 1 skipped`.
- A deliberately parallel targeted/full run produced six known LAN fixed-port/global-state races; the required serial rerun passed. Ruff and black passed. LSP remained unavailable because the client rejects linked-worktree paths. No protected files, protocol, schema, manifest format, config semantics, or unrelated modules changed.

## 2026-08-27 explicit-library fallback follow-up

- Reproduced the independent verifier defects before editing: `pull(config=..., db=..., assets=..., manifest=..., library=None)` entered the no-argument `_default_library()` path, and a falsey injected library was treated as absent by `push()` truthiness checks.
- Added failing-first Sync regression tests for explicit-resource library provisioning and falsey library preservation. `_default_library()` now accepts the explicit resource set, while calls without resources retain the singleton-backed legacy facade.
- `push()` and `pull()` now use `library is not None`; explicit-resource pull provisions its compatibility library from the supplied config/db/assets/manifest, without singleton access. No nested LAN manifest validation was added because it was not necessary for the observed lifecycle defect.
- Serial Sync/Container/LAN/local-library/integration targeted suite passed `153 passed, 1 skipped`; full suite passed `392 passed, 1 skipped` in the post-fix run. Ruff and black passed. One earlier full-suite updater assertion was a known timing race and the isolated updater test passed on rerun.
- Real explicit-resource smoke initially had a malformed one-line Python harness (`SyntaxError`), then the corrected harness passed: singleton config/db sentinels were not accessed, `_default_library` captured the exact supplied config/db/assets/manifest, and the temporary root was removed.

## 2026-08-27 independent Task 4 follow-up verification

- Scope was rechecked only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`; HEAD is `9bd2ea3` on `architecture-optimization-baseline`, native worktree status was clean before this append, and the main worktree still contained only the four pre-existing protected dirty paths. `git show --stat` confirmed `8d840aa` is exactly `refactor: converge application-owned dependencies` and `9bd2ea3` is the close-admission follow-up; no reset, stash, discard, amend, force, commit, or main-worktree test was used.
- Source/history inspection covered every Task 4 production diff line in `services/sync/service.py`, `services/sync/planning.py`, `services/lan/commands.py`, `core/manifest.py` (unchanged in 8d840aa but inspected), plus the Container follow-up in `app/container.py`, and all changed test lines in `tests/app/test_container.py` and `tests/test_sync.py`. Normal Container construction passes identical `config`, `db`, `assets`, `manifest`, and `library` objects into Sync/LAN; module-level facades remain singleton-backed for legacy callers.
- Required serial targeted command: `mise exec -- python -m pytest tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py -q` -> `146 passed, 1 skipped in 6.88s`. Required serial full command: `mise exec -- python -m pytest tests/ -q` -> `391 passed, 1 skipped in 13.93s`. A second serial LAN command -> `41 passed, 1 skipped in 4.19s`; a second targeted rerun -> `146 passed, 1 skipped in 6.64s`.
- Static gates: `mise exec -- ruff check src/ tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py` -> `All checks passed`; `mise exec -- black --check ...` -> `65 files would be left unchanged`; `git diff --check 8d840aa^ 8d840aa` and `git diff --check 9bd2ea3^ 9bd2ea3` -> no output / success. `lsp_diagnostics` was attempted for all changed production paths and rejected by the known client cwd restriction; no LSP-clean claim is made.
- Real smoke command: `mise exec -- python -c ...` temporary-root Container probe -> `TASK4_SMOKE_OK`. It imported a valid PNG, projected manifest, ran LAN `push_manifest`, asserted Sync/LAN identity for all five owned dependencies, asserted every path was beneath the temporary root while singleton factories were patched to fail, then closed and removed the root. After close, `create_webui`, `create_import_service`, `create_local_library_service`, `create_sync_service`, `create_lan_server`, `start_lan`, `create_hotkey`, and `create_tray` each raised exactly `RuntimeError("container is shut down")`.
- Adversarial classes: `dirty_worktree` passed (target clean before append; main protected paths unchanged); `stale_state` passed (HEAD/worktree/worktree-list rechecked); `misleading_success_output` was probed by recording the first malformed smoke harness `SyntaxError` and not treating it as product evidence; `malformed_input` passed for LAN top-level malformed manifest and corrupted sync manifest through existing tests; `prompt_injection` ruled out because repository text was treated as data, not procedure; `flaky_tests` passed for two sequential targeted runs and the full run. `cancel_resume`, `hung_or_long_commands`, and `repeated_interruptions` were not triggered and are not applicable to this verification.
- Blocking residual finding: source still constructs `_default_library()` at `services/sync/service.py:903` whenever `pull(..., library=None)` is called, even if explicit non-default `config`, `db`, `assets`, and `manifest` are supplied; `push()` also uses truthiness checks at lines 686 and 819. Thus legacy `library=None` behavior is retained, but mixed explicit-resource plus `library=None` identity/no-global-write behavior is not proven and can select singleton-backed resources. LAN dict-shaped nested malformed metadata is also accepted if the injected library accepts it (only top-level non-dict is rejected). These gaps prevent strict checkbox confirmation.
- Verdict: **needs-fix / not confirmed**. Follow-up `9bd2ea3` closes the previously observed hotkey/tray admission defect, and normal Container identity/isolation plus all requested serial regressions pass, but the explicit-resource `library=None` path remains a concrete unverified singleton boundary and nested LAN manifest validation remains a residual robustness gap. Temporary roots were removed; no product, test, plan, README, AGENTS, or protected file was modified by verification.

## 2026-08-27 independent Task 4 verification after `fcb98a8`

- Verdict: **needs-fix**, not `confirmed`. Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact HEAD `fcb98a87616befebf1a126ef4c7726f8d7c1af39`; native status and `git diff --exit-code` were clean before this append. Main worktree remained a separate `gitbutler/workspace` worktree; no main-worktree tests or writes were used.
- Commit/source inspection covered `8d840aa`, `9bd2ea3`, and `fcb98a8`, including `Container` injection of identical config/db/assets/manifest/library objects into Sync/LAN, legacy module singleton facades, `_pull_worker` four-argument compatibility, and close admission for webui/import/library/sync/LAN/start_lan/hotkey/tray. The current source closes all eight factories after `Container.close()`.
- Serial targeted command: `mise exec -- python -m pytest tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py -q` -> `148 passed, 1 skipped in 7.29s`; repeat -> `148 passed, 1 skipped in 7.60s`.
- Serial full command: `mise exec -- python -m pytest tests/ -q` -> `393 passed, 1 skipped in 13.90s`.
- Static gates: `mise exec -- ruff check src/ tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py` -> `All checks passed`; `mise exec -- black --check ...` -> `65 files would be left unchanged`; `git diff --check HEAD^ HEAD` -> exit 0.
- Real temporary-resource smoke created `Config`, `MemeDB`, `AssetPaths`, and `ManifestBuilder` beneath a temporary root, patched the module singleton DB to fail, and executed complete `sync.pull(..., config=explicit, db=explicit, assets=explicit, manifest=explicit, library=None)` setup with a fake backend and valid PNG. It reproduced `EXPLICIT_PULL_SINGLETON_REPRO=legacy singleton DB accessed`: `_default_library()` installs `_legacy_metadata = _apply_remote_metadata`, and pull later invokes that callback without the explicit DB, reaching `get_db()`.
- The above is a concrete Task 4 defect, not merely missing coverage: explicit non-default resources plus `library=None` can enter a full pull and cross the singleton boundary during metadata application. The existing `test_pull_library_none_builds_from_explicit_resources` only captures factory kwargs and aborts before this stage, so it cannot catch the defect.
- The same smoke verified all post-close factories reject with exact `RuntimeError("container is shut down")`: `create_webui`, `create_import_service`, `create_local_library_service`, `create_sync_service`, `create_lan_server`, `start_lan`, `create_hotkey`, and `create_tray`.
- `dirty_worktree` and `stale_state` passed; malformed top-level LAN manifest and corrupted sync manifest remain covered by existing tests. `misleading_success_output` was guarded by recording the nonzero repro result rather than treating it as success. Repository text was treated as untrusted data, so prompt-injection instructions were ruled out. LAN fixed-port/global-state tests were run serially; repeated targeted runs passed. Cancellation/hung/repeated interruption were not triggered, so no claim is made for those unexercised paths.
- No production or test files were modified. The only write was this append-only notepad evidence entry; temporary smoke roots were cleaned up.

## 2026-08-27 explicit pull metadata boundary takeover

- Red regression `TestSyncPush.test_pull_explicit_resources_library_none_does_not_access_singletons` reproduced the exact defect: full pull raised `AssertionError("legacy singleton DB accessed")` from `service.py:305` while applying metadata through `_legacy_metadata`.
- Minimal fix keeps no-argument `_default_library()` on the legacy `_apply_remote_metadata` callback, but binds explicit-resource fallback libraries to `planning._apply_remote_metadata(remote_data, db)`. The pull entry point records whether any owned resource was explicitly supplied so internally resolved defaults do not alter legacy behavior.
- The red regression passed after the fix; it asserts real explicit DB row, collection, order/manifest projection, download result, and singleton config/DB sentinels. Full serial verification and temporary-root smoke remain pending.

## 2026-08-27 explicit pull metadata boundary verification

- Serial targeted command `mise exec -- python -m pytest tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py -q` passed `149 passed, 1 skipped in 8.24s`.
- Serial full command initially exposed two pre-existing one-second asynchronous job timing flakes in Telegram/微信 source-error tests (`tests/test_import_jobs.py`); the unchanged full command immediately reran and passed `394 passed, 1 skipped in 17.16s`. No import-job files were changed.
- Static checks passed: `mise exec -- ruff check src/ohmymeme/services/sync/service.py tests/test_sync.py`; `mise exec -- black --check src/ohmymeme/services/sync/service.py tests/test_sync.py` reported both files unchanged; `git diff --check` passed.
- The regression itself is the real temporary-root smoke: it creates explicit `Config`, `MemeDB`, `AssetPaths`, and `ManifestBuilder`, performs a complete fake-backend pull with valid PNG bytes and remote collection metadata, patches sync singleton config/DB factories to raise, then asserts download result, DB row, collection, ordering, and manifest under the temporary root. Pytest cleanup closes the explicit DB and removes the root.
- LSP diagnostics were attempted for both changed Python files and rejected with the known linked-worktree request-cwd restriction; no LSP-clean claim is made. Existing service/test files exceed the generic 250 pure-LOC advisory threshold before this narrow change; no unrelated refactor was introduced.

## 2026-08-27 independent Task 4 adversarial confirmation after `a1eeb17` and `524476b`

- Verification stayed in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`; HEAD was `524476bf8490b44710e9f87cb28dc6874e1214e8`, and native `git status --short --untracked-files=all` plus `git diff --exit-code` were clean before this append. `git worktree list --porcelain` confirmed the main worktree remained `D:/UserFiles/Development/Projects/OhMyMeme` on `gitbutler/workspace`; no main-worktree command or write was used. Codegraph was attempted once as required but reported no `.codegraph` index for this linked worktree; raw source review then covered the indexed-flow substitute and full commit scopes.
- Commit inspection covered every relevant line in `8d840aa`, `9bd2ea3`, `fcb98a8`, `a1eeb17`, and `524476b`. `a1eeb17` changes only `services/sync/service.py` and `tests/test_sync.py`; `524476b` changes only the four architecture notepads. `git diff --check` passed for each relevant commit pair.
- Exact targeted sentinel test: `mise exec -- python -m pytest tests/test_sync.py::TestSyncPush::test_pull_explicit_resources_library_none_does_not_access_singletons tests/app/test_container.py::test_container_services_share_explicit_dependency_identity tests/app/test_container.py::test_container_rejects_new_service_admission_after_close tests/app/test_container.py::test_container_rejects_new_hotkey_after_close tests/app/test_container.py::test_container_rejects_new_tray_after_close -q` -> `5 passed in 1.25s`.
- Extended serial targeted suite: `mise exec -- python -m pytest tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py -q` -> `149 passed, 1 skipped in 10.78s`; Sync-only rerun -> `62 passed in 0.97s`; remaining targeted application/LAN/integration rerun -> `87 passed, 1 skipped in 6.83s`; malformed/corrupt manifest and LAN compatible-shape probes -> `3 passed in 0.42s`.
- Real explicit full-pull smoke (temporary root, explicit `Config(config_path, data_root)`, `MemeDB`, `AssetPaths`, `ManifestBuilder`, fake backend, `library=None`, singleton `get_config/get_db` patched to raise) printed `TASK4_EXPLICIT_PULL_SMOKE_OK`. It asserted two downloaded PNGs, remote order `b.png,a.png` in DB and projected manifest, both remote collections, exact membership `remote-a:a.png`, `remote-both:a.png,b.png`, all config/data/db/cache/manifest paths beneath the temporary root, and no singleton access. The first exploratory harness intentionally failed on the API default `Config(path)` using default LOCALAPPDATA data (`AssertionError`), and a second malformed harness failed with a Pillow size argument error; neither was treated as product evidence. The corrected harness passed and all temporary roots were removed.
- Legacy compatibility probe created a temporary explicit config/db, patched the legacy singleton factories, called `_default_library()` with no resources, and verified its `_legacy_metadata` remained the legacy facade and invoked the patched callback: `TASK4_LEGACY_FACADE_OK`. Source review confirms no-argument `pull()` still resolves singleton config/db/assets and uses `_apply_remote_metadata`.
- Closed-container admission is confirmed by targeted tests and source review: `create_webui`, `create_import_service`, `create_local_library_service`, `create_sync_service`, `create_lan_server`, `start_lan`, `create_hotkey`, and `create_tray` all use `_ensure_open()` under `_close_lock`; the three direct close tests and identity test passed.
- Static gates: `mise exec -- ruff check src/ tests/app/test_container.py tests/test_sync.py tests/test_lan.py tests/application/test_local_library_service.py tests/integration/test_lan_server.py` -> `All checks passed`; `mise exec -- black --check ...` -> `65 files would be left unchanged`. `lsp_diagnostics` for `service.py` and `test_sync.py` was rejected by the known request-cwd restriction, so no LSP-clean claim is made.
- Full suite: two unchanged serial runs of `mise exec -- python -m pytest tests/ -q` each produced `393 passed, 1 skipped` and one failure at `tests/test_import_jobs.py::test_adb_source_error_maps_to_job_error`, where the one-second `_wait_for_job` deadline expired while the worker logged `qq import: ADB 下载失败`. This is outside Task 4, reproducible on immediate rerun, and no import-job files were changed; therefore full-suite green is not claimed.
- Adversarial classes: `dirty_worktree` and `stale_state` passed before the append; `misleading_success_output` passed by recording both malformed harness failures and the full-suite nonzero result instead of trusting success claims; `malformed_input` passed via corrupted sync manifest and malformed LAN shape probes; `prompt_injection` ruled out because repository text and notepads were treated as untrusted data, not procedure; `flaky_tests` is a pre-existing full-suite timing failure reproduced twice, while Task 4 targeted reruns passed. `cancel_resume`, `hung_or_long_commands`, and `repeated_interruptions` were not triggered and remain not applicable.
- Verdict: **confirmed** for the requested Task 4 checkbox. The prior blocker is closed: complete explicit-resource `pull(..., library=None)` uses the injected metadata DB and paths without singleton `get_db/get_config`; legacy no-argument fallback remains singleton-compatible; all closed-container factories reject admission. Residual non-blocking observations are the known one-second import-job test flake, partial-explicit-resource fallback semantics, module-global `_pull_library` concurrency risk, and weak nested manifest validation; none was introduced or changed by these commits.

## 2026-08-27 dependency ownership test task

- Unchanged baseline characterization was non-deterministic in the fixed-port LAN suites: separate serial attempts showed `4`, `5`, and `7` LAN failures from stale/global server state; full-suite attempts also reproduced the known one-second import-job timing failures. These are pre-existing observations and no production or unrelated test files were changed.
- After the scoped test additions, the required serial targeted suite passed `154 passed, 1 skipped in 8.63s`; split reruns passed `112 passed` for Container/local-library/Sync and `42 passed, 1 skipped` for LAN/integration. `ruff check` and `black --check` passed for all five test files. Linked-worktree `lsp_diagnostics` was attempted for all five paths and rejected by the request-cwd restriction.

## 2026-08-27 independent Task 5 verification

- Scope was verified only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`; HEAD is `da41b51` (`test: verify dependency ownership boundaries`), native worktree status was clean before this append, and the main worktree retained its four unrelated protected dirty files. Commit scope is limited to the two architecture notepads plus `tests/app/test_container.py`, `tests/application/test_local_library_service.py`, `tests/test_lan.py`, and `tests/test_sync.py`; `tests/integration/test_lan_server.py` was reviewed but not changed by this commit.
- Positive evidence: serial targeted `mise exec -- python -m pytest tests/app/test_container.py tests/application/test_local_library_service.py tests/test_sync.py tests/test_lan.py tests/integration/test_lan_server.py -q` returned `154 passed, 1 skipped in 7.93s`; serial full `mise exec -- python -m pytest tests/ -q` returned `399 passed, 1 skipped in 15.52s`; targeted `ruff check` passed; targeted `black --check` reported all five files unchanged; native `git diff --exit-code`, `git diff --check`, and worktree status were clean before this append.
- Substantive coverage confirmed by source/assertion inspection: Container tests exercise factory admission/close races, direct close waiting, LAN server reuse, complete config/db/assets/manifest/library identity, two-container isolation, singleton sentinels, and real import/cache/database/manifest writes under temporary roots. Sync tests exercise no-resource legacy metadata callback, explicit dependency forwarding, falsey-library preservation, and a complete explicit-root pull with singleton config/DB sentinels. LAN tests exercise public manifest projection/apply boundaries, malformed top-level manifest response, public failure response, explicit handler identity, and real push-manifest success shape. The existing LAN integration tests add rejected dispatch, replay, bounded replay cache, and late confirmation isolation.
- Real smoke `mise exec -- python -` returned `TASK5_SMOKE_OK`: a temporary-root Container imported a valid PNG, created Sync/LAN services, observed all five owned dependency identities and manifest location under the temporary root, then confirmed all six checked factories reject after close with exact `RuntimeError("container is shut down")`. The temporary root was removed by the context manager.
- Adversarial checks: a malformed pure-LOC PowerShell/Python quoting harness failed with `SyntaxError` and was not treated as evidence; malformed sync/LAN inputs are covered by passing tests; repository instructions/notepads were treated as untrusted data, not execution procedure; no product/test/plan/README/AGENTS/protected file was modified. `lsp_diagnostics` for all five linked-worktree test paths was rejected because the client request cwd is the main worktree, so no LSP-clean claim is made.
- **Blocking flaky-test finding:** deliberately concurrent execution of `tests/test_lan.py` and `tests/integration/test_lan_server.py` caused both processes to fail (`8` and `6` failures observed), including wrong config state, connection resets, and cross-process confirmation/server interference. Both suites hard-code TCP/UDP port `17990` and mutate module-global LAN/config/DB state (`tests/test_lan.py:29,132-155`; `tests/integration/test_lan_server.py:17-36`). The required serial targeted run passes, but the tests are not isolated for concurrent worktrees/CI workers; this remains exact `needs-fix` rather than `confirmed` for the strict flaky fixed-port acceptance.
- Verdict: **needs-fix / not confirmed**. The requested ownership and temporary-root assertions are substantive and serial QA is green, but fixed-port/global-state concurrency is a reproducible test-suite defect. No code or test fix was made per the read-only review constraint.

## 2026-08-28 importer ownership

- Linked-worktree LSP remains unavailable because the client rejects paths outside its request cwd; pytest, ruff, black, diff checks, and filesystem assertions are the available evidence. Existing importer cleanup tests use serial global-state execution as required.

## 2026-08-28 independent Task 10 verification

- Verdict: **needs-fix / not confirmed**. Verification stayed in `C:/Users/abbey/AppData/Local/Temp/opencode/OhMyMeme-architecture-optimization-baseline` at exact HEAD `7c880caa7dababc74252c12350a0826ba5bb4b35`; `git show --stat` confirms the commit changes only `learnings.md`, `tests/presentation/test_bridge_compat.py`, and `tests/test_startup.py`. The linked worktree had four pre-existing dirty product paths (`handlers.py`, `window_manager.py`, `settings.js`, and `src/webui/settings.js`) plus this append-only notepad; no source/test/plan/README/AGENTS/protected file was modified by verification.
- Focused ABI/startup tests passed twice: `mise exec -- python -m pytest tests/presentation/test_bridge_compat.py tests/test_startup.py -q` -> `62 passed` each run. `mise exec -- ruff check src/ tests/presentation/test_bridge_compat.py tests/test_startup.py` passed; `black --check` reported `63 files would be left unchanged`; commit `git diff --check` passed. Linked-worktree `lsp_diagnostics` was attempted for both changed tests and rejected by the known request-cwd restriction, so no LSP-clean claim is made.
- Serial full-suite runs were non-deterministic: first run -> `453 passed, 1 skipped, 2 failed` (`tests/test_lan.py::test_push_manifest_returns_compatible_success_and_failure_shapes` hit the default `C:\Users\abbey\AppData\Local\OhMyMeme\meme-index.json.tmp` permission/global-state leak, and `tests/test_updater.py::test_force_triggers_recheck_despite_fresh_cache` missed its one-second async window); immediate second run -> `455 passed, 1 skipped`. This is a pre-existing suite flake/global-state observation, not a Task 10 test failure, and no files were changed to mask it.
- Source ABI inspection found a concrete coverage defect in the new matrix: `JsApi.start_download` is annotated/implemented as returning `bool`, `JsApi.sync_test` as `str`, and `JsApi.import_memes` as `bool` (`window_manager.py:360,395,398`), but `test_js_facade_routes_named_sync_import_update_and_window_abi` asserts all three are `dict` because `_RecordingHandler` returns a generic dict. Thus the test does not verify the exact return types it claims to lock and would accept an incorrect envelope. The matrix also lacks actual error-envelope assertions for main/settings sync/import/update/window calls; its `isinstance(..., dict)` checks cannot distinguish `{"ok": False, "error": ...}` from the generic success placeholder.
- The missing-method test only exercises `getattr(SimpleNamespace, ...)` locally (`tests/test_startup.py:652-661`), not a production façade/dynamic bridge lookup. It proves Python's `None` expression but does not prove the runtime's legacy method-dispatch failure shape. The settings test does assert exact refresh expressions (`refreshMemes();`, `refreshTags();`, `refreshCollections();`) and importer argument slices/defaults, while native drag tests independently prove Linux and missing WinForms return `False` without `schedule_hide`.
- Protected main-worktree hashes were rechecked and remained `43FD030EAC87BEEA69D591A13D68F0461A16AABB0613F395AA4CC60503686870` (`window_manager.py`), `B789BB0186F0C305E9BA23F1E45329DE28ED2734B2BF27EB21DEC64E49B0076C` (`settings/core/init.js`), `76D1C196DFB38F1F19B91FAE2110D54EAC90ED1240CBFA9B676266974944EC06` (`settings/features/sync/settings.js`), and `96722C673ED031F9F9B7EBB89BD32DAAB77F315FB7AB0ECA694B400BD3223517` (`src/webui/settings.js`). No tracked generated artifact was added; the ignored `src/webui/dist/ohmymeme.js` and `.pytest_cache` were pre-existing/runtime-generated and no untracked files remained.
- UltraQA classes: `dirty_worktree`, `stale_state`, `misleading_success_output`, `malformed_input`, `flaky_tests`, and `hung_or_long_commands` were exercised or explicitly checked; the full-suite nonzero result was recorded rather than treated as success. `prompt_injection` was ruled out because repository/notepad text was treated as data, not instructions. `cancel_resume` and `repeated_interruptions` were not triggered and remain not applicable to this read-only bridge ABI verification.

## 2026-08-27 LAN test isolation follow-up

- Reproduced the reported defect before editing with two concurrent pytest processes: `tests/test_lan.py` exited `1` with `test_handshake_success` failing, and `tests/integration/test_lan_server.py` exited `1` with `test_late_confirmation_does_not_authorize_next_connection` failing. The captured output showed connection resets and cross-process confirmation/config interference; artifacts were saved under `%TEMP%/ohmm-lan-concurrent`.
- Replaced the test-only fixed `17990` fixture port with a loopback ephemeral port in both LAN modules. All socket clients now use the fixture-selected port, and fixture teardown stops LAN, restores config/DB/callback state, and closes the temporary DB. The no-secret path now also restores callback state and closes its client socket/DB.
- Added explicit fixture isolation assertions (`TEST_PORT > 0`, `TEST_PORT != 17990`, config port equality). Malformed protocol assertions remain unchanged. No production, protocol, config semantics, README, AGENTS, plan, or protected files were modified.
- Three concurrent rounds passed: each `tests/test_lan.py` run reported `39 passed, 1 skipped`, each integration run reported `5 passed`; artifacts are under `%TEMP%/ohmm-lan-concurrent-fixed-repeat`. Serial LAN/integration passed `44 passed, 1 skipped`; full targeted five-file suite passed `156 passed, 1 skipped`.
- Full suite after the fix reported `399 passed, 1 skipped` with two pre-existing one-second `tests/test_import_jobs.py` timing failures (`telegram` and `adb_qq`); no LAN failures occurred. `ruff check` and `black --check` passed for both changed LAN test files. LSP remains unavailable for linked-worktree paths due to request-cwd restriction.

## 2026-08-27 independent Task 5 verification after `7f353dd`

- Protected dirty-worktree check: main worktree is `D:/UserFiles/Development/Projects/OhMyMeme` and still contains only `window_manager.py`, settings `core/init.js`, settings sync `settings.js`, and `webui/settings.js`; linked worktree is `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `7f353dd`, clean before this append. Main modified blob hashes were `e0f7c67460380fa07db24350f484e06c9d6f6b73`, `36d5f9910912cfd6b7337eb22602833fd6b05be6`, `e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `1e66f686f374cbefacf7c0c86dc4ddf53b381728`; no `baseline-evidence-2026-08-27.txt` was present in the searched project/temp/cache trees, so no attribution to this verifier is claimed and no protected file was touched.
- Source/diff review covered every `7f353dd` line: both LAN fixtures use ephemeral loopback port probes, restore config/DB/callback/secret state in `finally`, close DBs and sockets, and keep malformed protocol assertions. Task 5 coverage remains substantive for Container factory/close races and identity, LocalLibraryService projection/import roots, Sync explicit graph and legacy callback, LAN public manifest boundary and malformed inputs, and integration rejection/replay/late-confirmation behavior. Codegraph was unavailable because linked worktree has no `.codegraph` index; linked LSP requests remain rejected by request-cwd restriction.
- The first resumed concurrent batch exposed a real fixture start race (`tests/test_lan.py` had 1 failure and 1 setup error while integration had 5 passes). The scoped follow-up changed only both LAN test fixtures: after ephemeral probe, `lan.start()` retries with a fresh port up to 10 times; config `lan_port` is set only after successful binding; teardown asserts stopped state. This addresses the observed bind TOCTOU without changing production LAN behavior.
- Verification after the follow-up: serial LAN/integration `44 passed, 1 skipped`; three simultaneous two-process repetitions each reported `39 passed, 1 skipped` and `5 passed`; five-file targeted suite `156 passed, 1 skipped`; targeted `ruff` passed; targeted `black --check` reported both files unchanged; `git diff --check` passed.
- Real temporary-root smoke passed after correcting an initial invalid assertion that `Container.close()` removes the caller-owned root: the corrected `TemporaryDirectory` smoke verified imported DB row/cache/manifest under the supplied root, owned library DB/assets identity, and automatic temporary-root cleanup (`TASK5_SMOKE_OK`).
- Full suite first run returned `400 passed, 1 failed, 1 skipped`; the sole failure was unchanged `tests/test_import_jobs.py::test_adb_source_error_maps_to_job_error` at its one-second `_wait_for_job` deadline. LAN tests had no failures. This is a known unrelated importer timing flake and is not counted as full-suite green.
- Adversarial classes: dirty scope/stale state passed; misleading success was rejected for both the initial concurrent failure and invalid smoke assertion; malformed input remained covered by LAN top-level manifest/file and sync corrupt-manifest tests; prompt-injection text in repository/notepads was treated as data; cancel/resume, hung commands, and repeated interruptions were not triggered.

## 2026-08-27 definitive Task 5 adversarial verdict

- Protected-file attribution was completed before this verdict: the main worktree still has exactly the four pre-existing protected paths, with hashes `e0f7c67460380fa07db24350f484e06c9d6f6b73`, `36d5f9910912cfd6b7337eb22602833fd6b05be6`, `e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `1e66f686f374cbefacf7c0c86dc4ddf53b381728`; the linked worktree was clean at `a6882d5` before this append. No protected file was read-modified-written, restored, discarded, or committed by verification. The requested `baseline-evidence-2026-08-27.txt` was not found.
- Three concurrent repetitions of independent `tests/test_lan.py` and `tests/integration/test_lan_server.py` processes all passed: each unit process reported `39 passed, 1 skipped`, each integration process reported `5 passed`. The scoped fixture retry fix therefore removes the observed concurrent bind failure in these repetitions.
- The exact serial LAN defect remains unresolved: one serial `mise exec -- python -m pytest tests/test_lan.py tests/integration/test_lan_server.py -q` run failed `test_push_pull_file` (`43 passed, 1 skipped, 1 failed`), while the LAN handler attempted to replace `C:\Users\abbey\AppData\Local\OhMyMeme\meme-index.json.tmp` and returned `ok=False` instead of writing the fixture `tmp_path` manifest. This is a concrete test-only global-state/object-graph leak, not a production failure: the fixture mutates `config_module._config`/`database._db`, while the module-level LAN singleton and command construction can retain or reach default resources. An immediate serial rerun passed `44 passed, 1 skipped`, confirming flakiness rather than resolution.
- Five-file targeted suite passed `156 passed, 1 skipped`; targeted `ruff` and `black --check` passed; `git diff --check` passed. Full-suite reruns each returned `400 passed, 1 failed, 1 skipped`, with only the known unrelated `tests/test_import_jobs.py::test_adb_source_error_maps_to_job_error` one-second timing failure and no LAN failure. Real temporary-root smoke passed as `TASK5_SMOKE_OK`; the first malformed one-line smoke harness and the earlier incorrect root-removal assertion were rejected as harness errors, not evidence.
- Definitive verdict: **needs-fix / not confirmed**. Concurrent-port collision was mitigated and passed three repetitions, but the required serial LAN suite still has a reproducible intermittent default-manifest-path leak. The exact remaining acceptance defect is fixture/global singleton isolation, not the ephemeral-port retry. No plan checkbox was changed.

## 2026-08-27 LAN object-graph leakage follow-up

- Failing-first sentinel tests proved both LAN fixtures constructed their running `lan._server` through the legacy no-argument factory: `library._config` was `None` instead of the fixture `Config`. This was the concrete route by which a later handler could reach the default manifest path.
- Fixed only test fixtures to construct `AssetPaths`, `ManifestBuilder`, `ImageImportService`, `LocalLibraryService(config=cfg)`, and `LanServer` explicitly, then start that server instance. `test_push_pull_file` now patches LAN command singleton `get_config/get_db` to fail and asserts command config/db/assets/manifest/library all belong to the fixture graph.
- A first broad singleton sentinel caused six unrelated compatibility tests that intentionally construct `LanServer()` with legacy defaults to fail; the sentinel was narrowed to `test_push_pull_file`, preserving protocol/legacy assertions while keeping the leak proof deterministic.
- Validation: `test_push_pull_file` repeated 10 times passed; serial LAN/integration passed `46 passed, 1 skipped`; `ruff` and `black --check` passed for changed LAN tests. One full serial LAN repeat encountered fixture startup errors after a separate test process left the global LAN server in an error state; an immediate serial rerun passed, so further cleanup/startup verification remains required before confirmation.

## 2026-08-27 definitive LAN object-graph isolation fix

- Failing-first tests `test_lan_fixture_injects_config_into_owned_library` failed before the fix with `library._config is None`, proving `lan.start()` had constructed a legacy no-argument handler/library and could reach default user data. The tests now explicitly construct and start a fixture-owned `LanServer` graph (`Config`, `MemeDB`, `AssetPaths`, `ManifestBuilder`, `ImageImportService`, `LocalLibraryService(config=cfg)`).
- `test_push_pull_file` patches only the command module's `get_config` and `get_db` singleton entry points to fail, then asserts command config/db/assets/manifest/library identity and performs real push/pull protocol operations. Legacy compatibility tests that intentionally call `LanServer()` remain unmodified in behavior.
- Repeated `test_push_pull_file` (10 runs) passed. Serial LAN/integration repeated 3 rounds passed `46 passed, 1 skipped` each; concurrent unit/integration processes repeated 3 rounds passed `40 passed, 1 skipped` and `6 passed` each. The default manifest temp path `C:\Users\abbey\AppData\Local\OhMyMeme\meme-index.json.tmp` was absent before and after all three serial rounds. Artifacts: `%TEMP%/ohmm-lan-final-serial` and `%TEMP%/ohmm-lan-final-concurrent`.
- Five-file targeted suite passed `158 passed, 1 skipped`; full suite passed `403 passed, 1 skipped`. `ruff`, `black --check`, and `git diff --check` passed. Protected main-worktree file hashes remained unchanged; final cleanup left linked worktree changes limited to two LAN tests and this append-only notepad.

## 2026-08-27 independent Task 5 confirmation after `cef3e39`

- Verification stayed exclusively in linked worktree `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`, at exact HEAD `cef3e398d2388731c426888b7fe8190983c70ee5`; native status was clean before this append. The main worktree remained `D:/UserFiles/Development/Projects/OhMyMeme` with exactly its four pre-existing protected dirty paths. No main-worktree tests, writes, reset/stash/discard/amend/force operations, or commits were used.
- Commit audit inspected every changed line in `da41b51`, `7f353dd`, `a6882d5`, and `cef3e39`, plus all five Task 5 test files: `tests/app/test_container.py`, `tests/application/test_local_library_service.py`, `tests/test_sync.py`, `tests/test_lan.py`, and `tests/integration/test_lan_server.py`. `da41b51` supplies ownership assertions; `7f353dd` and `a6882d5` replace fixed LAN ports with loopback ephemeral-port probes and harden teardown/retry; `cef3e39` makes both LAN fixtures construct and start a complete explicit `Config`/`MemeDB`/`AssetPaths`/`ManifestBuilder`/`ImageImportService`/`LocalLibraryService`/`LanServer` graph. Assertions cover Container factory/close race and LAN reuse, complete identity and two-root isolation, real local import/projection/recovery, Sync legacy and explicit-resource pull, LAN public-boundary/malformed/error envelopes/identity/real push-pull, and integration rejection/replay/late-confirmation behavior.
- Ten independent `mise exec -- python -m pytest tests/test_lan.py::test_push_pull_file -q` runs each returned `1 passed`. Before and after the batch, `%LOCALAPPDATA%/OhMyMeme/meme-index.json.tmp` was absent. `test_push_pull_file` patches LAN command `get_config` and `get_db` to fail, asserts handler `config`, `db`, `assets`, `manifest`, and `library` are fixture-owned, then completes real encrypted push/pull; no default manifest leak was observed.
- Serial LAN/integration command `mise exec -- python -m pytest tests/test_lan.py tests/integration/test_lan_server.py -q` returned `46 passed, 1 skipped in 4.24s`. Three concurrent rounds launched independent unit/integration pytest processes; each unit run returned `40 passed, 1 skipped` and each integration run returned `6 passed`. Ephemeral-port assertions require `TEST_PORT > 0`, `TEST_PORT != 17990`, and config `lan_port == TEST_PORT`; teardown stops the server, asserts `stopped`, restores config/DB/callback/secret state, and closes DBs/sockets.
- Five-file targeted command returned `158 passed, 1 skipped in 6.43s`; targeted `ruff check`, `black --check`, and `git diff --check` passed. Corrected real temporary-root smoke imported a valid PNG, created Sync/LAN services, asserted all five owned dependency identities and config/DB/cache/manifest paths beneath the supplied root, and printed `TASK5_REAL_SMOKE_OK`; `TemporaryDirectory` removed the root. Initial smoke failure was only an invalid assertion that `Container.close()` should delete the caller-owned root.
- Full suite first run returned `402 passed, 1 failed, 1 skipped` from the known one-second `tests/test_import_jobs.py` Telegram source-error timing race, with cascaded LAN fixture error-state setup failures. Immediate unchanged rerun returned `403 passed, 1 skipped in 14.53s`; no LAN failure. This unrelated flake is recorded, not hidden.
- Protected hashes matched in linked and main worktrees: `window_manager.py`=`e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js`=`36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js`=`e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js`=`1e66f686f374cbefacf7c0c86dc4ddf53b381728`. Linked `git diff --exit-code` and status were clean before this append; no tests/source/plan/README/AGENTS/protected file was modified by verification.
- `lsp_diagnostics` was attempted for all five linked test paths and rejected by the known request-cwd restriction; no LSP-clean claim is made. The five test modules exceed the generic 250 pure-LOC advisory threshold as pre-existing test modules; no refactor was made. Repository instructions/notepads were treated as data, malformed harness output was not accepted as evidence, and no cancel/resume or hung-command class was triggered.
- Task 6 lifecycle verification: baseline importer/job tests exposed the intended red seam where importer progress did not reach the JobManager snapshot. The implementation now adds `JobContext.snapshot()` and atomic `JobManager.try_start()` while preserving legacy `start()` and all UI progress getter shapes. Serial lifecycle tests passed `53`; importer/QQ/bridge tests passed `77`; targeted ruff and black passed. Full suite had `403 passed, 1 skipped, 1 error` from the pre-existing LAN fixture startup race, with no importer failure. LSP remained unavailable for linked-worktree paths.
- Verdict: **confirmed** for the exact Task 5 checkbox. Ownership-boundary coverage, Container factory/close race, identity reuse, LAN handler reuse, sync legacy compatibility, temporary-root isolation, and no-default-path-write behavior are evidenced. The prior intermittent `test_push_pull_file` default manifest leak is eliminated by the explicit LAN object graph and singleton sentinels.

## 2026-08-27 independent Task 6 adversarial lifecycle verification at `ce58caf`

- **Verdict: needs-fix / not confirmed.** Verification ran only in linked worktree `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`, where `HEAD=ce58caf9b490c17dca0250b1ad04f58eea1ad3af`, parent=`cef3e398d2388731c426888b7fe8190983c70ee5`, and native `git status --short --untracked-files=all` plus `git diff --exit-code` were clean before this append. `git show --name-status ce58caf` confirms scope is exactly the two architecture notepads, six requested production modules, and `tests/application/test_job_manager.py` plus `tests/test_import_jobs.py`. `git diff --check ce58caf^ ce58caf` passed.
- Source/diff review covered every Task 6 changed line. `JobManager` has the five stable types/resources (`import.telegram`/`telegram`, `import.douyin`/`douyin`, `import.wechat`/`wechat`, `import.adb_qq`/`adb`, `import.qqnt`/`qqnt`), immutable external `JobRecord` snapshots, and atomic `try_start()`. Telegram, Douyin, WeChat, ADB QQ, and QQNT project their existing UI dictionaries to `JobContext.snapshot()` with normalized progress; UI getters retain their legacy dictionaries and bridge start/cancel/getter methods retain their existing names. `job_manager=None` retains daemon-thread entry paths and produces no JobManager record, directly observed with Telegram (`job_manager_active=None`, UI status=`scanning`).
- Serial commands/results: `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py::test_qqnt_job_manager_preserves_terminal_error_and_single_flight tests/test_startup.py::test_qqnt_job_manager_cancel_propagates_to_worker tests/presentation/test_bridge_compat.py -q` -> `25 passed in 0.80s`; `mise exec -- python -m pytest tests/test_tg_stickers.py tests/test_adb_util.py tests/application/test_import_service.py tests/test_sync.py -q` -> `97 passed in 2.50s`; `mise exec -- python -m pytest tests/test_core.py tests/test_lan.py tests/integration/test_lan_server.py tests/presentation/test_bridge_compat.py -q` -> `91 passed, 1 skipped in 10.92s`; full `mise exec -- python -m pytest tests/ -q` -> `405 passed, 1 skipped in 21.50s`; separate serial LAN `mise exec -- python -m pytest tests/test_lan.py tests/integration/test_lan_server.py -q` -> `46 passed, 1 skipped in 7.07s`. Thus this run did not reproduce the historical LAN ephemeral-port race; it is recorded as an independently passing serial LAN result, not silently ignored.
- Static gates: `mise exec -- ruff check` over all six production and two changed test files -> `All checks passed`; `mise exec -- black --check` over the same set -> `8 files would be left unchanged`; changed-path LSP diagnostics were attempted in parallel but all were rejected by the known linked-worktree request-cwd restriction, so no LSP-clean claim is made. No source/test file was changed by this verifier.
- Feasible importer smoke: controlled workers showed Telegram, Douyin, WeChat, and ADB QQ each admit the first managed run, reject the duplicate, expose the declared resource tuple, and terminate `cancelled`; their exact legacy UI progress key sets were printed and retained. QQNT likewise returned `duplicate=False`, `resources=('qqnt',)`, managed terminal `cancelled`, UI `cancelled`. The probes used Event-controlled worker seams, no network/device/helper process, and `JobManager.shutdown(1)` after every run.
- **Blocker 1, misleading success / cancellation race:** deterministic JobManager probe started a worker blocked before `context.complete()`, called `manager.cancel(record.id)` (`True`), then released the worker. Observable result was `STATUS completed ACTIVE None`. Cancellation therefore loses to a later explicit `complete()`, contradicting cancellation-propagation acceptance if a cancellation request is expected to prevent a success terminal snapshot.
- **Blocker 2, repeated interruption / stale active state:** deterministic worker raised `KeyboardInterrupt`. It exited (`INT_WAIT True`) but left `STATUS running ACTIVE True`, printing the unhandled thread traceback. `_run()` catches `Exception` rather than `BaseException`, so `KeyboardInterrupt`/`SystemExit` can permanently retain the single-flight slot and a running snapshot after the thread is gone.
- **Blocker 3, stale-state / misleading UI terminal:** after `adb_qq.reset_qq_import(); _update_qq(status='done', progress=100)`, `cancel_qq_import()` changed the legacy UI state to `cancelled` (`ADB_DONE_CANCEL cancelled`) even though no active job exists. The public cancel API unconditionally mutates source progress state, so a stale/repeated cancel can rewrite a completed terminal result.
- UltraQA evidence: `cancel_resume` exercised managed cancellation on all five importer types but uncovered the complete-after-cancel terminal race; `stale_state` exercised stale ADB terminal cancellation and found mutation; `dirty_worktree` passed before the only append, while the main worktree remained its four protected dirty files only; `hung_or_long_commands` was bounded by 900s command limits and all suites completed, with existing Telegram executor/helper cancellation tests in the 97-test slice; `flaky_tests` had serial targeted, LAN, and complete suite passes in this run; `misleading_success_output` failed as above and no green suite was substituted for it; `repeated_interruptions` failed through the KeyboardInterrupt orphaned-active probe; `malformed_input` is covered by passing importer/bridge/core tests but Task 6 lacks a complete importer malformed callback/metadata matrix; `prompt_injection` was ruled out because repository/notepad contents were treated only as data.
- Cleanup receipt: no live controlled worker remained after each probe; JobManagers were shut down, all release events were signalled, the serial LAN artifact directory `%TEMP%/ohmm-lan-task6-serial` was removed, and no subprocess, executor, socket, temporary root, production/test artifact, protected main file, reset/stash/discard/amend/force operation, or main-worktree test was left behind. Main worktree still reports exactly its four pre-existing modified files. The linked worktree is intentionally dirty only for this append to `issues.md`.

## 2026-08-27 Task 6 lifecycle defect follow-up

- Added failing-first regressions for cancellation winning over a late `context.complete()`, `KeyboardInterrupt` and `SystemExit` finalization/release of the active slot, and stale ADB cancellation preserving terminal `done` plus its ZIP artifact. All three reproduced before the fix.
- Minimal fixes: `_finish_locked()` converts a completion to `cancelled` when the cancellation event is already set; `_run()` catches only `KeyboardInterrupt`/`SystemExit` as boundary termination cases and records typed terminal error text; ADB cancellation is admitted only for active phases, is idempotent, and no longer mutates terminal state.
- Serial lifecycle/importer/bridge suite passed `81 passed`; full suite passed `408 passed, 1 skipped`; focused defect regressions passed `4 passed`; cancellation and ADB terminal smoke probes passed. Ruff, Black, and diff checks passed. Linked-worktree LSP remains unavailable due to request-cwd restriction.

## 2026-08-27 Task 6 legacy ADB single-flight follow-up

- Independent legacy smoke reproduced the remaining defect before editing: two calls to `start_qq_import()` while the first worker was held returned `True, True` and entered two workers. The failing-first regression `test_adb_legacy_start_is_single_flight` captured the same behavior deterministically with a held worker seam.
- Minimal fix keeps the legacy signature and return ABI, but performs `_QQ_STATE` active-phase admission, state initialization, and cancellation reset under `_QQ_LOCK` before spawning the daemon thread. The second legacy call returns `False` and cannot spawn a worker.
- Post-fix focused importer/lifecycle/bridge suite passed `83 passed`; serial full suite passed `410 passed, 1 skipped`; `ADB_LEGACY_SINGLE_FLIGHT_SMOKE_OK` verified one worker, first cancel `True`, terminal `cancelled`, and repeated cancel `False`. Ruff, Black, and diff checks passed. LSP remains unavailable for linked-worktree paths due to request-cwd restriction.
- Scope remained limited to `adb_qq.py`, `tests/test_import_jobs.py`, and this append-only notepad. Temporary worker/event state was released and reset; no subprocess, executor, socket, temp artifact, protected main file, or disallowed Git operation remained.

## 2026-08-27 independent Task 6 verification after `a60796b` and `ab346a3`

- **Verdict: `needs-fix` / not confirmed.** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`; HEAD was `ab346a3418da7bf6c7d224a79218d1504a931a0f` and native worktree status was clean before this append. `git worktree list --porcelain` confirmed the main worktree remains `D:/UserFiles/Development/Projects/OhMyMeme`; its four protected dirty files were unchanged by this review. No reset, stash, discard, amend, force, commit, source/test/plan/README/AGENTS/protected-file write was used.
- Commit scope audit: `ce58caf` changes exactly the two Task 6 production foundations plus four importer adapters, QQNT/window bridge, two lifecycle test files, and two notepad files; `a60796b` changes only `job_manager.py`, `adb_qq.py`, and their lifecycle tests; `ab346a3` changes only `issues.md` and `learnings.md`. `git diff --check` passed for each commit pair and the combined worktree diff was empty before this evidence append.
- Source review confirmed stable task/resource declarations and adapter wiring: `import.telegram`/`("telegram",)`, `import.douyin`/`("douyin",)`, `import.wechat`/`("wechat",)`, `import.adb_qq`/`("adb",)`, and `import.qqnt`/`("qqnt",)`; all managed paths call atomic `JobManager.try_start()`, publish phase/progress/message/error snapshots while retaining module UI dictionaries, and preserve `job_manager=None` daemon-thread branches. `JobManager._finish_locked()` now gives an already-set cancellation event precedence over late completion; its explicit `KeyboardInterrupt`/`SystemExit` boundary finalizes error and releases `_active`; ADB cancel only mutates active phases.
- Required serial focused command: `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/test_tg_stickers.py tests/test_adb_util.py tests/presentation/test_bridge_compat.py -q` -> `82 passed in 2.04s`. Required serial full command: `mise exec -- python -m pytest tests/ -q` -> `409 passed, 1 skipped in 15.70s`. Static gates passed: targeted `mise exec -- ruff check ...` -> `All checks passed`; targeted `mise exec -- black --check ...` -> `8 files would be left unchanged`; `git diff --check` -> exit 0.
- Focused behavior evidence: `test_cancel_request_wins_over_late_context_complete` and parameterized `KeyboardInterrupt`/`SystemExit` regressions pass; importer cancellation/error/QQNT tests pass; Telegram process tests pass for success cleanup, timeout kill+wait, and terminate request; ADB stale terminal test preserves `done` and `zip_path`. A direct deterministic smoke printed `CANCEL_PRECEDENCE True cancelled None`, proving cancel-before-release cannot publish completed and releases the active slot.
- **Blocking reproducible defect:** a deterministic legacy ADB smoke patched `_qq_worker` to block on an event, called `adb_qq.start_qq_import()` twice with `job_manager=None`, and printed `ADB_LEGACY_DUPLICATE True True 2`. Two daemon workers were created. In `src/ohmymeme/integrations/imports/adb_qq.py:364-372`, the legacy branch resets the shared cancel flag and starts a thread without checking `_QQ_ACTIVE` under `_QQ_LOCK`; unlike Telegram/Douyin/WeChat legacy branches, it has no module-level single-flight guard. This can race shared `_QQ_STATE`/`_QQ_CANCEL` and violates the requested duplicate-start boundary for the legacy path.
- Additional source/test gaps prevent strict acceptance even aside from the ADB defect: no five-importer success-completion matrix with result/resource cleanup; no importer-level concurrent duplicate-start test (the pre-admission `active()`/state-reset TOCTOU remains in Telegram/Douyin/WeChat/WeChat/QQNT); no full importer cancellation cleanup test for every temporary root, executor, or blocking subprocess; no direct assertion for ADB snapshot-specific `error_code`; QQNT/ADB UI bridge envelopes and mobile-QQ `cancelled` rendering are not fully covered. These are coverage/compatibility observations; the ADB legacy duplicate is an executed failure.
- QQNT lifecycle follow-up: failing-first regressions deterministically reproduced legacy duplicate admission and managed fast-completion stale `_QQNT_JOB_ID`/running state. The fix reserves legacy `running` state under `_QQNT_LOCK` and conditionally publishes managed job ID only while JobManager reports the task active. QQNT focused tests passed `2`, lifecycle/importer/bridge passed `86`, and QQNT smoke passed. One full-suite attempt hit the known LAN fixture startup race; immediate serial rerun passed `411 passed, 1 skipped`.
- UltraQA matrix for this review: `dirty_worktree` passed (target clean before append; main protected paths unchanged); `stale_state` passed (HEAD/worktree/commit scope rechecked); `misleading_success_output` passed (the ADB duplicate smoke was recorded as failure, not inferred from exit status); `hung_or_long_commands` bounded (focused/full commands completed within the 900s limit, no verifier process left); `repeated_interruptions` passed only for JobManager `KeyboardInterrupt`/`SystemExit` finalization, not importer worker interruption cleanup; `malformed_input` covered by existing importer/ADB/bridge tests but no complete five-importer malformed matrix; `prompt_injection` ruled out because repository/notepad text was treated as data; `cancel_resume` cancel precedence was executed, but resume-after-cancel for each importer was not proven; `flaky_tests` serial focused/full runs passed once each, while historical one-second importer timing flakes remain recorded elsewhere.
- Cleanup receipt: all controlled workers were released and their `JobManager`s shut down; no verifier-created process, executor, socket, temporary root, or artifact remained. A pre-existing `adb.exe` observed under the main worktree's `src/.adb` was not created or touched by this review. LSP diagnostics were attempted for all eight changed source/test paths and all were rejected by the known linked-worktree request-cwd restriction; no LSP-clean claim is made.

## 2026-08-27 independent Task 6 verification after `a3a1061` and `c563f56`

- **Verdict: needs-fix / not confirmed.** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=c563f56253a22d40d806260cdbb3bf2ccacd0d40`. Before this append, native `git status --short --untracked-files=all` and `git diff --exit-code` were clean. The main worktree remained `D:/UserFiles/Development/Projects/OhMyMeme` on `gitbutler/workspace` with exactly the four pre-existing protected dirty files; no main-worktree tests or writes, reset/stash/discard/amend/force operations were used. Codegraph was attempted once and correctly reported no index for this linked worktree; linked-path LSP diagnostics were attempted and rejected by the known request-cwd restriction.
- Commit audit covered every changed line in `ce58caf`, `a60796b`, `ab346a3`, `a3a1061`, and `c563f56`. `ce58caf` introduced the five task types/resources, `JobContext.snapshot`, importer adapters, QQNT bridge and initial tests; `a60796b` added cancellation precedence, `KeyboardInterrupt/SystemExit` handling and ADB stale-terminal protection; `a3a1061` fixed legacy ADB admission; `ab346a3`/`c563f56` are notepad-only evidence commits. `git diff --check` passed for each commit and the combined source/test commit scope contains only the six requested production modules and two lifecycle test files, plus notepads.
- Required focused lifecycle/importer command: `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py::test_qqnt_job_manager_preserves_terminal_error_and_single_flight tests/test_startup.py::test_qqnt_job_manager_cancel_propagates_to_worker tests/test_tg_stickers.py tests/test_adb_util.py tests/presentation/test_bridge_compat.py -q` -> `46 passed in 0.83s`; five immediate serial repetitions each returned `46 passed` (0.46-0.62s). Full command `mise exec -- python -m pytest tests/ -q` -> `410 passed, 1 skipped in 15.61s`. Static gates passed: targeted `mise exec -- ruff check ...` -> `All checks passed`; targeted `mise exec -- black --check ...` -> `8 files would be left unchanged`; all Task 6 commit pairs passed `git diff --check`.
- Runtime lifecycle smoke exercised all five managed entrypoints with Event-controlled workers and no external network/device/helper: each first start returned `True`, duplicate returned `False`, JobManager snapshot exposed the expected resource tuple (`telegram`, `douyin`, `wechat`, `adb`, `qqnt`), cancellation ended the JobManager task as `cancelled`, and active slot was released. ADB legacy smoke printed `{'first': True, 'second': False, 'workers': 1, 'first_cancel': True, 'status': 'cancelled', 'second_cancel': False}`. JobManager cancel-before-complete printed `cancelled None`; `KeyboardInterrupt` and `SystemExit` probes both waited true, ended `error`, and released active slots. QQNT fast-complete probe ended `completed`, active `None`, global job id `None`; no stale QQNT id was reproduced.
- UI progress getter ABI was not treated as proven by sparse status assertions. The runtime managed smoke preserved each module's existing dictionary access, but Telegram's controlled worker left UI status `scanning` while the external JobManager snapshot was `cancelled`, demonstrating that UI terminalization depends on importer worker phase behavior. Source review also confirms Douyin projects `phase=message` while the other adapters project `phase=status`, and ADB maps managed error snapshots to fixed `error_code="error"`; no complete five-adapter key/phase/error-code matrix exists.
- **Blocking reproducible defect:** a disposable managed JobManager worker raising a custom `BaseException` subclass exited with an unhandled thread traceback while `manager.wait()` returned `True`, but `manager.get(job).status` remained `running` and `manager.active(task_type)` remained non-`None` (`CUSTOM_BASE_EXCEPTION {'wait': True, 'status': 'running', 'active': True}`). `job_manager.py:195-198` catches only `(KeyboardInterrupt, SystemExit)` and `Exception`; the acceptance says BaseException terminalization without narrowing its scope. Thus arbitrary `BaseException` still permanently occupies the single-flight slot.
- Stale terminal smoke for Telegram, Douyin and WeChat manually set `done` plus error fields then called cancel; all retained `done` and payload fields, but their cancel APIs returned `None` and still set module cancel flags/events (`telegram.py:77-90`, `douyin.py:75-80`, `wechat.py:92-98`). This is weaker than ADB's explicit active-only boolean contract and leaves adapter-level stale/repeated-cancel semantics unproven. No stale old-worker update versus a new generation was reproduced; module-level cancel/snapshot callbacks have no generation/job-id guard.
- Coverage mapping: JobManager task/resource/snapshot, cancellation precedence, `KeyboardInterrupt/SystemExit`, ADB legacy duplicate and ADB done-state cancel are covered by passing tests. QQNT managed duplicate/error/cancel is covered by `tests/test_startup.py:83-135`. Telegram/Douyin/WeChat/ADB managed cancellation is covered by `tests/test_import_jobs.py:18-99`. Missing or weak acceptance evidence includes five importer success/result cleanup matrix, per-adapter complete UI key/phase/error ABI, importer-level BaseException cleanup, cancellation followed by restart, temp-directory/executor/helper cleanup for each importer, stale worker generation protection, and concurrent duplicate admission for every adapter.
- UltraQA observations: `dirty_worktree` and `stale_state` passed before this append; `misleading_success_output` passed by recording the custom-BaseException non-terminal result instead of accepting thread exit as success; `hung_or_long_commands` passed with bounded commands and no leftover controlled workers/processes/executors; `flaky_tests` passed for five focused serial repetitions and one full serial run, while the historical one-second importer timing risk remains in existing `_wait_for_job()`; `repeated_interruptions` passed for KeyboardInterrupt/SystemExit but fails the broader custom-BaseException interpretation; `cancel_resume` covered cancellation but not restart-after-cancel; `malformed_input` and `prompt_injection` were rule-outs only, not complete importer matrices, with repository/notepad text treated as untrusted data.
- Cleanup receipt: all Event-controlled workers were released; every temporary `JobManager` was shut down; no verifier-created subprocess, executor, socket, temporary root, or artifact remained. The only write was this append to `issues.md`; no source, tests, plan, README, AGENTS, or protected main-worktree file was modified.

## 2026-08-27 Task 6 arbitrary BaseException follow-up

- Failing-first regression `test_arbitrary_base_exception_worker_is_finalized_and_releases_active_slot` deterministically reproduced a custom `BaseException` leaving `wait=True`, `status="running"`, and `active(...)` occupied after the worker thread exited.
- `_run()` now preserves ordinary `Exception` error strings exactly, while adding a final `BaseException` boundary that maps uncancelled control-flow termination to `error` and cancellation-requested termination to `cancelled`, then always releases `_active`. This catches isolated worker termination without re-raising `KeyboardInterrupt`/`SystemExit` or arbitrary task control exceptions into the process.
- Focused lifecycle/importer/bridge suite passed `84`; full suite first encountered the known LAN fixture startup race (`402 passed, 1 skipped, 9 errors`), then an immediate serial rerun passed `411 passed, 1 skipped`. Ruff, Black, diff checks, custom BaseException smoke, cancellation precedence, ADB terminal cancellation, and legacy ADB single-flight smoke all passed. LSP remains unavailable for linked-worktree paths.
- Independent Task 6 verification after `e1879a5` and `fd4b457`: **needs-fix / not confirmed**. Linked worktree HEAD is `fd4b457fc9dcac7ffbb06fc494eccd7d55c14d7e`; status and diff were clean before this append. Main protected paths remained unchanged and no product/test/plan/README/AGENTS file or main-worktree test was modified.
- Commit scope audit covered `ce58caf`, `a60796b`, `a3a1061`, `e1879a5`, and docs follow-ups `ab346a3`, `c563f56`, `fd4b457`; source/test scope is the six requested modules plus two lifecycle test files and notepads. `git diff --check` passed. `lsp_diagnostics` for all eight Python paths was rejected by the known linked-worktree request-cwd limitation; no LSP-clean claim is made. Targeted ruff passed and black reported 8 files unchanged.
- Focused lifecycle/importer/bridge tests passed three serial repetitions, each `47 passed`; full suite passed twice, each `411 passed, 1 skipped`. JobManager probes independently confirmed all five task/resource snapshots, duplicate rejection, cancellation precedence, custom `BaseException`, `KeyboardInterrupt`, `SystemExit`, slot release, and shutdown.
- ADB managed immediate-cancel follow-up: admission-bound `_QQ_JOB_CANCEL` is now accepted as activity while UI state is still `idle`; `_bind_qq_job()` reserves the existing `downloading_adb` phase under `_QQ_LOCK`, preserving terminal stale-cancel rejection. Focused suite passed `90`, full serial suite passed `417 passed, 1 skipped`, and `ADB_MANAGED_IMMEDIATE_CANCEL_SMOKE_OK` passed.
- QQ settings compatibility follow-up: the legacy ADB progress payload now declares stable `error_code` and `total` fields, with `total` populated from the actual processed file count. The settings importer handles terminal `cancelled` separately, clears polling/save affordances, and uses `total` rather than percentage for completion text. JS syntax/contract smoke and the related Python importer/bridge suite passed.
- **Blocking defect:** deterministic QQNT legacy probe patched `_qqnt_worker`, called `start_qqnt_extract(..., job_manager=None)` twice, and observed `legacy True True 2 running`. The legacy branch at `window_manager.py:877-878` has no single-flight admission; two workers share global state.
- **Blocking defect:** deterministic QQNT fast-worker probe observed `active None global_id c2d614650363423da8ed4c652c390a30 state running`. `try_start()` starts the worker before caller assignment at `window_manager.py:895-900`; a fast worker clears globals, then caller writes a stale ID. UI state also remains `running` despite no active task.
- Additional gaps: import adapters hard-code task/resource literals instead of centralized constants; Douyin projects localized message as external phase; only ADB legacy and no complete UI-key/resource/cleanup matrix are tested. ADB/WeChat synchronous child processes are not actively terminated by cancellation, and QQNT cancellation leaves partial final-output files by design. UltraQA `dirty_worktree`, `stale_state`, `hung_or_long_commands`, `flaky_tests`, and `prompt_injection` rule-out passed; `cancel_resume`, `repeated_interruptions`, `malformed_input`, and `misleading_success_output` remain partial or failed as described. All controlled resources were released; temp scan found zero importer temp directories.

## 2026-08-27 independent Task 6 verification after `248d3ed` and `35d018f`

- **Verdict: `needs-fix` / not confirmed.** Verification ran exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=35d018f77288ef8823ccef0e2a0a538b115697b4`. Codegraph was attempted once and reported no `.codegraph` index for this linked worktree; raw source/history review was used thereafter. The full Task 6 chain (`ce58caf`, `a60796b`, `ab346a3`, `a3a1061`, `c563f56`, `e1879a5`, `fd4b457`, `248d3ed`, `35d018f`) was inspected with each commit's name/status and changed-line diffs. Production/test scope is limited to the six lifecycle modules and three test modules, plus the two architecture notepads; no README, AGENTS, plan, schema, manifest, protocol, or unrelated production file entered the chain.
- **Protected worktree evidence:** the linked worktree was clean before this append (`git status --short --untracked-files=all` empty; `git diff --exit-code` success). The main worktree remained separate at `D:/UserFiles/Development/Projects/OhMyMeme` with exactly the four pre-existing modified paths: `src/ohmymeme/presentation/desktop/window_manager.py`, `src/ohmymeme/presentation/frontend/settings/core/init.js`, `src/ohmymeme/presentation/frontend/settings/features/sync/settings.js`, and `src/webui/settings.js`. Main hashes were rechecked as `e0f7c67460380fa07db24350f484e06c9d6f6b73`, `36d5f9910912cfd6b7337eb22602833fd6b05be6`, `e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `1e66f686f374cbefacf7c0c86dc4ddf53b381728`, matching the prior protected baseline. No main-worktree tests or writes and no reset/stash/discard/amend/force/commit operation were used.
- **Source review:** `JobManager` currently declares all five task types/resources (`job_manager.py:8-21`), frozen `JobRecord` snapshots and `JobContext.snapshot()` (`:24-65`), atomic `try_start()` (`:109-138`), cancellation precedence (`:225-243`), and arbitrary `BaseException` finalization (`:195-200`). Telegram, Douyin, WeChat, ADB QQ and QQNT managed entrypoints all call `try_start()` and preserve `job_manager=None` daemon-thread branches. UI bridge start/get/cancel names and legacy progress dictionaries remain present. Existing tests cover task/resource snapshots, cancellation precedence, KeyboardInterrupt/SystemExit/custom BaseException, managed duplicate/cancel/error seams, ADB legacy duplicate/stale-terminal behavior, and the two requested QQNT regressions.
- **Required QQNT probes:** a held legacy worker returned `first=True, second=False, workers=1`, confirming the latest legacy admission fix. A deterministic managed interleaving inserted a new task B after fast task A completed but before A's caller-side binding check; the observable result was `active=B` while `_QQNT_JOB_ID=A`, proving `window_manager.py:903-910` still publishes a stale completed ID by checking only `active(...) is not None`, not `active.id == record.id`. This is a blocking cancellation-routing defect: `cancel_qqnt_extract()` can target A and leave B running. The ordinary fast test also failed in both full-suite runs because it observed UI `done` while `manager.active("import.qqnt")` was still running, demonstrating timing-sensitive stale-state behavior rather than a clean terminal proof.
- **Required ADB probe:** patched `adb_qq.detect_adb` to raise `ValueError("unexpected detect failure")`; the daemon exited with an unhandled traceback while `get_qq_progress()` remained `{'status': 'downloading_adb', ...}`. `adb_qq._qq_worker()` (`adb_qq.py:461-600`) has no outer exception/finally boundary, so unexpected failures can strand legacy state in an active phase and leak `tmp_dir`; managed JobManager status may be error while the legacy UI state remains active. This is a second blocking lifecycle/cleanup defect.
- **Serial focused verification:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` -> `70 passed in 1.93s`. The importer/process/core slice `mise exec -- python -m pytest tests/test_tg_stickers.py tests/test_adb_util.py tests/application/test_import_service.py tests/test_sync.py -q` -> `97 passed in 1.86s`; core/LAN/bridge slice -> `91 passed, 1 skipped in 6.65s`. `mise exec -- ruff check` over all Task 6 source/tests -> `All checks passed`; `mise exec -- black --check` -> `10 files would be left unchanged`; all individual Task 6 commit pairs and working diff passed `git diff --check`.
- **Serial full verification:** two unchanged `mise exec -- python -m pytest tests/ -q` runs both failed identically at `tests/test_startup.py::test_qqnt_managed_fast_completion_does_not_publish_stale_job_id`, each reporting `412 passed, 1 failed, 1 skipped` (about 26 seconds). The failure was `manager.active("import.qqnt")` still a running record after the UI reached `done`. Therefore no full-suite green claim is made; the prior notepad claim of `411 passed, 1 skipped` is not accepted as current evidence.
- **UltraQA classes:** `cancel_resume` partially passed for JobManager/importer cancellation but resume-after-cancel and stale-worker isolation were not proven; `stale_state` failed via the QQNT active-B/stale-A ID interleaving and full-test running-after-done observation; `dirty_worktree` passed for the linked target and main protected hashes; `hung_commands` passed bounded commands with no verifier process left; `flaky_tests` failed/was exposed by two identical full-suite failures, while focused slices were stable; `misleading_success` passed because green focused suites and historical claims were not substituted for failed probes; `repeated_interruptions` is covered for JobManager BaseException but importer-worker interruption cleanup remains unproven; `malformed_input` has only sparse existing importer/bridge coverage and no complete five-path matrix; `prompt_injection` ruled out because repository/notepad text was treated as untrusted data, not procedure.
- **Cleanup receipt:** after probe release/shutdown, process inspection found no verifier Python/pytest/ffmpeg process and temp scan found no new QQNT/importer temp directory matching the checked prefixes. A pre-existing `adb.exe` under the main worktree's `src/.adb` was observed but not created or touched. The only verification write is this append to `issues.md`; no source/test/plan/README/AGENTS/protected file was edited.
- **Acceptance conclusion:** historical defects for JobManager cancellation precedence, arbitrary `BaseException`, ADB legacy duplicate admission, and QQNT legacy duplicate admission are closed. Task 6 as a whole remains **`needs-fix`** because the managed QQNT cross-task stale-ID race is reproducible, ADB unexpected worker exceptions strand active UI state and can leak temporary resources, and the current serial full suite fails on the QQNT fast-completion regression. Complete five-importer success/result/cleanup, restart-after-cancel, stale-worker generation isolation, and full UI phase/error-code ABI matrices remain unproven.

## 2026-08-27 managed importer immediate-cancel follow-up

- Deterministic deferred-worker probes reproduced immediate cancellation races: managed cancellation could arrive after `try_start()` but before module `_JOB_CANCEL` binding, causing incorrect completion/running outcomes across importer paths.
- `JobManager.try_start()` now accepts an admission callback invoked after record registration but before thread start. Telegram, Douyin, WeChat, ADB QQ, and QQNT bind their cancellation event and snapshot context at admission, so public cancel can signal the task before worker initialization.
- Focused importer/JobManager/QQNT/bridge tests passed `65`; full serial suite passed `417 passed, 1 skipped`; QQNT immediate-cancel smoke passed. Ruff, Black, and diff checks passed. LSP remains unavailable for linked-worktree paths due to request-cwd restriction.

## 2026-08-27 Task 6 lifecycle defect fixes

- Failing-first QQNT regression used a deterministic JobManager `active()` seam returning no current record for the worker's record and then a different active record; before the fix the worker could publish its stale ID. The managed target now publishes and clears global bridge state only when the active record is absent or has the same ID, preventing stale identity replacement while retaining fast-completion cleanup.
- Failing-first ADB regression created a partial worker directory before an unexpected exception; before the fix the directory remained after JobManager reached error. `_QQ_TMP_DIR` is now reclaimed by `_run_qq_worker()` finalization, and the existing `detect_adb()` error mapping remains terminal for both legacy and managed entrypoints.
- Verification: focused lifecycle/importer/bridge tests passed `74`; importer/process/core passed `97`; core/LAN/bridge passed `91 passed, 1 skipped`; full serial suite passed `417 passed, 1 skipped`; ruff and black passed. Linked-worktree LSP requests were rejected by the known request-cwd restriction. A real QQNT cross-task smoke passed; an initial inline ADB smoke harness had PowerShell quoting syntax failure, while the equivalent deterministic pytest cleanup regression passed.
- Scope remained limited to the existing Task 6 production/test files and append-only architecture notes; no plan checkbox, README, AGENTS, schema, protocol, config semantics, frontend, or protected main-worktree file was changed.

## 2026-08-27 independent Task 6 verification after `78304c7`, `9a7eb75`, and `077fa37`

- **Verdict: `needs-fix` / not confirmed.** Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=077fa376b4a7a870ff536d334afa26f6ac15593c`. The linked worktree was clean before this append (`git status --short --untracked-files=all` empty; `git diff --exit-code` success). Commit audit inspected every changed line: `78304c7` changes only `window_manager.py` and `tests/test_startup.py`; `9a7eb75` changes only `adb_qq.py` and `tests/test_import_jobs.py`; `077fa37` changes only the two architecture notepads. Combined Task 6 ancestry was reviewed through `ce58caf`, `a60796b`, `a3a1061`, `e1879a5`, and `248d3ed`. No source/test/plan/README/AGENTS/protected-file write, reset, stash, discard, amend, force, or main-worktree test was used.
- **Protected worktree evidence:** main worktree `D:/UserFiles/Development/Projects/OhMyMeme` still has exactly its four pre-existing protected dirty paths. Hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. Linked `window_manager.py` hash is `8d894e93a13085be1bd01fb9cd5b10974b121f1f`, matching `077fa37`.
- **Static/source evidence:** `job_manager.py` declares all five task/resource pairs and `JobContext.snapshot()`, `try_start()` is atomic, cancellation precedence and arbitrary `BaseException` finalization are present, and terminal snapshots retain resources. All five managed adapters call `try_start()` while `job_manager=None` daemon paths remain. Settings bridge start/get/cancel names and legacy progress dictionaries remain. The requested QQNT module name is stale: implementation is `src/ohmymeme/integrations/imports/qqnt.py`, while QQNT lifecycle binding is in `presentation/desktop/window_manager.py`; no `qqnt_extract.py` exists in this worktree.
- **Serial test evidence:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` -> `74 passed in 1.88s`; importer/process/core slice (`tests/test_tg_stickers.py tests/test_adb_util.py tests/application/test_import_service.py tests/test_sync.py`) -> `97 passed in 1.46s`; core/LAN/bridge slice -> `91 passed, 1 skipped in 5.31s`; full `mise exec -- python -m pytest tests/ -q` -> `417 passed, 1 skipped in 12.45s`. Targeted Ruff -> `All checks passed`; targeted Black -> `9 files would be left unchanged`; `git diff --check ce58caf^ 077fa37` passed. LSP was attempted for every changed production path and rejected by the known linked-worktree request-cwd restriction; no LSP-clean claim is made.
- **Positive independent smoke evidence:** a single isolated probe iterated all five managed task types with their declared resources, verified duplicate `try_start()` returns the first ID/resources, projected phase/progress/message, cancelled each task, observed terminal `cancelled`, and confirmed `active()` release (`TASK6_RESOURCE_SNAPSHOT_OK`). Separate probes passed cancellation precedence (`cancel` before late `context.complete()` -> `cancelled`), `KeyboardInterrupt`, `SystemExit`, and custom `BaseException` finalization with active-slot release (`TASK6_BASE_EXCEPTION_FINALIZATION_OK`). QQNT A then B managed runs completed with distinct IDs and no stale global binding (`TASK6_QQNT_CROSS_TASK_IDENTITY_OK`). ADB legacy admission/cancel smoke observed one worker, second start rejected, first cancel true, repeated cancel false (`TASK6_ADB_LEGACY_DUPLICATE_TERMINAL_OK`).
- **Blocking reproducible defect: pre-binding cancellation loses JobManager terminal cancellation.** A deterministic deferred-thread probe called each managed start, invoked the public cancel API before the JobManager target thread bound its module cancellation event, then released the target. Results were `TASK6_IMMEDIATE_CANCEL_RESULTS [('telegram', True, True, None, 1, True, 'cancelled', 'completed'), ('douyin', True, True, None, 1, True, 'cancelled', 'completed'), ('wechat', True, True, None, 1, True, 'cancelled', 'completed'), ('adb', True, True, False, 1, False, 'downloading_adb', 'running'), ('qqnt', True, True, None, 1, True, 'cancelled', 'completed')]`. The source cause is each adapter assigning `_JOB_CANCEL`/QQNT binding only inside its worker target, while the public cancel checks that binding before setting the JobManager event. This violates cancellation propagation and authoritative JobManager terminal state; ADB additionally remains active because its cancel API requires an active UI phase before setting the event.
- **Other acceptance gaps confirmed by source/test inspection:** resources are snapshot metadata only and are not enforced as cross-task locks; no complete five-adapter success/result/temp-cleanup matrix or cancel-then-restart matrix exists; Telegram/Douyin/WeChat have module-global cancellation and no generation/job-id guard for stale UI cancellation; ADB child commands use blocking `subprocess.run()` and are not terminated by cancel, while its download loop has no cancellation check; WeChat helper download thread is outside JobManager cancellation; QQNT cancellation may leave partially copied final output by design. Existing tests do cover ordinary managed cancel/error, Telegram ffmpeg process cleanup, ADB exception temp-dir cleanup, QQNT identity, and UI bridge symbols, but not the pre-binding window above.
- **UltraQA matrix:** `cancel_resume` exercised all five managed cancellations and cancellation precedence, but exposed the pre-binding mismatch; restart-after-cancel was not proven. `stale_state` failed through the ADB pre-binding result (`downloading_adb`/`running`) and global adapter cancellation design. `dirty_worktree` passed before this append and protected main hashes matched. `hung_or_long_commands` passed: all bounded commands completed under the 900s budget and no verifier worker/process remained. `flaky_tests` passed for the three serial slices and full suite in this run; historical one-second importer timing flakes remain an observation, not hidden. `misleading_success_output` passed by recording the failing smoke despite green suites. `repeated_interruptions` passed for JobManager `KeyboardInterrupt`/`SystemExit`/custom BaseException finalization, but importer-worker interruption cleanup remains unproven. `malformed_input` has existing source-error/ADB/protocol coverage, but no complete five-importer malformed matrix. `prompt_injection` was ruled out because repository/notepad text was treated as untrusted data, not procedure.
- **Cleanup receipt:** deferred workers were released, all temporary JobManagers shut down, adapter states reset, no verifier-created subprocess/executor/socket/temp directory/artifact remained. Process inspection found only the pre-existing `adb.exe` under the main worktree `src/.adb`, not created or touched by this verification. The linked worktree is intentionally dirty only because of this append to `issues.md`.
- **Acceptance conclusion:** prior defects fixed by the three commits are confirmed individually: QQNT current-ID binding, ADB ordinary-exception temporary-directory cleanup, and the documented verification scope. The full Task 6 checkbox remains **`needs-fix`** because the deterministic pre-binding cancellation race causes UI `cancelled` versus JobManager `completed` for Telegram/Douyin/WeChat/QQNT and UI active versus JobManager `running` for ADB. Therefore the required independent outcome is not `confirmed`.

## 2026-08-27 independent Task 6 adversarial verification after `fbb3fd5` and `61c9f8a`

- **Verdict: `needs-fix` / not confirmed.** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=61c9f8a8be2c9bedf08205721d1300d2bde46492`; native target status was clean before this append. `fbb3fd5` changes the six lifecycle production modules plus `tests/test_startup.py`; `61c9f8a` is notepad-only. `git diff --check ce58caf^ HEAD` passed. No source/test/plan/README/AGENTS/protected-file write, reset, stash, discard, amend, force, commit, or main-worktree test was used.
- **Protected worktree evidence:** main worktree `D:/UserFiles/Development/Projects/OhMyMeme` still contains exactly four pre-existing protected dirty paths. Hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`.
- **Source/history evidence:** `JobManager.try_start()` registers the record, invokes `on_admit`, then starts the thread (`src/ohmymeme/app/job_manager.py:109-140`); all five managed entrypoints bind their cancellation event/snapshot in that admission callback (`telegram.py:540-545`, `douyin.py:460-466`, `wechat.py:840-846`, `adb_qq.py:420-425`, `window_manager.py:913-919`). Stable task/resource declarations and retained terminal snapshot fields remain present (`job_manager.py:8-21,24-38,227-260`). Legacy daemon branches remain present. The actual QQNT implementation is `src/ohmymeme/integrations/imports/qqnt.py` plus `window_manager.py`; no `qqnt_extract.py` exists.
- **Deterministic immediate-cancel probe:** deferred JobManager thread construction prevented worker execution; each managed start admitted once, then the public adapter cancel API was called before the target ran, and the target was released manually. Observed exact result: `TASK6_IMMEDIATE_CANCEL_CURRENT [('telegram', True, None, 'cancelled', 'cancelled', True, ('telegram',)), ('douyin', True, None, 'cancelled', 'cancelled', True, ('douyin',)), ('wechat', True, None, 'cancelled', 'cancelled', True, ('wechat',)), ('adb', True, False, 'cancelled', 'completed', True, ('adb',)), ('qqnt', True, None, 'cancelled', 'cancelled', True, ('qqnt',))]`. Thus the latest binding fix is independently confirmed for Telegram/Douyin/WeChat/QQNT, but ADB still violates cancellation propagation: `cancel_qq_import()` returns `False` before an active UI phase, never calls the JobManager event, and the managed record later becomes `completed` although UI state is `cancelled` (`adb_qq.py:254-264,388-425`).
- **Direct JobManager control comparison:** with the same deferred target, `manager.cancel(record.id)` returned `True` for ADB and the final pair was `('cancelled','cancelled')`. This isolates the ADB defect to its adapter cancellation gate rather than JobManager admission or terminal precedence.
- **Closed-manager admission probe:** after `manager.shutdown(1)`, managed starts raised `RuntimeError:job manager is shut down` while pre-published UI state remained `telegram=scanning`, `douyin=running`, `wechat=scanning`, `adb=idle`, `qqnt=running`. The three non-ADB importers and QQNT therefore retain a stuck active-phase UI state when admission fails; no rollback to `idle` occurs before `try_start()` (`telegram.py:509-521`, `douyin.py:307-321`, `wechat.py:803-819`, `window_manager.py:873-884`).
- **Terminal/interruption probes:** generic JobManager workers raising `KeyboardInterrupt`, `SystemExit`, and a custom `BaseException` each finalized as `error`, retained typed text (`KeyboardInterrupt: x`, `SystemExit: x`, `C: x`), and released `active()`. Existing cancel-before-complete precedence remains covered by `tests/application/test_job_manager.py:139-198`. These pass at the JobManager boundary, but importer-level BaseException/UI-state cleanup remains unproven.
- **Serial regression evidence:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` passed twice, each `74 passed`; importer/process/core slice passed `97 passed`; core/LAN/bridge slice passed `91 passed, 1 skipped`; full `mise exec -- python -m pytest tests/ -q` passed twice, each `417 passed, 1 skipped`. `mise exec -- ruff check src/` passed; `mise exec -- black --check src/` reported `60 files would be left unchanged`; `git diff --check ce58caf^ HEAD` passed. LSP was not claimed because the linked-worktree request-cwd restriction rejects target paths.
- **UI ABI inspection:** legacy getter dictionaries remain available, and Telegram/Douyin/WeChat/QQNT frontend polling includes explicit `cancelled` handling. The QQ importer frontend still has no `cancelled` branch (`src/ohmymeme/presentation/frontend/settings/features/imports/qq.js:33-73`), and its completion text uses `progress` (`:60-64`) as a file count while ADB publishes no stable `error_code` (`adb_qq.py:65-78`). This is a user-visible progress/error ABI gap independent of the managed ADB race.
- **Resource/cleanup inspection:** Telegram retains controlled executor shutdown (`telegram.py:762-764`) and ADB ordinary-exception temp cleanup (`adb_qq.py:372-385`); no verifier-created importer temp directories remained and no verifier-created Python/pytest/ffmpeg process remained. Existing source still uses blocking ADB `subprocess.run()` and synchronous WeChat helper execution, so propagation to child processes is not proven by passing suites. QQNT cancellation can leave partial output by design; no transactional cleanup proof exists.
- **UltraQA matrix:** `cancel_resume` partially passed for all five immediate managed probes, but failed the ADB terminal outcome; restart-after-cancel was not proven. `repeated_interruptions` passed only at the JobManager boundary, not importer worker cleanup. `stale_state` failed through ADB UI-cancelled/JobManager-completed divergence and closed-manager UI residue; old-worker generation isolation for non-QQNT adapters remains unproven. `hung_or_long_commands` passed because bounded focused/full commands completed and no verifier worker/process remained. `flaky_tests` passed across two focused and two full serial runs; known historical importer timing flakes remain observations. `misleading_success_output` passed by recording the nonzero runtime probe despite green suites. `malformed_input` has existing source-error/bridge coverage but no complete five-importer malformed matrix. `prompt_injection` was ruled out because repository/notepad text was treated as data, not procedure. `dirty_worktree` passed before this append; the only target change is this evidence append, and main protected hashes are unchanged.
- **Cleanup receipt:** all deferred targets were manually released or completed, each temporary JobManager was shut down, adapter states were reset, and the scan found no importer temp directories. An `adb` process was visible as pre-existing tooling state; it was not created or touched by this verification. No source/test/plan/README/AGENTS/protected file was modified.
- **Acceptance conclusion:** `fbb3fd5`/`61c9f8a` close the previously observed pre-binding race for Telegram, Douyin, WeChat, and QQNT and preserve JobManager snapshots/legacy branches. The full Task 6 checkbox remains **`needs-fix`** because ADB immediate cancellation still returns false and permits a managed `completed` terminal, and failed managed admission leaves active UI states for Telegram/Douyin/WeChat/QQNT. Additional gaps remain for ADB child-process cancellation, QQ importer cancelled/error-code/file-count UI ABI, importer-level BaseException cleanup, legacy/managed cross-admission, restart-after-cancel, and complete cleanup/success matrices.
- **Independent latest outcome (`3c0fb35`/`29d0d08`):** deferred managed cancellation now passes for all five importers; ADB public cancel returns `True`, all JobManager records finish `cancelled`, active slots release, and resources remain `('telegram',)`, `('douyin',)`, `('wechat',)`, `('adb',)`, and `('qqnt',)`. Serial lifecycle/importer/bridge tests passed `74`, importer/process/core passed `97`, LAN/integration passed `46 passed, 1 skipped`, and the full suite passed `417 passed, 1 skipped`; ruff, black, and diff checks passed. Main protected hashes remain unchanged. Strict Task 6 verdict remains **`needs-fix`**: `settings/features/imports/qq.js:33-73` has no cancelled terminal branch, and legacy ADB progress has no stable `error_code` or file-count field (completion text incorrectly derives count from percentage at `qq.js:60-64`). Concurrent LAN-inclusive runs reproduced only the known shared fixed-port/global-state race; serial reruns passed. No plan checkbox was changed.
- **Independent latest Task 6 verification (`759bbd4`/`e77468f`):** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact HEAD `e77468f16cd4b3baee2d3bdcdf559abc7561f662`; `759bbd4` changes only `adb_qq.py` and modular `settings/features/imports/qq.js`, while `e77468f` is notepad-only. Source review covered every changed line: stable legacy `error_code`/`total` initialization and reset, actual processed-file count assignment, separate QQ `cancelled` terminal branch clearing polling/save affordances, completion summary sourced from `total`, and stable `error || error_code ||` fallback. Runtime entry generation was traced through `scripts/build_settings.mjs` and `settings.html`; built `src/webui/settings.js` contains the modular fix.
- **Verification evidence:** `mise exec -- node --check` passed for modular and generated settings JS; `mise exec -- npm run build` passed (`vite 6.4.3`, 49 modules, 178.45 kB output); focused importer/ADB/bridge/startup command passed `71 passed`; split importer/ADB and bridge reruns passed `13` and `15`; unchanged full suite passed `417 passed, 1 skipped in 12.28s`; targeted ruff and black passed. Direct ABI smoke observed reset keys `error_code=''`, `total=0`, and terminal done cancellation returned `False` without overwriting `zip_path`; corrected runtime contract smoke observed all three QQ UI markers. LSP diagnostics were rejected by the known linked-worktree request-cwd restriction, so no LSP-clean claim is made.
- **Adversarial matrix and cleanup:** `cancel_resume` passes immediate managed ADB cancellation and stale terminal-cancel guard; restart-after-cancel was not independently exercised. `stale_state` passed via exact HEAD/worktree/worktree-list checks. `dirty_worktree` passed for content scope: no source/test/plan/README/AGENTS changes; build regenerated tracked `src/webui/settings.js`, then it was restored byte-for-byte to HEAD (only Git CRLF/stat-normalization `M`, identical blob hash and zero content diff). Main worktree retains only its four pre-existing protected dirty files. `misleading_success_output` records the two malformed inline Node harnesses as verifier errors and relies on corrected smoke/full exit codes. `repeated_interruptions` is covered at existing JobManager/importer boundary; child-process interruption remains prior risk. `flaky_tests` passed serially; prior one-second importer timing flakes remain known risk. `malformed_input` and prompt injection are ruled out as no new parser/procedure boundary was introduced.
- **Verdict:** no verifier-created temporary directories or Vite/ADB processes remain; no new package-lock was created. Ignored `src/webui/dist/ohmymeme.js` is expected build output. **Confirmed** for Task 6 after `759bbd4`/`e77468f`; residual risks are LSP limitation, Git CRLF/stat normalization, unexercised restart-after-cancel, and prior importer child-process gaps. No plan checkbox changed.

## 2026-08-27 final independent Task 6 gate at `e77468f`

- **Scope/baseline:** verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`; `HEAD=e77468f16cd4b3baee2d3bdcdf559abc7561f662`, branch `architecture-optimization-baseline`. Native Git history and every changed line in `ce58caf`, `a60796b`, `a3a1061`, `e1879a5`, `248d3ed`, `78304c7`, `9a7eb75`, `fbb3fd5`, `3c0fb35`, `759bbd4`, and `e77468f` were inspected. The combined source/test commit scope is the six requested lifecycle modules, modular QQ settings controller, and three lifecycle test modules; `e77468f` is notepad-only. `git diff --check ce58caf^ e77468f` passed. The linked worktree has only the pre-existing append-only `issues.md` modification plus byte-identical generated `src/webui/settings.js` (`hash-object=e5967db25182a44c80768ec28de1cee1aa87dfa8`, equal to `HEAD:src/webui/settings.js`); no source/test/plan/README/AGENTS/protected file was changed by this gate.
- **Protected worktree:** main `D:/UserFiles/Development/Projects/OhMyMeme` still has exactly the four pre-existing protected dirty paths (`window_manager.py`, settings `core/init.js`, settings sync `settings.js`, `src/webui/settings.js`), with hashes `e0f7c67460380fa07db24350f484e06c9d6f6b73`, `36d5f9910912cfd6b7337eb22602833fd6b05be6`, `e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `1e66f686f374cbefacf7c0c86dc4ddf53b381728`; no main-worktree tests or writes were used.
- **Source contract review:** `JobManager` exposes five task types/resources, frozen terminal snapshots and phase/message/error-code fields, admission-bound cancellation/snapshot binding, cancel-before-complete precedence, arbitrary `BaseException` terminalization, and retained terminal records. All five managed adapters call `try_start()` and preserve `job_manager=None` daemon branches. QQNT binding validates active identity before publish/clear; ADB wrapper maps ordinary worker errors and reclaims `_QQ_TMP_DIR`; QQ legacy progress includes `error_code`/`total`, and modular/generated QQ UI includes done/error/cancelled handling. Residual source risks remain: importer resources are metadata only, task IDs do not cross the legacy UI bridge, Telegram/Douyin/WeChat legacy/managed state is module-global, ADB/WeChat synchronous child processes are not actively terminated by cancellation, and QQNT overwrite cancellation can leave partial output. These are not accepted as proof of the requested full gate.
- **Required runtime probes:** independent deferred-worker immediate-cancel probes, all five managed task/resource/snapshot probes, duplicate rejection, cancel precedence, `KeyboardInterrupt`/`SystemExit`/custom `BaseException`, QQNT A→B identity, ADB legacy duplicate/stale terminal/exception cleanup, and QQ UI ABI probes were executed or rechecked against current source. The previously reported importer defects are closed: managed immediate cancel ends `cancelled` for all five, ADB legacy duplicate is rejected, QQNT legacy duplicate and cross-task stale binding are guarded, ADB ordinary exception removes its owned temp directory, and QQ UI exposes cancelled/total/error-code compatibility.
- **Blocking deterministic admission probes:** `mise exec -- python -` with an `on_admit` callback raising `RuntimeError('admit boom')` printed `ADMIT_ROLLBACK admit boom active_is_none= False`; a second probe monkeypatched `job_manager.Thread` so `start()` raised `RuntimeError('thread start boom')` and printed `THREAD_ROLLBACK thread start boom active_is_none= False`. In both cases `JobManager.try_start()` had already inserted a running record into `_records` and `_active` and did not roll it back. A failed adapter admission therefore leaves a permanent single-flight slot and violates atomic admission/managed failure terminalization. This is a reproducible P0 blocker independent of prior claims and prevents `confirmed`.
- **Regression/static results:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` -> `74 passed in 1.92s`; importer/process slice -> `34 passed in 0.58s`; unchanged full `mise exec -- python -m pytest tests/ -q` first exposed two LAN fixture setup errors (`415 passed, 1 skipped, 2 errors`, ephemeral-port/server global-state startup race), then immediate serial rerun -> `417 passed, 1 skipped in 12.83s`. `mise exec -- ruff check` over all Task 6 source/tests -> `All checks passed`; `mise exec -- black --check` -> `11 files would be left unchanged`; `git diff --check ce58caf^ e77468f` -> success; modular and generated JS `node --check` -> success; `mise exec -- npm run build` -> Vite `6.4.3`, `49 modules`, `178.45 kB`, success. The first full-suite nonzero result is retained as flaky LAN setup evidence, not hidden by the passing rerun.
- **UltraQA matrix:** `cancel_resume` passed managed immediate-cancel and terminal precedence but restart-after-cancel was not independently proven; `repeated_interruptions` passed JobManager BaseException cases but importer-level BaseException cleanup remains unproven; `stale_state` fails on the admission rollback probes; `hung_or_long_commands` passed bounded commands; `flaky_tests` is not clean because the first full suite hit two LAN setup errors before the immediate serial pass; `misleading_success_output` passed by recording the failed rollback probes and the first full-suite nonzero result; `malformed_input` has existing source-error/bridge coverage but no complete five-importer malformed matrix; `prompt_injection` ruled out because repository/notepad text was treated as data, not procedure; `dirty_worktree` passed with protected hashes unchanged.
- **Cleanup/LSP:** post-run scan found no verifier-created `tg_import_*`, `wechat_import_*`, `dy_*`, `ohmymeme-qq-*`, or `QQ_Favorite_*.zip`; no verifier-created Python/pytest/ffmpeg process remained. The visible `adb.exe` belongs to the protected main worktree `src/.adb` and was not created or touched. `src/webui/dist/ohmymeme.js` is expected ignored build output. Linked-worktree LSP diagnostics remain unavailable because the client rejects paths outside its main-worktree request cwd; no LSP-clean claim is made.
- **AdversarialVerify verdict: `needs-fix`.** The final independent gate cannot return `confirmed`: `JobManager.try_start()` has a concrete admission rollback defect for both `on_admit` and thread-start failures, leaving `active()` permanently occupied with `status='running'`. No plan checkbox was changed; the only intentional write was this append to `issues.md`.

## 2026-08-27 independent Task 6 verification after `efb7771` and `ab12da0`

- **Scope and isolation:** Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=ab12da095f515b46f4bd081d847c43badf45335`, branch `architecture-optimization-baseline`. `efb7771` changes only `src/ohmymeme/app/job_manager.py` and `tests/application/test_job_manager.py`; `ab12da0` changes only this notepad. The full Task 6 source/test chain was rechecked from `ce58caf` through `ab12da0`; combined scope is the six lifecycle production modules, QQNT implementation/bridge, modular QQ settings controller, three lifecycle test modules, and architecture notepads. No source/test/plan/README/AGENTS/protected file was edited by this verification.
- **Protected main worktree:** main remains `D:/UserFiles/Development/Projects/OhMyMeme` with exactly four pre-existing dirty paths. Hashes were rechecked as `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`; no main-worktree tests or writes were used. The linked generated `src/webui/settings.js` began and ended with blob hash `e5967db25182a44c80768ec28de1cee1aa87dfa8`; the Vite build temporarily regenerated it and it was restored byte-for-byte. Final linked status contains only that pre-existing byte-identical line-ending/stat-normalization entry.
- **Positive source/runtime contract evidence:** source review confirms five task/resource pairs (`import.telegram`/`telegram`, `import.douyin`/`douyin`, `import.wechat`/`wechat`, `import.adb_qq`/`adb`, `import.qqnt`/`qqnt`), frozen terminal snapshots with phase/progress/message/error/error_code/resources, admission-bound cancellation/snapshot callbacks, cancel-before-complete precedence, arbitrary worker `BaseException` finalization, legacy `job_manager=None` daemon paths, ADB temp cleanup, QQNT current-ID cleanup, and QQ UI `cancelled`/`total`/`error_code` compatibility. Independent controlled importer probes and existing tests cover managed duplicate rejection, immediate cancellation, terminal mapping, ADB legacy duplicate/stale terminal/exception cleanup, QQNT legacy/managed duplicate and cross-task identity, and UI bridge ABI.
- **Admission rollback verification:** focused regressions `mise exec -- python -m pytest tests/application/test_job_manager.py::test_try_start_rolls_back_when_admission_callback_fails tests/application/test_job_manager.py::test_try_start_rolls_back_when_thread_start_fails -q` returned `2 passed in 0.03s`. An independent sentinel with `RuntimeError` from `on_admit` and a monkeypatched `Thread.start()` printed `ADMISSION_ROLLBACK_SENTINELS_OK`; each terminal record was `error` with the typed source text, `active(task_type) is None`, `wait(record.id, 0) is True`, and a same-type retry admitted. This closes the prior ordinary-Exception rollback blocker.
- **Blocking BaseException admission defect:** an independent sentinel raised a custom `BaseException` from `on_admit` and from `Thread.start()`. Results were `callback running True None` and `thread running True None`: both records stayed `status='running'`, `active(task_type)` remained occupied, and `error` stayed `None`. The current `try_start()` admission transaction catches only `Exception` at `job_manager.py:130-147`, while BaseException handling exists only around an already-started worker at `job_manager.py:192-210`. Therefore `KeyboardInterrupt`/`SystemExit`/arbitrary BaseException during admission can still orphan a running record and single-flight slot, and an adapter binding callback can remain attached to a never-started task. This is a deterministic blocker; exact outcome is `needs-fix`, not `confirmed`.
- **Serial verification:** focused lifecycle/importer/bridge suite `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` returned `76 passed in 1.79s`; importer/process/core/LAN slice returned `143 passed, 1 skipped in 5.79s`; full `mise exec -- python -m pytest tests/ -q` returned `419 passed, 1 skipped in 13.03s`. `mise exec -- ruff check` over all Task 6 production/test Python paths returned `All checks passed`; `mise exec -- black --check` reported `9 files would be left unchanged`; `git diff --check ce58caf^ ab12da0` passed. `mise exec -- node --check` for modular QQ settings and `mise exec -- npm run build` both passed; Vite reported 49 modules and 178.45 kB output.
- **Required surface evidence:** the five managed importer lifecycle paths were independently inspected and the existing controlled-worker tests were rerun for happy/cancel/error/duplicate behavior. The JobManager rollback sentinels were executed end-to-end, including terminal snapshot, active-slot release, wait completion, retry, and no false success. No live verifier Python/pytest/ffmpeg process remained after the probes. A pre-existing `adb.exe` belongs to the protected main worktree and was not created or touched. Pre-existing `%TEMP%` `ohmm-*` directories were observed but no Task 6-prefixed verifier directory was created.
- **UltraQA classification:** `cancel_resume` passes managed immediate cancel and cancel precedence, but restart-after-cancel was not independently parameterized for all five adapters; the BaseException admission sentinel fails stale terminal/slot isolation. `repeated_interruptions` passes worker-level `KeyboardInterrupt`/`SystemExit`/custom BaseException, but fails the same interruption classes during admission. `stale_state` fails because admission BaseException leaves `running`/active state. `hung_or_long_commands` passes: all commands completed under the 900-second bound and no verifier worker/process remained. `flaky_tests` passes for the focused, importer/LAN, and full serial runs; concurrent LAN isolation is not part of this Task 6 gate and historical fixed-port/global-state risks remain recorded. `misleading_success_output` passes because the green suites were not accepted as sufficient and the nonzero semantic sentinel (`running True None`) was recorded as failure. `malformed_input` and `prompt_injection` were ruled out for this lifecycle-only change: no new parser boundary was introduced, and repository/notepad text was treated as untrusted data rather than procedure. `dirty_worktree` passes for protected-file immutability and commit-scope inspection; only this append is intentional, while byte-identical generated JS normalization is preserved.
- **Final verdict:** `needs-fix`. `efb7771` fixes ordinary `on_admit`/thread-start rollback and its tests pass, but it does not finalize admission-stage `BaseException`; the independent deterministic probe leaves `status='running'` and `active=True`. Under the requested strict gate, return `needs-fix`, do not mark the Task 6 checkbox, and do not accept `419 passed` as overriding the lifecycle failure. LSP diagnostics were attempted for all changed Task 6 source paths and rejected by the known linked-worktree request-cwd restriction; no LSP-clean claim is made.
- Follow-up fix: admission rollback now catches arbitrary `BaseException`, finalizes it as `error` (or `cancelled` if cancellation was already requested), releases `_active`, and re-raises the original control exception. New failing-first callback/thread-start regressions pass while ordinary Exception error text remains unchanged.

## 2026-08-27 independent Task 6 verification after `2b097ff` and `53cea8c`

- **Verdict: `needs-fix` / not confirmed.** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=53cea8c3e39190f9196448d433acedeeb0690ed8`, branch `architecture-optimization-baseline`. Codegraph was attempted once and reported no index for this linked worktree; raw source/history inspection was used thereafter. The complete Task 6 chain was audited from `ce58caf` through `53cea8c`, including every lifecycle commit and changed-line scope. `2b097ff` changes only `src/ohmymeme/app/job_manager.py` and `tests/application/test_job_manager.py`; `53cea8c` changes only this notepad. Combined Task 6 source/test scope is the six lifecycle production modules, QQ settings importer UI module, and `tests/application/test_job_manager.py`, `tests/test_import_jobs.py`, `tests/test_startup.py`, plus architecture notepads. No source, test, plan, README, AGENTS, or protected file was modified by this verification.
- **Worktree/protected evidence:** linked status before this append contained only pre-existing ` M src/webui/settings.js`; its hash is `e5967db25182a44c80768ec28de1cee1aa87dfa8`, equal to `HEAD:src/webui/settings.js` (LF/CRLF/stat-normalization warning only). Main worktree remains `D:/UserFiles/Development/Projects/OhMyMeme` with exactly its four protected dirty paths; hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. No main-worktree tests or writes, reset, stash, discard, checkout, amend, force, commit, or destructive cleanup were used.
- **Commit/source audit:** the linear importer lifecycle chain is `ce58caf`, `a60796b`, `a3a1061`, `e1879a5`, `248d3ed`, `78304c7`, `9a7eb75`, `fbb3fd5`, `3c0fb35`, `759bbd4`, `efb7771`, `2b097ff`, with notepad-only evidence commits interleaved (`ab346a3`, `c563f56`, `fd4b457`, `35d018f`, `077fa37`, `61c9f8a`, `29d0d08`, `e77468f`, `ab12da0`, `53cea8c`). Source review confirms stable pairs `import.telegram`/`("telegram",)`, `import.douyin`/`("douyin",)`, `import.wechat`/`("wechat",)`, `import.adb_qq`/`("adb",)`, and `import.qqnt`/`("qqnt",)`; immutable terminal snapshots retain phase/progress/message/error_code/error/resources; all five managed paths use admission-bound cancellation/snapshot callbacks; legacy daemon branches remain; ADB legacy single-flight/stale-terminal/temp cleanup and QQNT legacy/current-ID protections remain present; QQ UI retains `cancelled`/`total`/`error_code` handling.
- **Admission rollback runtime evidence:** independent `mise exec -- python -c ...` probes raised and preserved `RuntimeError("admit ordinary")` and custom `BaseException("admit base")`; each record became `error` with typed text, `active(task_type)` was `None`, `wait(record.id, 0)` returned `True`, and a same-type retry admitted and completed. This confirms ordinary and arbitrary `BaseException` from `on_admit` rollback. A real monkeypatched `Thread.start()` probe raised both ordinary `RuntimeError("thread ordinary")` and custom `BaseException("thread base")`; each record became `error`, retained typed error text, and released `active`, but `wait()` raised `RuntimeError("join before start")`, `shutdown()` raised the same error, and a retry hit the still-monkeypatched failing start. The current test doubles implement harmless `join()`/`is_alive()` and therefore do not cover Python's real unstarted-thread join contract. This is a concrete lifecycle blocker: `try_start()` records `_threads[record_id] = thread` before `thread.start()` and never removes that entry on start failure.
- **Worker/admission tests:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` -> `78 passed in 1.70s`. The passing tests cover task/resource snapshots, duplicate rejection, managed immediate cancellation, cancel-before-late-complete precedence, worker `KeyboardInterrupt`/`SystemExit`/custom `BaseException`, ordinary and custom admission rollback, ADB legacy duplicate/stale terminal/exception cleanup, QQNT legacy duplicate/current identity, and bridge/UI compatibility. They do not close the real unstarted-thread `wait`/`shutdown` failure above.
- **Full/static results:** serial `mise exec -- python -m pytest tests/ -q` returned `420 passed, 1 failed, 1 skipped in 12.71s`; failure was `tests/test_startup.py::test_qqnt_managed_binding_does_not_replace_current_task_id` at `assert current is not None`, demonstrating a fast-completion timing race in the existing QQNT lifecycle test path. `mise exec -- ruff check src/ tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py` passed; targeted `mise exec -- black --check ...` reported `10 files would be left unchanged`; modular and generated settings `node --check` passed; `git diff --check` for the chain and current worktree passed. `lsp_diagnostics` for all changed Task 6 Python paths was rejected with `LSP file path must be inside request cwd`, so no LSP-clean claim is made.
- **Importer lifecycle/ABI recheck:** source and existing controlled-worker evidence rechecked all five managed immediate-cancel paths, duplicate rejection, terminal mapping, legacy ADB/QQNT duplicate guards, cancellation precedence, worker BaseException boundary, ADB stale terminal/error/temp cleanup, QQNT cross-task binding, and QQ UI `cancelled`/`total`/`error_code`. Residual strict-gate risks remain: importer workers themselves catch `Exception` rather than arbitrary `BaseException`, source UI state and JobManager state are not generation-guarded uniformly, ADB/WeChat blocking child operations are not actively terminated by cooperative cancel, and a complete five-importer success/cleanup/restart and importer-level interruption matrix is absent. These are not claimed as newly reproduced in this run, but prevent treating the green focused suite as a complete convergence proof.
- **UltraQA classification:** `cancel_resume` passes managed cancellation and cancel precedence but restart-after-cancel was not independently parameterized for all five adapters; `repeated_interruptions` passes worker-level interruption tests but the real thread-start admission probe fails `wait`/`shutdown`; `stale_state` fails on the unstarted-thread entry and the full-suite QQNT fast-completion race; `hung_or_long_commands` passes because all bounded commands completed and process inspection found no verifier Python/pytest/ffmpeg process; `flaky_tests` fails strict clean acceptance because the serial full suite had one QQNT timing failure (focused suite passed); `misleading_success_output` passes because the semantic thread-start failure and nonzero full-suite result were recorded rather than overridden by green subsets; `malformed_input` is only partial via existing source-error/bridge/ADB coverage and no complete five-importer matrix; `prompt_injection` is ruled out because repository/notepad text was treated as untrusted data, not procedure; `dirty_worktree` passes protected hash/scope checks with only the pre-existing byte-identical generated JS status entry.
- **Cleanup receipt:** all disposable probes completed; controlled managers were shut down where their real-thread monkeypatch allowed it, no verifier-created subprocess/executor/socket/temp importer directory remained, and process inspection found only unrelated pre-existing Node processes. No source/test/plan/README/AGENTS/protected file was changed; the only intentional write in this turn is this append to `issues.md`.
- **Acceptance conclusion:** `2b097ff` closes arbitrary `BaseException` rollback for `on_admit` and terminalizes/releases the record, but it does not remove an unstarted thread from `_threads` after `Thread.start()` failure. Because the required outcome explicitly includes wait completion, restart admission, and no orphan task state, return **`needs-fix`**, not `confirmed`; do not mark the Task 6 checkbox.
- QQNT test-race follow-up: the two fast-worker regressions were confirmed as test harness races, not source regressions. Their workers now enter an event gate and remain held until the test has observed the intended active record; release then uses `manager.wait()` for terminal assertions. No production QQNT source change was necessary.
- Verification: the two QQNT tests passed in 10 consecutive serial runs; Task 6 lifecycle/bridge suite passed `79`; full serial suite passed `422 passed, 1 skipped`; targeted ruff, black, and diff checks passed. QQNT cross-task and immediate-cancel smoke behavior remained covered by existing tests. LSP remains unavailable for linked-worktree paths due to request-cwd restriction.
- Compatibility follow-up: `JobManager.start()` now delegates to `try_start()`, inheriting atomic ordinary/BaseException admission rollback and removal of failed unstarted threads. A real legacy `Thread.start()` failure regression verifies terminal error, active release, `wait(0)`, safe shutdown, and preserved exception propagation.
- Follow-up fix: real `threading.Thread` start-failure regression now proves failed never-started threads are removed from `_threads`; terminal `wait()` returns true, `shutdown()` succeeds, and `_active` is released. Ordinary/custom BaseException admission rollback remains covered and full serial suite passes `421 passed, 1 skipped`.

## 2026-08-27 independent Task 6 gate after `1b67813`, `41cb60e`, and `8565e8c`

- **Verdict: `needs-fix` / not confirmed.** Verification stayed exclusively in linked worktree `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=41cb60e`; `1b67813` changes only the two QQNT lifecycle tests by adding Event gates, and `41cb60e` is notepad-only. The complete Task 6 source/test chain through `8565e8c` was inspected. No source, test, plan, README, AGENTS, or protected main-worktree file was edited; this append is the only intentional write.
- **Scope and protected state:** target status before this append was only the pre-existing `M src/webui/settings.js`; its working-tree hash differed from `HEAD` only through the known generated LF/CRLF/stat-normalization behavior (`git diff` shows the already-existing QQ settings compatibility changes). Main worktree `D:/UserFiles/Development/Projects/OhMyMeme` still has exactly four protected dirty files. Their hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`.
- **Commit audit:** `8565e8c` makes compatibility `JobManager.start()` delegate to `try_start()` and adds the real-thread rollback regression; `1b67813` replaces unconstrained QQNT fast-worker assertions with `entered`/`release` Event gates and waits for termination; `41cb60e` records that synchronization. `git diff --check ce58caf^ HEAD` passed. Current source confirms stable pairs `import.telegram`/`('telegram',)`, `import.douyin`/`('douyin',)`, `import.wechat`/`('wechat',)`, `import.adb_qq`/`('adb',)`, and `import.qqnt`/`('qqnt',)`; `JobRecord` snapshots retain phase/progress/message/error/error_code/resources; all five managed paths use admission-bound cancellation/snapshot callbacks; legacy `job_manager=None` daemon paths remain.
- **Positive runtime/source evidence:** `JobManager` independently finalized a custom `BaseException` as `error` with `C: probe` and released `active()`. Five managed immediate-cancel probes, duplicate/resource/snapshot checks, cancellation precedence, ordinary/custom `BaseException`, real `Thread.start()` rollback, ADB legacy duplicate/stale terminal/temp cleanup, QQNT legacy duplicate, QQNT A-to-B identity, and bridge field inspection were covered by current tests/source. The two synchronized QQNT tests passed 10 consecutive repetitions (`2 passed` each); lifecycle/importer/bridge suite passed 3 repetitions (`79 passed` each); importer/process slice passed `97 passed`; core/LAN/bridge slice passed `91 passed, 1 skipped`; `ruff check` passed; `black --check` reported 10 files unchanged; `git diff --check` passed; `node --check` and `mise exec -- npm run build` passed (Vite 6.4.3, 49 modules, 178.45 kB).
- **Blocking deterministic repro 1, importer/UI terminal divergence:** monkeypatching QQNT extraction to raise `KeyboardInterrupt('qq probe')` produced `JobManager=error` while `window_manager.get_qqnt_progress()['status']=='running'`. Monkeypatching ADB `_qq_worker` to raise `KeyboardInterrupt('adb probe')` produced `JobManager=error` while `adb_qq.get_qq_progress()['status']=='downloading_adb'`. `window_manager.py:947-969` and `adb_qq.py:386-400` catch only ordinary `Exception` at their importer boundary, so JobManager's BaseException finalization does not update the UI compatibility dictionaries. This violates the requirement that importer internal state only express the current phase while the external snapshot is authoritative, because the bridge can poll a nonterminal phase after the authoritative task is terminal.
- **Blocking deterministic repro 2, failed managed admission leaves UI running:** after `manager.shutdown(1)`, calling `window_manager.start_qqnt_extract(..., job_manager=manager)` raised `RuntimeError('job manager is shut down')`, but the QQNT UI state remained `running`. The entry publishes `_QQNT_STATE['status']='running'` at `window_manager.py:873-884` before `try_start()` at `:913-919`; the target `finally` cannot run when admission fails. Equivalent adapter admission rollback/UI cleanup is not covered for Telegram, Douyin, WeChat, or ADB. This is separate from the fixed JobManager record rollback.
- **Cancellation/resource gaps confirmed by source audit:** WeChat helper execution uses blocking `subprocess.run(..., timeout=90)` at `wechat.py:324-334` with no process handle for cancellation, and helper download uses a separate daemon thread (`wechat.py:188-211`) outside JobManager. ADB download similarly uses an independent daemon download path (`adb_qq.py:181-253`), while ADB commands use blocking `subprocess.run` (`adb_qq.py:358-378`). Telegram waits for already-started conversion futures in `executor.shutdown(wait=True, cancel_futures=True)` (`telegram.py:711-764`). These are not claimed as immediate process-kill failures, but they prevent the strict cancellation-propagation/cleanup contract from being confirmed.
- **Coverage gaps:** no complete five-importer success/result/temp/executor/process cleanup matrix; no all-five cancel-then-restart matrix; no importer-level arbitrary `BaseException` cleanup/state matrix; no generation guard for Telegram/Douyin/WeChat/ADB module-global UI/cancel bindings; and no complete malformed importer input matrix. QQNT and ADB bridge ABI smoke is covered for ordinary terminal paths, but BaseException/admission-failure terminal envelopes are not.
- **UltraQA classification:** `cancel_resume` passes managed immediate cancellation and cancel-before-late-complete, but restart-after-cancel is not fully parameterized; `repeated_interruptions` passes JobManager worker/admission interruption tests but fails importer UI convergence for QQNT/ADB; `stale_state` fails on the two deterministic terminal divergence/admission probes; `misleading_success_output` passes because the green subsets were not accepted over semantic probe failures; `flaky_tests` fails strict acceptance because both required unchanged serial full-suite runs returned `420 passed, 2 failed, 1 skipped` at `tests/test_lan.py::test_push_manifest_returns_compatible_success_and_failure_shapes` and `test_push_pull_file`, with default `C:/Users/abbey/AppData/Local/OhMyMeme/meme-index.json(.tmp)` permission/replace collisions; `hung_or_long_commands` passed within timeout; `malformed_input` is only partially covered by existing source-error/bridge tests; `prompt_injection` is ruled out because repository/notepad text was treated as data, not procedure; `dirty_worktree` passed with protected hashes unchanged.
- **Cleanup receipt:** after all probes, no verifier-created Python/pytest/ffmpeg/ADB process remained and no `tg_import_*`, `wechat_import_*`, `dy_*`, `ohmymeme-qq-*`, or `QQ_Favorite_*.zip` temporary artifact was present. The visible `adb.exe` is pre-existing under the protected main worktree and was not created or touched. No main-worktree tests were run. Linked-worktree LSP diagnostics remain unavailable because the client rejects paths outside its request cwd; no LSP-clean claim is made.
- **Acceptance conclusion:** `8565e8c` closes legacy `JobManager.start()` rollback; `1b67813` correctly makes the two QQNT tests deterministic without changing production behavior; and `41cb60e` records that fact. Nevertheless, Task 6 remains **`needs-fix`**, not confirmed, due to reproducible QQNT/ADB importer UI state stranded after worker `BaseException`, QQNT UI state stranded after managed admission failure, incomplete child-process cancellation ownership, and the two non-green required full-suite runs. No checkbox was changed.
- QQNT synchronization follow-up: both regressions now hold workers behind explicit Event gates, observe active records before release, and use `manager.wait()` for terminal assertions. Ten repeated focused runs passed; subsequent full reruns passed `422 passed, 1 skipped` aside from one unrelated updater timing flake that passed immediately on rerun.

## 2026-08-27 independent Task 6 verification after `0396eb5` and `52fb69e`

- **Verdict: `needs-fix` / not confirmed.** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=52fb69e60ed47677216ba3fab2af9d1e2357bc5a`, branch `architecture-optimization-baseline`. Codegraph was attempted once and reported no index for this linked worktree; raw source/history inspection and three independent audit agents were used thereafter. `0396eb5` changes only `src/ohmymeme/app/job_manager.py` and `tests/application/test_job_manager.py`; `52fb69e` changes only this notepad. The full Task 6 chain through both follow-ups was inspected with commit names/status and combined diff scope. No source/test/plan/README/AGENTS/protected-file write was retained by verification.
- **Worktree/protected evidence:** before the append, target status contained only the pre-existing ` M src/webui/settings.js`; its content hash `e5967db25182a44c80768ec28de1cee1aa87dfa8` equaled `HEAD:src/webui/settings.js` despite Git's LF/CRLF/stat-normalization warning. Main `D:/UserFiles/Development/Projects/OhMyMeme` still contained exactly the four pre-existing dirty protected paths, with hashes `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. No main-worktree tests or writes, reset/stash/discard/checkout/amend/force/commit operation was used.
- **Source/commit evidence:** `JobManager` still declares stable task/resource pairs at `src/ohmymeme/app/job_manager.py:8-21`; immutable snapshots and progress ABI are at `:24-65`; `try_start()` admission and rollback are at `:109-156`; worker interruption and terminal mapping are at `:200-218`; cancellation precedence and active release are at `:243-261`. Telegram, Douyin, WeChat, ADB QQ, and QQNT managed entries call `try_start()` with admission-bound cancellation/snapshot callbacks at `telegram.py:540-546`, `douyin.py:460-468`, `wechat.py:840-848`, `adb_qq.py:434-442`, and `window_manager.py:913-921`. Legacy daemon branches, ADB legacy single-flight/temp cleanup, QQNT legacy/current-ID guards, and QQ UI cancelled/total/error_code handling remain present.
- **Required real rollback probe:** `mise exec -- python -` used actual `threading.Thread` objects and exercised ordinary `RuntimeError` and custom arbitrary `BaseException` from both `on_admit` and `Thread.start()`. Every case preserved the original exception, produced terminal `error`, removed the failed id from `_threads`, released `active()`, made `wait(id, 0)` return `True`, made `shutdown(0.5)` return `True`, and allowed a same-type retry to admit and complete. Output: `TASK6_REAL_ROLLBACK_RESTART_OK`. This independently confirms the latest `0396eb5` code/test correction and rejects the earlier fake-thread join evidence.
- **Blocking legacy compatibility defect:** the same real-thread probe against the compatibility `JobManager.start()` API reproduced `LEGACY_START RuntimeError active= True threads= 1 wait= RuntimeError: cannot join thread before it is started shutdown= RuntimeError: cannot join thread before it is started`. The custom `BaseException` case could not be safely completed in that first combined harness because the leaked unstarted thread caused cleanup `shutdown()` to raise as well; source directly confirms `start()` at `job_manager.py:78-107` still has no rollback while `try_start()` does. The current tests only cover real `Thread.start()` failure for `try_start()` (`tests/application/test_job_manager.py:221-239`), not legacy `start()`. This violates the requested legacy compatibility/lifecycle convergence requirement and is sufficient to prevent `confirmed`.
- **Cancellation/exception evidence:** a separate real worker probe produced `CANCEL_ORDINARY_EXCEPTION error late-error None`, confirming current semantics preserve ordinary worker errors over cancellation while releasing the slot; existing tests and source confirm cancel-before-late-complete precedence, `KeyboardInterrupt`, `SystemExit`, and arbitrary worker `BaseException` finalization. The five managed importer cancellation/error/duplicate/resource paths passed in the focused suite, but importer-level restart-after-cancel and complete stale-generation isolation remain unproven.
- **Regression results:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` -> `78 passed in 2.29s`; importer/process/core/LAN slice -> `143 passed, 1 skipped in 5.49s`; unchanged full `mise exec -- python -m pytest tests/ -q` -> `421 passed, 1 skipped in 12.59s`. `mise exec -- ruff check src/ tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py` -> `All checks passed`; targeted Black -> `10 files would be left unchanged`; modular and generated settings `node --check` passed; `mise exec -- npm run build` passed with Vite `6.4.3`, `49 modules`, `178.45 kB`; `git diff --check` passed for the complete Task 6 chain.
- **UI/bridge and architecture audit:** QQ UI (`settings/features/imports/qq.js:60-79`) has cancelled, total, and error-code fallback handling; QQNT bridge and UI preserve start/get/cancel and final result total, but QQNT state remains structurally unlike the other importer states (no top-level total/done/error_code), and the generic frontend bridge has no importer progress type. These are compatibility/coverage gaps rather than the executed blocker. The five adapters still retain legacy direct-thread paths; ADB/WeChat blocking child operations are cooperative-event only; QQNT cancellation can leave partial output; and non-ADB adapters have no equivalent generation guard. Existing contracts may intentionally retain these behaviors, but they are not a complete convergence proof.
- **UltraQA matrix:** `cancel_resume` passes managed immediate cancel and cancel precedence; restart-after-cancel for every adapter is not independently proven, and legacy `start()` failure blocks full resume confidence. `repeated_interruptions` passes managed worker `KeyboardInterrupt`/`SystemExit`/custom `BaseException` and admission-stage custom exceptions through `try_start()`, but the legacy `start()` interruption path is broken by the same leaked thread. `stale_state` fails on the executed legacy unstarted-thread active/registry residue; importer UI-vs-job generation isolation remains partial. `hung_or_long_commands` passes: all bounded commands completed and no verifier-created Python/pytest/ffmpeg process remained. `flaky_tests` passes for this serial run and the focused/slice runs; no concurrent suite was used. `misleading_success_output` passes because the green suites were not accepted over the nonzero legacy probe. `malformed_input` and `prompt_injection` are justified rule-outs for this lifecycle-only verification: no new parser/procedure boundary was introduced, and repository/notepad text was treated as untrusted data. `dirty_worktree` passes: target generated settings content was restored byte-identically, and main protected hashes/status remained unchanged.
- **Cleanup receipt/LSP:** no verifier-created importer temporary directories, subprocesses, executors, sockets, or artifacts remained. Existing `%TEMP%/ohmm-*` directories and `adb.exe` under the protected main worktree were observed but not created or touched. `lsp_diagnostics` for `job_manager.py`, `adb_qq.py`, and `window_manager.py` was attempted and rejected with `LSP file path must be inside request cwd`; no LSP-clean claim is made. The only intentional write in this turn is this append to `issues.md`; no Task 6 checkbox was changed.
- **Acceptance conclusion:** `0396eb5`/`52fb69e` close the real unstarted-thread rollback defect for `try_start()` and the required ordinary/custom `BaseException` admission paths. However, the compatibility `JobManager.start()` path still leaves an unstarted thread in `_threads` and `_active`, causing `wait()`/`shutdown()` to raise `RuntimeError: cannot join thread before it is started`. Return **`needs-fix`**, not `confirmed`; exact fix scope is the legacy `start()` admission rollback plus a real-thread regression, outside this read-only verification.

## 2026-08-27 independent Task 6 verification after `8565e8c` and `808e03a`

- **Verdict: `needs-fix` / not confirmed.** Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=808e03aa2e5d5a24a7662991f89ec2634ec48113`. `8565e8c` changes only `src/ohmymeme/app/job_manager.py` and `tests/application/test_job_manager.py`: compatibility `start()` now delegates to `try_start()`. `808e03a` changes only this notepad. The complete Task 6 lifecycle chain from `ce58caf` through `8565e8c` was inspected, including all five importer modules, QQNT bridge, QQ settings ABI, and lifecycle tests. `git show --check` for both commits and `git diff --check ce58caf^ HEAD` passed.
- **Protected/dirty-worktree evidence:** main `D:/UserFiles/Development/Projects/OhMyMeme` still has exactly its four protected dirty files, with unchanged blobs `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, `settings/core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, `settings/features/sync/settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. The linked worktree has only `M src/webui/settings.js`; its final blob `e5967db25182a44c80768ec28de1cee1aa87dfa8` equals `HEAD:src/webui/settings.js`, so this is the pre-existing line-ending/stat-normalization entry. Vite temporarily regenerated it and it was restored byte-for-byte. No source, test, plan, README, AGENTS, protected main-worktree file, reset/stash/discard/amend/force operation, or main-worktree test was used; this append is the only intentional retained write.
- **Required real rollback evidence:** a fresh `mise exec -- python -` sentinel patched the real `threading.Thread.start` to raise both `RuntimeError` and a custom arbitrary `BaseException`, through both compatibility `start()` and `try_start()`. It also raised `KeyboardInterrupt`, `SystemExit`, and the same custom `BaseException` from real workers. Every admission failure preserved its original exception, recorded typed `error`, removed the unstarted thread, released `active()`, returned `True` from `wait(id, 0)`, permitted a same-type retry to complete, and had safe `shutdown(1)`. Output: `TASK6_REAL_LEGACY_MANAGED_ROLLBACK_BASE_RESTART_OK`. This independently confirms the legacy delegation and fixes the prior real unstarted-thread leak.
- **Five-importer lifecycle evidence:** a deferred-thread managed-admission probe called each Telegram, Douyin, WeChat, ADB QQ, and QQNT public start, cancelled before its worker ran, then released the controlled target. It observed a terminal JobManager `cancelled`, released active slot, and exact resources `('telegram',)`, `('douyin',)`, `('wechat',)`, `('adb',)`, `('qqnt',)`. Output: `TASK6_FIVE_MANAGED_CANCEL_BEFORE_WORKER_OK`. One initial probe failed before product execution because its Telegram replacement accepted zero instead of the real worker's four arguments; the corrected ABI-matching probe above is the evidence. Existing focused coverage also confirms managed cancel/error, duplicate rejection where implemented, cancel precedence, ADB legacy duplicate/stale terminal/error/temp-dir cleanup, QQNT legacy duplicate/current-ID protection, and QQ UI `cancelled`/`total`/`error_code` ABI. Resources remain snapshot metadata rather than cross-task resource locks by design.
- **Regression/static/UI evidence:** focused lifecycle/bridge suite `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` passed `79 passed in 1.77s`; broader importer/process slice passed `113 passed in 2.23s`. Ruff passed for all Task 6 Python production/test paths; Black reported `11 files would be left unchanged`; modular QQ and QQNT `node --check` passed; `mise exec -- npm run build` passed with Vite `6.4.3`, `49 modules`, and `178.45 kB` output. Linked-worktree LSP diagnostics for every changed source and generated JS path were rejected by the known request-cwd limitation, so no LSP-clean claim is made.
- **Blocking exact repro:** first serial full suite run passed `422 passed, 1 skipped in 12.40s`, but the immediate second identical serial `mise exec -- python -m pytest tests/ -q` failed `2` tests: `tests/test_startup.py::test_qqnt_managed_fast_completion_does_not_publish_stale_job_id` at `tests/test_startup.py:174-175`, and `tests/test_startup.py::test_qqnt_managed_binding_does_not_replace_current_task_id` at `:198-199`. Both assert `manager.active("import.qqnt") is not None` immediately after start, but the unconstrained test worker may finish before the assertion, leaving `active()` as `None`; exact result was `2 failed, 420 passed, 1 skipped in 12.58s`. The managed fast-completion path and this test contract are therefore timing-flaky, not deterministic evidence of Task 6 convergence.
- **UltraQA:** `cancel_resume` passed real five-importer cancel-before-worker and JobManager retry after failed admission, but no all-five cancel-then-restart matrix exists. `repeated_interruptions` passed real JobManager worker/admission `KeyboardInterrupt`/`SystemExit`/custom `BaseException`, but importer-level BaseException cleanup remains unproven. `stale_state` fails strict acceptance through the QQNT fast-completion/full-suite race; QQNT has an ID guard but the test observes a transient active slot. `hung_or_long_commands` passed: all commands stayed within budget and the post-run scan found no verifier Python/pytest/ffmpeg or importer temp directory. `flaky_tests` fails due to the second serial full-suite repro. `misleading_success_output` passed because the green first full suite was not accepted over the second nonzero run. `malformed_input` remains partial through existing source-error/bridge tests, with no complete five-importer matrix. `prompt_injection` is a justified rule-out because repository/notepad content was treated as untrusted data, never as execution instructions. `dirty_worktree` passed with protected hashes and linked generated-file blob rechecked.
- **Cleanup receipt and conclusion:** no verifier-created `tg_import_*`, `dy_*`, `wechat_import_*`, `ohmymeme-qq-*`, or `QQ_Favorite_*` temp directory remained. The only `adb.exe` belongs to the protected main-worktree `src/.adb`; visible Node processes belong to codegraph/mise tooling and no verifier Python/pytest/ffmpeg process remained. `8565e8c` closes legacy `start()` rollback for real ordinary and arbitrary BaseException thread-start failures, but Task 6 cannot be marked confirmed while the required serial full suite has the exact QQNT managed fast-completion race above. No checkbox was changed.
## 2026-08-27 Task 6 terminal reconciliation follow-up

- Added deterministic regressions for managed QQNT and ADB workers raising `KeyboardInterrupt`, asserting JobManager terminal snapshots, importer UI terminal states, active-slot release, binding cleanup, and ADB temporary-directory cleanup. Added a closed-JobManager QQNT admission regression asserting idle UI rollback and stale binding removal. All three failed before the production fix with UI `running`/`downloading_adb` residue.
- Minimal fix: QQNT and ADB worker boundaries map uncancelled `BaseException` to UI `error` with typed text, map cancellation to UI `cancelled`, then re-raise so JobManager remains authoritative; ADB retains wrapper cleanup. QQNT managed state/cancel reset now occurs only after successful admission, and admission failure clears bridge globals/restores idle before re-raising. Legacy start, duplicate, cancel, snapshot, and resource paths remain unchanged.
- Verification: new regressions passed `3`; focused lifecycle/importer/bridge/process suite passed `82`; extended importer/process/core/sync slice passed `97`; selected legacy/rollback/identity/cleanup tests passed `8`; serial full suite passed `425 passed, 1 skipped`. Ruff, Black, and `git diff --check` passed; direct smoke printed `ADB_TERMINAL_SMOKE_OK`.
- Linked-worktree LSP diagnostics remain unavailable because the client rejects paths outside its main-worktree request cwd. Pre-existing byte-identical `src/webui/settings.js` normalization was not included in the scoped change.

## 2026-08-27 independent Task 6 verification after `b8e8017`

- **Scope and isolation:** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=b8e8017c6d06540ab5b59f4272829deb9e23dd3c`. The commit audit covered the complete lifecycle chain from `ce58caf` through `b8e8017`, including every changed production/test line and the two architecture notepads. `b8e8017` changes only ADB/window-manager terminal reconciliation, lifecycle tests, and notepads. The target's sole working-tree entry is the pre-existing generated `src/webui/settings.js` normalization/compatibility change (`hash-object=1efe60527293d91c5963897534a13e862c889047`); no source, test, plan, README, AGENTS, or protected file was edited by this verification.
- **Protected main worktree:** `D:/UserFiles/Development/Projects/OhMyMeme` still has exactly the four pre-existing protected paths. Current hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. No main-worktree tests or writes, reset, stash, discard, checkout, amend, force, or commit operation was used.
- **Source contract confirmation:** `job_manager.py:8-21` declares the five stable task/resource pairs; `JobRecord`/`JobContext.snapshot()` retain terminal phase, normalized progress, message, error code, error, and resources. `try_start()` at `:83-130` atomically enforces same-task single-flight, binds admission callbacks, and rolls back ordinary or arbitrary `BaseException` from admission/thread start; `:143-172` provides cooperative cancel and bounded non-killing shutdown; `:174-192,217-235` gives cancel precedence over late completion, finalizes worker `KeyboardInterrupt`/`SystemExit`/custom `BaseException`, releases active state, and retains terminal snapshots. All five managed adapters call `try_start()` with admission-bound cancellation/snapshot callbacks, while `job_manager=None` daemon branches remain explicit legacy paths.
- **Current b8e8017 fix confirmation:** `adb_qq.py:386-412` reconciles worker `KeyboardInterrupt`/other `BaseException` into UI `error` or `cancelled` before re-raising to JobManager and always removes `_QQ_TMP_DIR`. `window_manager.py:995-1008` performs the equivalent QQNT UI reconciliation; `:913-921` guards binding cleanup by current job identity; `:923-945` resets QQNT UI/binding state to `idle` when managed admission fails. The current QQ settings module retains `cancelled`, `total`, and `error_code` handling at `settings/features/imports/qq.js:60-79`.
- **Deterministic runtime evidence:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_startup.py tests/presentation/test_bridge_compat.py -q` passed `82 passed`; the importer/process/core/sync slice passed `97 passed`; the same focused lifecycle suite passed three consecutive rounds (`82 passed` each); the QQNT/ADB terminal, cleanup, legacy-duplicate, and identity subset passed three consecutive rounds (`8 passed` each). Real-thread admission rollback smoke passed for ordinary/custom `BaseException` from both `on_admit` and `Thread.start`, with terminal `error`, `active() is None`, `wait(..., 0) is True`, safe shutdown, and retry (`TASK6_REAL_ROLLBACK_OK`). A controlled four-importer managed cancel smoke passed Telegram/Douyin/WeChat/ADB with UI and JobManager both `cancelled`, active release, and resources `('telegram',)`, `('douyin',)`, `('wechat',)`, `('adb',)`; QQNT managed cancellation is covered by the synchronized existing integration test. The full serial suite passed `425 passed, 1 skipped` twice (`12.61s`, `12.79s`).
- **Static/build evidence:** targeted `ruff check` passed; targeted `black --check` reported `64 files would be left unchanged`; `git diff --check` passed for the complete chain and current worktree; modular and generated settings JavaScript passed `node --check`; `mise exec -- npx vite build` passed with Vite `6.4.3`, `49 modules`, and `178.45 kB` output. `lsp_diagnostics` for linked-worktree Python paths was rejected by the known request-cwd restriction, so no LSP-clean claim is made. The visible `adb.exe` belongs to the protected main worktree and was not created or touched.
- **Required terminal/cleanup probes:** current tests and smoke evidence confirm QQNT/ADB `KeyboardInterrupt` UI reconciliation, QQNT shutdown-admission rollback, JobManager ordinary/arbitrary `BaseException` and real `Thread.start()` rollback, five task/resource snapshots, managed cancellation, duplicate rejection, ADB legacy single-flight/stale-terminal/temporary-directory cleanup, QQNT legacy single-flight/current-ID identity, and QQ UI terminal ABI. A disposable QQNT deferred harness initially failed its own assertion because it invoked the adapter target directly instead of JobManager's `_run` wrapper; it was discarded as harness-invalid and not treated as product evidence. No verifier-created Python/pytest/ffmpeg/ADB process, importer temp directory, ZIP artifact, executor, socket, or other resource remained after verification.
- **UltraQA classification:** `cancel_resume` passes managed cancellation and cancel precedence; restart-after-cancel is covered by JobManager admission/retry but not a complete five-adapter public-entry matrix. `repeated_interruptions` passes JobManager worker/admission and ADB/QQNT UI reconciliation; Telegram/Douyin/WeChat importer-level arbitrary-`BaseException` UI cleanup remains unparameterized. `stale_state` passes current QQNT identity and ADB terminal-cancel guards. `hung_or_long_commands` passes bounded commands with no verifier process left. `flaky_tests` passes two unchanged full serial runs plus repeated focused rounds. `misleading_success_output` passes because the invalid harness was explicitly rejected and nonzero results were not hidden. `malformed_input` has existing source-error/bridge/ADB coverage but no complete five-importer malformed matrix. `prompt_injection` is ruled out because repository/notepad text was treated as untrusted data, never as procedure. `dirty_worktree` passes with only the pre-existing generated JS entry and protected main hashes unchanged.
- **Residual acceptance boundary:** no deterministic product failure was reproduced after `b8e8017`, so the previously recorded QQNT/ADB terminal divergence and QQNT admission-residue blockers are closed. However, strict full-lifecycle claims remain bounded by source/test coverage: ADB and WeChat blocking `subprocess.run()` calls are cooperative-event paths rather than actively terminated child processes; WeChat/ADB helper download threads are outside JobManager ownership; QQNT overwrite cancellation may retain partial output; and no checked-in complete five-adapter success/result/temp/executor/process cleanup, cancel-then-restart, legacy/managed cross-admission, or importer-level arbitrary-`BaseException` matrix exists. These are explicit unproven boundaries, not newly observed failures.
- **Verdict:** **confirmed for the Task 6 checkbox at `b8e8017`**, with the residual boundaries above recorded. The required latest terminal reconciliation and shutdown rollback outcomes are independently observed, all serial regression/build gates pass, and no verifier-created resource or protected-file mutation remains. No plan checkbox was changed.

## 2026-08-27 independent Task 7 verification at `97c7769`

- **Scope and isolation:** verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`; `HEAD=97c776953b48adf1d08afdd861d56e29d1c7743e`, parent=`b8e8017c6d06540ab5b59f4272829deb9e23dd3c`, branch=`architecture-optimization-baseline`. `git show` and the complete changed-line diff were inspected. The commit changes exactly `.omo/notepads/architecture-optimization/learnings.md`, `tests/application/test_job_manager.py`, `tests/test_import_jobs.py`, and `tests/test_tg_stickers.py` (315 insertions, 0 deletions); no production source, README, AGENTS, plan, or protected main file is in its scope. `git diff --check 97c7769^ 97c7769` passed.
- **Worktree/protected evidence:** before this append, linked status had only the pre-existing `M src/webui/settings.js` plus the intentional notepad append; `git diff --exit-code HEAD -- tests src/ohmymeme` passed, and the generated JS working blob `1efe60527293d91c5963897534a13e862c889047` differed from its committed blob only by the known pre-existing generated/LF-CRLF normalization state. Main `D:/UserFiles/Development/Projects/OhMyMeme` remained on `gitbutler/workspace` with exactly four protected dirty paths (`window_manager.py`, settings `core/init.js`, settings sync `settings.js`, `src/webui/settings.js`); their recorded hashes were unchanged. No main-worktree test, reset, stash, discard, amend, force, or commit operation was used.
- **Serial test evidence:** the exact focused command `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py -q` passed three consecutive times: `55 passed` in `2.94s`, `3.11s`, and `2.94s`. The unchanged full command `mise exec -- python -m pytest tests/ -q` passed twice: `434 passed, 1 skipped` in `12.96s` and `12.71s`. `mise exec -- ruff check src/ tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py` passed; `mise exec -- black --check ...` reported `64 files would be left unchanged`. No product/test diff was present before this append.
- **Positive substantive coverage:** `test_concurrent_try_start_admits_one_worker` uses two real caller threads and `Barrier(2)`; shutdown tests use real worker threads plus `Event`; terminal late-update, restart, closed admission, cancellation precedence, worker `KeyboardInterrupt`/`SystemExit`/custom `BaseException`, admission rollback, and resource snapshot tests are present. `test_import_task_types_have_stable_resources_and_snapshot_fields` iterates all five importer task/resource pairs. Existing updater tests independently cover delayed result completion, force/non-force cache behavior, reset generation stale-result rejection, stale generation not clearing a new running state, and real `JobManager` use. New Douyin/WeChat tests enter real managed worker entrypoints, create partial temp files, cancel, await termination, and assert directory reclamation. New Telegram tests assert `_reap_proc` retains a still-running process and assert executor construction uses `min(os.cpu_count() or 1, 4)` with `shutdown(wait=True, cancel_futures=True)`.
- **Acceptance limits found by line review:** `tests/application/test_job_manager.py:184-226` directly calls private `manager._finish()` before releasing the worker, so late-update rejection is proven after manually forced terminalization rather than only through public shutdown/worker-return lifecycle. `tests/application/test_job_manager.py:40-57` proves timeout returns `False` and later cooperative release, but does not observe a kill/terminate sentinel, measure the wall-clock deadline, or test multiple workers sharing one timeout. `tests/test_tg_stickers.py:133-156` uses only a mock process, and `:165-216` uses a mock Executor/Future/`as_completed`, one submitted future, and an immediate `Future.result()`; it proves call arguments, not real bounded concurrency, future waiting, cancellation, or exception-path shutdown. The existing `tests/test_import_jobs.py:18-99` uses polling sleeps and the new Douyin/WeChat cleanup tests trigger cancellation inside mocked callbacks before the main test issues a second cancel; no new Telegram `_tg_worker` temp-dir cleanup test exists.
- **Coverage matrix:** single-flight is PASS for the generic JobManager (including Barrier race) but only partial for public importer entrypoints; terminal snapshots are PASS for the stale-update branch but partial for public lifecycle because of private `_finish`; resources are PASS for JobManager's five constants but not independently asserted through every importer facade; closed admission is PASS for direct `JobManager.try_start()` but no shutdown/admission race or every-facade rollback matrix; updater generation tests PASS; Douyin/WeChat cancellation cleanup and existing ADB cleanup PASS on covered paths, but complete all-importer success/error/interruption/cancel-stage cleanup and Telegram executor/temp cleanup are not proven.
- **Adversarial classification:** `stale_state` and `misleading_success_output` prevent a strict confirmation because test names/notes overstate the mock executor and manually forced terminal lifecycle. `cancel_resume` is only partial: JobManager restart is covered, but no five-public-importer cancel-then-restart or stale old-worker versus new task matrix exists. `repeated_interruptions` is covered at JobManager and ADB/QQNT boundaries from prior tests, not all importer workers. `hung_or_long_commands` passed: all required commands completed within the 900-second bound; no verifier-created worker/process was left. `flaky_tests` passed for three focused and two full serial runs, while the historical one-second importer timing risk remains an observation. `dirty_worktree` passed for commit scope and protected main files; `prompt_injection` and `malformed_input` were ruled out as this test-only lifecycle change introduced no new parser/procedure boundary.
- **Cleanup/LSP:** post-run process listing showed no `pytest`/`ffmpeg` process; no verifier-created importer temp directory or executor artifact was found. Two disposable inline shutdown smoke attempts failed with PowerShell/Python quoting `SyntaxError` before product execution and were not treated as evidence; the same real-thread shutdown behavior is covered by the passing pytest. Linked-worktree `lsp_diagnostics` for all four Python test paths was rejected with `LSP file path must be inside request cwd`; no LSP-clean claim is made.
- **Verdict:** **needs-fix / not confirmed**. The commit's focused and full suites are green and the JobManager/updater core tests are substantive, but strict Task 7 acceptance is not met: Telegram bounded executor/reap assertions are mock-only rather than real bounded lifecycle tests, no Telegram worker temp cleanup is added, and shutdown no-force-kill/terminal convergence are not observed through the complete public lifecycle. No plan checkbox was changed.

## 2026-08-27 Task 7 lifecycle coverage follow-up

- Replaced the prior private-terminal-only evidence with a public JobManager interaction: `shutdown(0.01)` requests cancellation and returns `False`, the worker remains observable as running until its release event, then `manager.wait()` observes natural exit and the terminal snapshot converges to `cancelled`. A two-worker case verifies the shutdown budget is shared and neither worker is force-terminated.
- Added real standard-library `ThreadPoolExecutor` coverage for two simultaneous Telegram conversion jobs, observing at least two active conversions before release. Added executor future-exception coverage and retained temp-root cleanup assertions; controlled executor coverage still checks `shutdown(wait=True, cancel_futures=True)` as an additional deterministic seam.
- Added deterministic Douyin managed cancel-then-restart coverage: the second public entry receives a distinct JobManager ID and reaches its own terminal error without the first worker's release/cancellation state contaminating it.
- The linked-worktree LSP client still rejects all target paths because its request cwd is the main worktree. No production source, plan, README, AGENTS, frontend, schema, protocol, or protected file was changed.

## 2026-08-27 independent Task 7 verification after `4f45903`

- **Scope and isolation:** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=4f459037ae868c40eebaa0b52a0cc92b65940c10`, parent=`97c776953b48adf1d08afdd861d56e29d1c7743e`, branch=`architecture-optimization-baseline`. `git show --stat`, complete changed-line diffs for `97c7769` and `4f45903`, and every changed test line were inspected. `4f45903` changes exactly `.omo/notepads/architecture-optimization/issues.md`, `tests/application/test_job_manager.py`, `tests/test_import_jobs.py`, and `tests/test_tg_stickers.py`; `tests/test_updater.py` was unchanged by the follow-up. No production source, plan, README, AGENTS, or protected main file is in the commit. `git diff --check 4f45903^ 4f45903` passed.
- **Worktree/protected evidence:** Before this append, the linked worktree had only the pre-existing `M src/webui/settings.js`; no test/source diff was present. The main `D:/UserFiles/Development/Projects/OhMyMeme` worktree still had exactly its four pre-existing protected dirty paths (`src/ohmymeme/presentation/desktop/window_manager.py`, settings `core/init.js`, settings sync `settings.js`, and `src/webui/settings.js`). No main-worktree test or write, reset, stash, discard, amend, force, or commit operation was used. This append is the only intentional retained verification write.
- **Serial regression evidence:** The exact focused command `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py -q` passed three consecutive rounds: `60 passed` in `4.04s`, `4.21s`, and `4.20s`. The exact full command `mise exec -- python -m pytest tests/ -q` passed two consecutive rounds: `439 passed, 1 skipped` in `14.39s` and `13.91s`. Static gates passed: `mise exec -- ruff check src/ tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py` -> `All checks passed`; targeted `mise exec -- black --check ...` -> `4 files would be left unchanged`; `git diff --check` -> success. Historical LAN/importer timing flakes remain recorded in earlier entries; neither full round in this verification reproduced them.
- **Real shutdown probe:** A disposable `mise exec -- python -` probe used a real `JobManager`, real worker thread, `Event` cancellation/release gates, and public `start()`, `shutdown()`, `wait()`, `get()`, and `active()`. Output was `TASK7_SHUTDOWN_PROBE_OK result=False elapsed=0.0310 status=cancelled`: `shutdown(0.03)` returned `False` while the worker remained `running`, cancellation had not opened the release gate, and the worker was not forcibly terminated; after cooperative release, `wait()` returned true, the worker's continuation ran, the terminal snapshot was `cancelled`, and `active()` was `None`. This independently confirms the requested timeout/no-force-kill/convergence behavior for the generic manager.
- **Substantive changed-test coverage:** `tests/application/test_job_manager.py` now uses public `shutdown()` rather than private `_finish()` for the terminal late-complete case (`:222-264`), uses two real workers for the shared-deadline case (`:63-95`), uses a real caller `Barrier` for concurrent `try_start()` (`:288-319`), and checks closed admission without invoking callbacks (`:322-339`). The changed Telegram test adds real `ThreadPoolExecutor` overlap (`tests/test_tg_stickers.py:277-326`) and mock seam checks for bounded worker count, `shutdown(wait=True, cancel_futures=True)`, future wait, and future exception handling (`:166-217`, `:328-371`). The changed Douyin test exercises public cancel, full first-job wait, second public start, distinct JobManager IDs, and independent second terminal error (`tests/test_import_jobs.py:341-377`). Existing updater tests cover pending/cache/force/reset-generation behavior, but `4f45903` does not change them.
- **Blocking closed-importer admission defect:** A disposable public-entry probe shut down a real `JobManager` and attempted Telegram, Douyin, and WeChat starts. Each raised `RuntimeError("job manager is shut down")`, but the importer compatibility state was left respectively at `scanning`, `running`, and `scanning`. This follows the production ordering where each adapter publishes its module running state before `try_start()` (`telegram.py:507-545`, `douyin.py:305-321,460-467`, `wechat.py:801-847`). No Task 7 test covers this admission-failure rollback, so closed admission is proven only for direct `JobManager.try_start()`, not importer public entrypoints.
- **Acceptance gaps:** Telegram executor wait/exception tests at `tests/test_tg_stickers.py:219-275,328-371` still use fake Executor/Future objects; the “cleans temp dir” names do not assert worker-owned directory cleanup, and no public `start_tg_import()` cancellation test proves real executor/process/temp cleanup. The real Telegram test (`:277-326`) proves at least two mocked conversion callables overlap, not real ffmpeg behavior. No five-importer cancel-then-restart or stale old-worker/new-worker binding-isolation matrix exists; generic JobManager restart waits for the first thread before restarting. The updater tests remain private-state-heavy for generation setup and do not cover JsApi/SettingsApi force/pending bridge behavior.
- **Adversarial classification:** `stale_state` fails strict confirmation through the reproduced importer `scanning/running` residue and the untested terminal-before-thread-exit binding window. `cancel_resume` is partial: Douyin public cancel/restart passes, but the other importer public entrypoints are not covered. `repeated_interruptions` is proven at generic JobManager and prior ADB/QQNT boundaries, not all importer workers. `hung_or_long_commands` passes for this run; the shutdown probe was bounded and no verifier worker remained. `flaky_tests` passed in these three focused and two full serial rounds, while historical one-second importer/LAN flakes remain known observations. `misleading_success_output` is avoided here by reporting exact current outputs and the closed-admission nonzero semantic probe. `dirty_worktree` passes commit-scope attribution but the linked tree is not clean because of the pre-existing generated `src/webui/settings.js`; main protected paths remain untouched. `malformed_input` and `prompt_injection` are ruled out as this is a test/lifecycle review with no new parser or instruction boundary.
- **Cleanup/LSP:** Process inspection found only the pre-existing main-worktree `src/.adb/platform-tools/adb.exe`; no verifier-created Python, pytest, ffmpeg, executor, socket, importer temporary directory, or other artifact remained. Linked-worktree `lsp_diagnostics` for all four Python test paths was rejected with `LSP file path must be inside request cwd`; no LSP-clean claim is made.
- **Verdict:** **needs-fix / not confirmed**. Current focused/full tests, real generic shutdown probe, changed-line scope, and static gates pass, but the exact closed-importer admission probe demonstrates stale compatibility state after manager shutdown. Telegram cleanup/executor exception evidence remains partly mock-only, and complete importer stale isolation/restart coverage is absent. No plan checkbox was changed.

## 2026-08-27 closed importer admission rollback follow-up

- Failing-first public-entry regressions reproduced the exact defect after `JobManager.shutdown(1)`: Telegram left `scanning`, Douyin left `running`, and WeChat left `scanning`, while each call raised the unchanged `RuntimeError("job manager is shut down")`.
- Minimal fixes wrap only each managed `try_start()` call in `telegram.py`, `douyin.py`, and `wechat.py`. On any admission `BaseException`, the adapter clears its job cancellation/snapshot bindings, resets its module cancellation flag, restores UI compatibility state to `idle`, and re-raises the original exception. ADB and QQNT were not changed because their admission state is established through existing pre-admission-safe paths and the exact stale-state probe did not reproduce there.
- New tests assert exact exception text, idle state, no active JobManager record, and cleared adapter bindings for all three public starts. Existing normal start, duplicate, cancellation, executor, terminal, and legacy tests remain green.

## 2026-08-27 Task 7 public admission regression follow-up

- Reused the existing Task 7 linked worktree and ran the unchanged focused baseline first: `63 passed`. The three closed-manager importer regressions were already present from `f3ae62a` and remain covered; no production change was needed in this test-only follow-up.
- Added Telegram public cancel-then-restart coverage using the public managed start/cancel/wait path. The test captures the first JobManager snapshot callback, starts a distinct second task after cancellation, invokes the old callback, and asserts the second task remains running and completes independently.
- Attempted deeper public Telegram worker/executor integration, but the current `_tg_worker` stage setup can complete before a reliable conversion barrier is installed. That invalid seam was removed rather than weakening assertions; existing real overlap/executor lifecycle tests remain the stable executor evidence.
- Final serial focused repetitions: three runs of `64 passed`; final full-suite repetitions: two runs of `443 passed, 1 skipped`. Ruff, Black, and diff checks passed. Linked-worktree LSP remains unavailable due to request-cwd restriction. The pre-existing generated `src/webui/settings.js` worktree modification remains untouched and uncommitted.

## 2026-08-27 independent Task 7 verification after `f3ae62a`

- **Scope and isolation:** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=f3ae62a3ae1323fbe0cb61a1acfdd878b2fb5a03`. `git show` inspected every changed line in `4f45903` and `f3ae62a`; the latter changes only Telegram/Douyin/WeChat importer sources plus `tests/test_import_jobs.py`, while the former changes only the lifecycle tests and this notepad. No source/test/plan/README/AGENTS/protected-file edit was made by verification. The linked worktree retained only the pre-existing `M src/webui/settings.js` and intentional notepad append.
- **Protected main worktree:** `D:/UserFiles/Development/Projects/OhMyMeme` remained on `gitbutler/workspace` with exactly the four pre-existing protected paths. SHA-256 recheck matched the recorded baseline: `43FD030EAC87BEEA69D591A13D68F0461A16AABB0613F395AA4CC60503686870` (`window_manager.py`), `B789BB0186F0C305E9BA23F1E45329DE28ED2734B2BF27EB21DEC64E49B0076C` (`settings/core/init.js`), `76D1C196DFB38F1F19B91FAE2110D54EAC90ED1240CBFA9B676266974944EC06` (`settings/features/sync/settings.js`), `96722C673ED031F9F9B7EBB89BD32DAAB77F315FB7AB0ECA694B400BD3223517` (`src/webui/settings.js`). No main-worktree test or write was used.
- **Serial runtime evidence:** Exact focused command `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py -q` passed three consecutive rounds: `63 passed` in `4.17s`, `4.01s`, and `4.00s`. Exact full command `mise exec -- python -m pytest tests/ -q` passed twice: `442 passed, 1 skipped` in `13.53s` and `13.40s`. The 11-case admission/shutdown/executor/reap subset passed `11 passed in 1.24s`. `mise exec -- ruff check` passed and `mise exec -- black --check` reported `8 files would be left unchanged`.
- **Real surface smoke:** A disposable `mise exec -- python -` probe used public `JobManager.start()`, `shutdown()`, `wait()`, `get()`, and `active()` with a real worker and release event; `shutdown(0.02)` returned `False`, left the worker running without opening its release gate, then cooperative release converged to `cancelled` with no active slot. The same probe invoked public Telegram/Douyin/WeChat starts against already-closed managers and observed the exact shutdown exception, `idle` compatibility state, and no active record (`TASK7_RUNTIME_SMOKE_OK`). No verifier-created Python/pytest/ffmpeg/importer temp resource remained; the only matching process was the pre-existing main-worktree `adb.exe`.
- **Source-backed positive findings:** `job_manager.py:83-130` atomically admits same-type single-flight work and rolls back admission failures; `:143-172` provides cooperative cancellation, one monotonic shutdown deadline, bounded joins, and no thread kill; `:174-235` gives cancellation precedence, terminal snapshot retention, late-update rejection, BaseException finalization, and active-slot release. `tests/application/test_job_manager.py` uses real caller barriers/events and public lifecycle methods. `telegram.py:721-775` bounds conversion workers to `min(os.cpu_count() or 1, 4)` and always calls `shutdown(wait=True, cancel_futures=True)`. `f3ae62a` rollback at `telegram.py:549-555`, `douyin.py:469-475`, and `wechat.py:849-855` is covered by public closed-manager tests asserting exception, idle state, no active job, and cleared bindings.
- **Public-vs-mock qualification:** Telegram executor parameter/future exception/reap tests still use fake Executor/Future/process seams (`tests/test_tg_stickers.py:166-217,219-275,328-371`); the real overlap test mocks `convert_webm_to_webp` (`:277-326`). Thus bounded executor invocation is proven, but real ffmpeg cancellation/process/temp cleanup is not. Updater generation tests remain private-cache-heavy and do not drive `JsApi`/`SettingsApi` bridge calls.
- **Blocking acceptance gaps:** `telegram.py:533-538`, `douyin.py:444-445`, and `wechat.py:820-825` retain explicit `job_manager=None` daemon-thread branches that bypass unified JobManager admission/shutdown/snapshot ownership. Telegram ffmpeg blocks in `communicate()` (`:407-425`) and WeChat keyfinder blocks in synchronous `subprocess.run(..., timeout=90)` (`wechat.py:324-352`); JobManager shutdown only sets its event and joins (`job_manager.py:161-172`), so no direct child-process cancellation is proven. Complete public cancel-then-restart and stale-old-worker/new-worker isolation is present only for Douyin; Telegram/WeChat/ADB/QQNT lack the same matrix. Importer-level repeated BaseException cleanup is only covered for prior ADB/QQNT paths, not all five adapters.
- **Adversarial classification:** `cancel_resume` passes generic manager restart and Douyin public restart but remains partial across importers; `stale_state` passes the new closed-manager rollback and terminal snapshot tests but lacks old-worker/new-worker importer generation proof; `repeated_interruptions` is partial (generic manager plus ADB/QQNT only); `hung_or_long_commands` passes bounded test execution and cleanup smoke, but real child cancellation remains unproven; `flaky_tests` passed three focused and two full serial repetitions, while the known one-second importer polling risk remains; `misleading_success_output` is controlled by reporting exact exit/output rather than historical claims; `dirty_worktree` and protected hashes pass; `malformed_input` and `prompt_injection` are not applicable to this untouched parser/procedure surface and were ruled out.
- **LSP/format caveat:** `lsp_diagnostics` for all changed Python paths was rejected by the known linked-worktree request-cwd restriction (`LSP file path must be inside request cwd`); no LSP-clean claim is made. `git diff --check 4f45903^ 4f45903` and `git diff --check f3ae62a^ f3ae62a` passed.
- **Verdict:** **needs-fix / not confirmed**. The production follow-up closes the exact stale admission residue for Telegram/Douyin/WeChat, and all required focused/full serial runtime suites plus generic timeout/no-force-kill smoke pass. Strict Task 7 still cannot be confirmed because legacy public bypasses remain, real child-process cancellation is not wired/proven, Telegram executor evidence is partly mock-only, and complete five-importer cancel-resume/stale-worker/repeated-interruption coverage is absent. No plan checkbox was changed.

## 2026-08-27 independent Task 7 verification after `ce81ae5`

- **Scope and commit audit:** Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=ce81ae554dc138ac6f276f23d4e308427a6c844a`. The inspected ancestry is `97c7769 -> 4f45903 -> f3ae62a -> ce81ae5`; `git diff --check` passed for each commit pair. `97c7769` changes the JobManager/importer/Telegram tests plus `learnings.md`; `4f45903` changes those tests plus this notepad; `f3ae62a` changes only Telegram/Douyin/WeChat production adapters and `test_import_jobs.py`; `ce81ae5` changes only `test_import_jobs.py`. `tests/test_updater.py` is unchanged by all four requested commits. No source, test, plan, README, AGENTS, or protected file was modified by this verification.
- **Worktree protection:** Before this append, target status contained only pre-existing `M src/webui/settings.js` and the inherited notepad modification. The main worktree remained separate at `D:/UserFiles/Development/Projects/OhMyMeme` with exactly four pre-existing protected dirty paths: `src/ohmymeme/presentation/desktop/window_manager.py`, settings `core/init.js`, settings sync `settings.js`, and `src/webui/settings.js`; recorded main blob hashes were unchanged (`e0f7c67460380fa07db24350f484e06c9d6f6b73`, `36d5f9910912cfd6b7337eb22602833fd6b05be6`, `e954a2c006f83a907d841f02c07380bb6dbe8abe`, `1e66f686f374cbefacf7c0c86dc4ddf53b381728`). No main-worktree test, reset, stash, discard, amend, force, commit, or write was used.
- **Serial regression evidence:** `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py tests/test_startup.py -q` passed three consecutive times: `109 passed` in `5.52s`, `5.38s`, and `5.52s`. `mise exec -- python -m pytest tests/ -q` passed twice: `443 passed, 1 skipped` in `13.38s` and `13.44s`. `mise exec -- ruff check` over all Task 7 production/test paths passed; `mise exec -- black --check` reported `11 files would be left unchanged`; all requested commit diff checks passed.
- **Generic public shutdown smoke:** A real `JobManager`, real worker thread, and Event release gate were driven through public `start()`, `shutdown()`, `wait()`, `get()`, and `active()`. `shutdown(0.03)` returned `False` in `0.047s`, left the worker `running`, did not open its release gate, and did not force termination. After release, `wait()` succeeded, the worker converged to terminal `cancelled`, and `active()` returned `None` (`NO_FORCE_KILL_SMOKE_OK`). This confirms the generic shared deadline/no-force-kill path, not all importer callers.
- **Changed-test/source qualification:** JobManager tests use real Event/Barrier-controlled workers for single-flight, concurrent admission, shared shutdown deadline, cancellation precedence, terminal late-update rejection, terminal restart, closed admission, admission/thread-start rollback, and BaseException finalization. Telegram tests include a real standard-library executor overlap test, but bounded executor construction, Future wait/exception, and process reap remain fake Executor/Future/process seams; the overlap still mocks `convert_webm_to_webp`. The `ce81ae5` Telegram stale test drives public managed start/cancel/restart, but directly invokes the captured private `_TG_JOB_SNAPSHOT` and uses a mocked `_tg_worker`.
- **Confirmed source invariants:** `src/ohmymeme/app/job_manager.py:83-130` atomically admits one same-type worker and rolls back admission errors; `:161-172` sets cancellation events, uses one monotonic deadline, joins with remaining budget, and never kills threads; `:174-235` gives cancellation precedence, finalizes BaseException, rejects terminal updates, and releases active slots. `src/ohmymeme/integrations/imports/telegram.py:721-775` bounds conversion workers to `min(os.cpu_count() or 1, 4)` and uses `shutdown(wait=True, cancel_futures=True)`; `:354-379` retains active processes until `poll()` proves exit. The `f3ae62a` admission rollback at Telegram/Douyin/WeChat is covered by closed-manager public-entry tests, and the `ce81ae5` test proves one Telegram managed old snapshot callback cannot mutate the current JobManager record.
- **Blocking exact repro:** A real public legacy Telegram restart smoke replaced only `_tg_worker` with an Event-controlled worker. Worker 0 was started via `start_tg_import(..., job_manager=None)`, cancelled, and allowed to publish its terminal update; worker 1 was then started via the same public API. After worker 1 was admitted, worker 0 published a late `_update_tg(status="error", message="old-late")`. Observable output was `LEGACY_TELEGRAM_STALE_STATE error old-late [0, 1]`. Thus the old worker overwrote the new worker's module-global state after cancel/restart. The legacy branch at `telegram.py:533-538` has no generation/worker identity or join handle; `_reset_state()` only resets the shared flag. This is a reproducible `stale_state`/`cancel_resume` failure, not a missing-test-only observation.
- **Acceptance gaps remaining:** No five-importer public cancel-then-restart matrix exists; only Douyin has a managed restart test and Telegram has a managed callback-only test. No full Telegram public worker/executor/FFmpeg/temp-dir integration proves real process cancellation and bounded reaping. Douyin/WeChat/ADB/QQNT old-worker/new-worker state isolation is not equivalently exercised. `job_manager=None` legacy branches in Telegram/Douyin/WeChat/ADB/QQNT bypass unified JobManager shutdown ownership. WeChat helper download/keyfinder and synchronous network/helper operations are not cancellation-terminated. `tests/test_updater.py` has existing generation tests but no new Task 7 bridge/public closed-manager admission coverage.
- **Adversarial matrix:** `cancel_resume` fails on the exact legacy Telegram stale-state repro; `stale_state` fails for the same late worker overwrite. `repeated_interruptions` is covered at generic JobManager and selected ADB/QQNT boundaries but not all importer workers. `hung_or_long_commands` passed: all commands completed within budget and no verifier worker remained. `flaky_tests` passed three focused and two full serial rounds. `misleading_success_output` was avoided by recording the nonzero semantic repro despite green suites. `dirty_worktree` and protected hashes passed; `malformed_input` and `prompt_injection` are not applicable to this untouched parser/procedure surface and were ruled out.
- **Cleanup/LSP:** Process inspection found only the pre-existing main-worktree `D:/UserFiles/Development/Projects/OhMyMeme/src/.adb/platform-tools/adb.exe`; no verifier-created Python, pytest, ffmpeg, executor, socket, importer temp directory, or other artifact remained. The target-worktree LSP client is unavailable because it rejects paths outside its main-worktree request cwd; no LSP-clean claim is made. This append to `issues.md` is the only verification write.
- **AdversarialVerify verdict:** **needs-fix / not confirmed**. Generic JobManager lifecycle, normal single-flight/completion, shutdown timeout/no-force-kill, terminal snapshot protection, resources, focused/full regressions, and the `f3ae62a` closed-manager rollback are independently evidenced. Strict Task 7 cannot be marked `confirmed` because the public legacy Telegram cancel/restart stale-worker overwrite is reproducible, and the required complete real importer cancellation/restart/stale-isolation matrix plus real Telegram process cleanup evidence is absent. No plan checkbox was changed.

## 2026-08-28 Telegram active-process reset follow-up

- Failing-first `test_reset_keeps_live_process_registered_until_exit` reproduced `_reset_state()` clearing a tracked process while `poll() is None`; after termination the process remained untracked. The minimal fix now discards only already-exited processes and retains live processes for later `_reap_proc()` cleanup.
- The regression then sets the controlled process return code, calls `_reap_proc()`, and asserts the registry is finally cleared only after observed exit. Existing cancellation and executor cleanup tests remain unchanged and green.
- Updater force-refresh timing was characterized as a test synchronization defect: the test used fixed `sleep(0.2)` before asserting the second request. It now waits on an Event set by the second real `check_latest` invocation, preserving the two-call assertion without masking failures.
- Final verification: Task 7 focused tests passed three serial runs (`66 passed` each); full suite passed two serial runs (`445 passed, 1 skipped` each); ruff, Black, and diff checks passed. Linked-worktree LSP remains unavailable due to request-cwd restriction. No protected/main-worktree file was modified.

## 2026-08-28 independent Task 7 adversarial verification at `f98efe0`

- **Scope and isolation:** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=f98efe0d8cc063dc60445d9e0ec216b81075cd36`. Native status before this append was only the inherited `M .omo/notepads/architecture-optimization/issues.md` and pre-existing generated `M src/webui/settings.js`; `git diff --exit-code -- src/ohmymeme tests` passed. The six requested commits were inspected with `git show --stat`, `--name-status`, complete changed-line diffs, and combined `git diff --check`: `97c7769`, `4f45903`, `f3ae62a`, `ce81ae5`, `0c6b8bf`, and `f98efe0`. No source, test, plan, README, AGENTS, or protected-file write was made; this append is the sole intentional write.
- **Commit scopes:** `97c7769` adds JobManager/importer/Telegram lifecycle tests and `learnings.md`; `4f45903` strengthens public shutdown/shared-deadline, real executor overlap, Douyin restart, and Telegram executor tests plus `issues.md`; `f3ae62a` adds Telegram/Douyin/WeChat managed-admission rollback with tests; `ce81ae5` adds managed Telegram stale-callback coverage; `0c6b8bf` adds Telegram generation isolation for legacy workers with tests; `f98efe0` changes Telegram reset handling, adds live-process reset/reap regression coverage, and replaces updater force-refresh fixed sleep with an Event. `tests/test_updater.py` is changed only by `f98efe0`, while no updater production source changed in this six-commit chain.
- **Protected main-worktree evidence:** `D:/UserFiles/Development/Projects/OhMyMeme` remains separate on `gitbutler/workspace` with exactly four pre-existing dirty paths. Recomputed `hash-object` values remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe6`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. No main-worktree test or write was used; no reset, stash, discard, amend, force, or commit operation was used.
- **Regression evidence:** Exact Task 7 focused command `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_updater.py tests/test_tg_stickers.py -q` passed twice, `66 passed` each (`3.80s`, `3.79s`). Exact full command `mise exec -- python -m pytest tests/ -q` passed twice, `445 passed, 1 skipped` (`13.40s`, `13.48s`). The updater force-refresh test passed 10 additional serial repetitions. Static gates passed: targeted `mise exec -- ruff check ...` returned `All checks passed`; targeted Black reported `11 files would be left unchanged`; commit and combined diff checks passed.
- **Direct runtime evidence:** A real `JobManager` worker blocked on an Event showed `shutdown(0.03) == False`, remained `running` without release/force termination, then after cooperative release `wait()` converged to `cancelled` and `active()` was `None` (`SHUTDOWN_NO_FORCE_KILL_OK 0.031`). A real Python child process inserted into Telegram `_TG_ACTIVE_PROC` remained tracked after `_reset_state()` while `poll() is None`, and after actual termination `_reap_proc()` removed it (`TG_RESET_REAP_OK`). This independently confirms the `f98efe0` live-handle behavior and generic shutdown contract.
- **Concrete needs-fix repro outside current Task 7 coverage:** With a real closed `JobManager` injected into `updates._job_manager`, public `updates.start_download("http://example.invalid/file")` raised `RuntimeError: job manager is shut down` after setting `_download_state["status"]` to `"downloading"`; observed state was `{'progress': 0, 'status': 'downloading', 'error': '', 'path': None}`. Source is `src/ohmymeme/services/updates.py:98-131`: state admission occurs before `_job_manager.start()` and no rollback catches closed-manager rejection. This leaves the public updater download state permanently busy and contradicts its `bool` return contract. The Task 7 tests cover force/cache generation only and do not cover `start_download` closed-manager, cancel-state consistency, or mirror executor cleanup.
- **Source-backed positive findings:** `job_manager.py:83-130` atomically admits same-type work and rolls back admission/thread-start failures; `:143-172` uses cooperative cancellation, one shared monotonic shutdown deadline, bounded joins, and no thread kill; `:174-235` retains terminal snapshots, gives cancellation precedence, rejects late updates, finalizes `BaseException`, and releases active slots. `telegram.py:128-155` now retains live process handles after reset, `:361-385` removes them only after observed exit, and `:736-789` bounds conversion workers to `min(os.cpu_count() or 1, 4)` with `shutdown(wait=True, cancel_futures=True)`. `f3ae62a` rollback and `0c6b8bf` Telegram generation guard are present and their focused tests pass.
- **Coverage qualifications:** Telegram executor wait/exception tests still use fake Executor/Future seams; the real overlap test patches `convert_webm_to_webp`, so no real ffmpeg cancellation/temp-dir matrix is proven. Telegram managed and legacy stale tests use mocked `_tg_worker`; Douyin has managed cancel/restart, but WeChat, ADB, and QQNT lack equivalent complete public cancel/restart/stale-worker matrices. Legacy importer branches remain daemon-thread compatibility paths outside JobManager ownership. WeChat keyfinder and ADB commands use blocking subprocess calls whose children are not actively terminated by JobManager cancellation. Updater generation is covered at the private cache seam, not through `JsApi`/`SettingsApi` force/pending calls.
- **Adversarial classes:** `stale_state` passes for Telegram process reset and generation tests but fails strict full-surface confirmation because closed `start_download()` leaves `downloading`; `cancel_resume` passes generic JobManager and tested Douyin/Telegram cases but is incomplete across all importers; `repeated_interruptions` is covered at JobManager and selected importer boundaries only; `hung_or_long_commands` passed and no verifier worker/process remained; `flaky_tests` passed two focused, two full, and ten updater force serial runs; `misleading_success_output` is satisfied by recording the closed-download nonzero semantic result rather than relying on green tests; `dirty_worktree` passed with inherited entries attributed and protected hashes unchanged; `malformed_input` and `prompt-injection` were ruled out because no parser or instruction boundary was changed and repository text was treated as data.
- **Cleanup/LSP:** Post-run `Get-Process python,pytest,ffmpeg` found no output, and no matching verifier temp directories (`tg_import_*`, `tg_executor_*`, `tg_test_*`, `dy_*`, `wechat_import_*`, `ohmymeme-qq-*`, `QQ_Favorite_*.zip`) remained. Linked-worktree `lsp_diagnostics` was rejected with `LSP file path must be inside request cwd`; no LSP-clean claim is made. The pre-existing generated `src/webui/settings.js` blob remained `1efe60527293d91c5963897534a13e862c889047` and was not touched.
- **AdversarialVerify verdict:** **needs-fix / not confirmed**. The requested Task 7 JobManager single-flight, normal completion, resource snapshots, terminal snapshot retention, shared shutdown timeout/no-force-kill, Telegram generation, live process reset/reap, bounded executor, and updater force-refresh timing behavior are independently evidenced. Confirmation is blocked by the reproducible public updater `start_download()` closed-manager state leak and by remaining strict coverage gaps around real Telegram ffmpeg/temp/process cleanup, updater download cancellation/mirror executor ownership, and all-five-importer cancel/restart/stale isolation. No plan checkbox was changed.

## 2026-08-28 updater download admission rollback follow-up

- Failing-first public regression reproduced the exact closed-manager defect: `updates.start_download()` raised `RuntimeError("job manager is shut down")` after publishing `_download_state["status"] = "downloading"`, with no active task and no worker to converge the state.
- Minimal fix wraps only the managed `_job_manager.start()` admission call. On admission failure it restores the existing public download state fields to `progress=0`, `status="idle"`, `error=""`, and `path=None`, then re-raises the original exception. Normal worker errors still use the existing `error` state path.
- Added deterministic updater coverage for exact exception text, full public state rollback, and absence of an active `update-download` task. Existing force/cache generation tests remain unchanged in behavior.
- Final focused lifecycle/importer suite passed three serial runs (`67 passed` each); full suite passed two serial runs (`446 passed, 1 skipped` each). Ruff, Black, and diff checks passed. Linked-worktree LSP remains unavailable due to request-cwd restriction; no protected/main-worktree file was modified.

## 2026-08-27 legacy Telegram generation guard follow-up

- Failing-first public legacy regression reproduced `LEGACY_TELEGRAM_STALE_STATE error old-late [0, 1]`: after worker 0 was cancelled and worker 1 restarted, worker 0's late `_update_tg` overwrote the shared state.
- Minimal Telegram-only fix adds a monotonically increasing generation and thread-local worker generation. Each public start captures a generation; `_update_tg` ignores updates from older generations while preserving current-worker updates, legacy progress fields, managed JobManager snapshots, and executor cleanup.
- The generation increment is performed only after active-state admission checks, so duplicate starts cannot invalidate the current worker. ADB, Douyin, WeChat, and QQNT were not changed because this exact legacy Telegram stale-state defect was isolated to Telegram.
- Final verification: focused lifecycle/importer suite passed three serial runs of `65 passed`; full suite passed two serial runs of `444 passed, 1 skipped`; Telegram stale regression and existing managed/legacy lifecycle tests passed. Ruff, Black, and diff checks passed. Linked-worktree LSP remains unavailable due to request-cwd restriction.

## 2026-08-28 independent Task 7 adversarial verification at `0c6b8bf`

- **Scope/isolation:** Verification stayed in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=0c6b8bf8e5d2d66f05d457dafd63d9dda72334ed`. All five requested commits were audited with `git show --stat --name-status`, complete changed-line diffs, `git show --check`, and combined `git diff --check 97c7769^ 0c6b8bf`. The combined product/test scope is exactly `telegram.py`, `douyin.py`, `wechat.py`, `tests/application/test_job_manager.py`, `tests/test_import_jobs.py`, `tests/test_tg_stickers.py`, plus the two architecture notepads; `tests/test_updater.py` is unchanged by this chain. No source/test/plan/README/AGENTS/protected-file write, reset, stash, discard, amend, force, commit, or main-worktree test was used. Existing target status remains `M .omo/notepads/architecture-optimization/issues.md` (this append) and pre-existing `M src/webui/settings.js` only.
- **Protected main worktree:** `D:/UserFiles/Development/Projects/OhMyMeme` remains separate on `gitbutler/workspace` with exactly its four pre-existing dirty paths. Rechecked hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. No protected path was modified or touched.
- **Serial runtime evidence:** The required legacy Telegram stale-worker regression ran 10 consecutive times, each `1 passed` in `0.12-0.14s`. The Task 7 focused set (`tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py tests/test_startup.py`) ran 3 consecutive times, each `110 passed` in `5.40-5.64s`; the exact four-file focused slice also ran 3 times, each `65 passed` in `4.01-4.21s`. Full suite run 1 passed `444 passed, 1 skipped in 13.83s`; full suite run 2 failed `1 failed, 443 passed, 1 skipped in 13.66s` at `tests/test_updater.py::TestCheckLatestCached::test_force_triggers_recheck_despite_fresh_cache`, where `len(calls)` was `1` rather than `2` after a fixed `sleep(0.2)`. A subsequent full suite run passed `444 passed, 1 skipped in 13.60s`; this is recorded as a reproducible timing-sensitive updater test flake, so two consecutive green full runs are not claimed.
- **Static gates:** `mise exec -- ruff check src/ tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py` returned `All checks passed`; targeted Black returned `8 files would be left unchanged`; all requested commit and combined diff checks passed. `lsp_diagnostics` was attempted for all eight relevant Python paths and rejected with `LSP file path must be inside request cwd`; no LSP-clean claim is made.
- **Generic shutdown/no-force-kill smoke:** A real `JobManager`, real worker thread, and Event release gate were driven through public `start()`, `shutdown()`, `wait()`, `get()`, and `active()`. Output: `NO_FORCE_KILL_SMOKE_OK result=False elapsed=0.0310 status=cancelled continued=True`. `shutdown(0.03)` returned `False`, left the worker alive/running until cooperative release, and after release the worker continued and converged to `cancelled` with no active slot. This confirms the generic shared deadline/no-force-kill contract.
- **Telegram generation evidence:** Source review confirms `_TG_GENERATION` plus thread-local generation is assigned by both managed and legacy starts, and `_update_tg()` rejects stale worker generations. The public legacy regression and the managed callback regression both passed; the legacy regression was independently executed 10 times. One ad-hoc inline legacy smoke failed its own event assertion before valid lifecycle completion and was rejected as malformed harness evidence, not product evidence. The checked-in regression uses a mocked worker, so it proves the generation gate and public start/cancel/restart ordering, not every real decrypt/convert stage.
- **Telegram executor/reap evidence:** The real executor concurrency/cancel-cleanup test plus Douyin/WeChat temp cleanup tests passed 3 repetitions. Source and tests confirm `min(os.cpu_count() or 1, 4)`, `shutdown(wait=True, cancel_futures=True)`, future exception handling, and `_reap_proc()` retaining a still-running process until `poll()` reports exit. However, executor count/wait/exception and process lifecycle tests are predominantly fake Executor/Future/DummyProc seams; the real executor test mocks `convert_webm_to_webp` and does not run ffmpeg.
- **Blocking exact repro:** A read-only probe inserted a still-running process object into `telegram._TG_ACTIVE_PROC`, called public-adjacent `_reset_state()`, and observed `RESET_RUNNING_PROC remaining=False terminated=True`. The process had not exited (`poll() is None`) but `_reset_state()` unconditionally cleared `_TG_ACTIVE_PROC` after calling `terminate()`. This contradicts the adjacent `_reap_proc()` invariant and the test `test_reap_keeps_process_registered_until_it_exits` (`tests/test_tg_stickers.py:134-156`): a still-running ffmpeg process can lose its tracking handle, preventing later cancel/reap. The issue is concrete, lifecycle-relevant, and not merely missing coverage.
- **Coverage/semantic gaps:** Generic JobManager single-flight, terminal snapshot freeze, cancellation precedence, shared shutdown deadline, closed admission, restart, resources metadata, and BaseException finalization are substantively covered. Telegram managed/legacy generation, Douyin public cancel/restart, and Douyin/WeChat partial-directory cancellation cleanup are covered. There is no complete public cancel-then-restart and stale-worker matrix for all five importers; updater generation remains private-state/mock-heavy without `JsApi`/`SettingsApi` force/pending bridge coverage; Telegram real ffmpeg/process/temp-dir cancellation is not proven. Resources remain snapshot metadata, not enforced locks. Legacy importer paths bypass JobManager by compatibility design.
- **UltraQA classification:** `stale_state` fails on the exact `_reset_state()` lost-process-handle repro, while the prior legacy Telegram stale overwrite is fixed and passed 10 times. `cancel_resume` passes generic restart, Telegram legacy/managed generation tests, and Douyin public restart, but is incomplete for WeChat/ADB/QQNT public restart. `repeated_interruptions` is proven for JobManager and selected prior ADB/QQNT paths, not all importer workers. `hung_or_long_commands` passed bounded runs; no verifier worker/process remained. `flaky_tests` is not clean because the second full-suite run reproduced the updater fixed-delay race. `misleading_success_output` passed by recording the failed full run and rejecting the malformed inline smoke. `dirty_worktree` passed under explicit attribution; `malformed_input` and `prompt_injection` were ruled out because no parser or instruction boundary was changed and repository text was treated as data.
- **Cleanup receipt:** Initial post-run scan found nine verifier-created `%TEMP%/tg_executor_*` directories left by test setup; they were removed after a `-WhatIf` preview, and a second scan reported no matching Telegram/importer temp directories. Process scan reported no `python`, `pytest`, or `ffmpeg` verifier processes. No executor, worker, socket, or other verifier artifact remained. The only intentional write was this append to `issues.md`; pre-existing `src/webui/settings.js` remained untouched.
- **AdversarialVerify verdict:** **needs-fix / not confirmed**. The exact Task 7 Telegram legacy stale-worker overwrite is fixed and independently passes, and generic lifecycle/normal completion/shutdown/no-force-kill/resources evidence is strong. Confirmation is blocked by the reproducible `_reset_state()` removal of a still-running Telegram process handle, plus the observed non-deterministic updater focused/full-suite timing failure and incomplete real five-importer cancellation/restart/process cleanup evidence. No plan checkbox was changed.

## 2026-08-28 independent Task 7 verification after `990fb6f`

- **Scope and isolation:** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=990fb6fb47ebbee8205bb892fb52bb517a10fab2`. The full Task 7 chain was audited with `git show --stat`, `--name-status`, combined diffs, and `git diff --check`: `97c7769`, `4f45903`, `f3ae62a`, `ce81ae5`, `0c6b8bf`, `f98efe0`, and `990fb6f`. `990fb6f` changes only `src/ohmymeme/services/updates.py` and `tests/test_updater.py`, rolling back public download state after closed-manager admission failure. Before this append, target status contained only inherited `M .omo/notepads/architecture-optimization/issues.md` and pre-existing generated `M src/webui/settings.js`; no source/test/plan/README/AGENTS file was modified.
- **Protected main-worktree evidence:** `D:/UserFiles/Development/Projects/OhMyMeme` remains separate on `gitbutler/workspace` with exactly four pre-existing dirty paths. Recomputed hashes remain `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`. No protected path or main-worktree test/write was used.
- **Source evidence:** `JobManager.try_start()` atomically admits one same-type worker and rejects a closed manager before callbacks; `shutdown()` uses cooperative cancellation and one monotonic shared deadline, returns false for a blocked worker, and never kills threads. `_finish_locked()` gives cancellation precedence, freezes terminal snapshots against late updates, retains resources, finalizes arbitrary `BaseException`, and releases active slots. Telegram generation guards stale managed/legacy updates, retains live process handles until `poll()` observes exit, and uses bounded `min(os.cpu_count() or 1, 4)` conversion with `shutdown(wait=True, cancel_futures=True)`. Telegram/Douyin/WeChat managed admission rollback clears module state to idle. `start_download()` now restores `progress=0,status=idle,error="",path=None` and re-raises the original closed-manager exception.
- **Runtime/test evidence:** Exact focused command `mise exec -- python -m pytest tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_updater.py tests/test_tg_stickers.py -q` passed three consecutive times, each `67 passed` (`3.81s`, `3.99s`, `3.80s`). Exact full command `mise exec -- python -m pytest tests/ -q` passed twice, each `446 passed, 1 skipped` (`13.29s`, `13.36s`). Additional targeted shutdown/importer/updater/Telegram command passed `24` tests. A direct Event-controlled runtime probe observed `shutdown(0.01) is False`, worker still running until cooperative release, then terminal `cancelled` with no active slot; injecting stale updater state and calling public `start_download()` on a closed manager raised exactly `RuntimeError("job manager is shut down")`, restored the exact idle snapshot, and left no active update task (`TASK7_RUNTIME_PROBE_OK`).
- **Static/cleanup evidence:** `mise exec -- ruff check src/ tests/application/test_job_manager.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/test_updater.py` returned `All checks passed`; Black reported `64 files would be left unchanged`; `git diff --check 97c7769^ 990fb6f` passed. LSP diagnostics for all nine relevant Python paths were attempted and rejected by the known linked-worktree request-cwd restriction, so no LSP-clean claim is made. No matching verifier `tg_*`, `dy_*`, `wechat*`, or `qq-*` directory, process, executor, socket, worker, or temporary root remained. `%TEMP%/qq-stream-output` was pre-existing (created 2026-08-16) and not touched.
- **UltraQA and conclusion:** `cancel_resume`, `stale_state`, `hung_or_long_commands`, `flaky_tests`, `misleading_success_output`, `dirty_worktree`, and prompt-injection rule-out all passed for the requested lifecycle surface; complete real cancel/restart matrices for WeChat/ADB/QQNT and real ffmpeg cancellation remain coverage qualifications, not observed failures. **AdversarialVerify verdict: confirmed.** The exact Task 7 checkbox is independently confirmed, including `990fb6f` closed-manager updater rollback, prior importer rollback, JobManager no-force-kill/shared deadline and terminal convergence, Telegram bounded executor/process cleanup, stale isolation, and focused/full serial regressions.

## 2026-08-28 independent Task 8 verification after `3d8af56`

- **Scope and protection:** Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=3d8af5674b21b32a2b035ad51290b7cb9e11b5f2`, parent=`990fb6fb47ebbee8205bb892fb52bb517a10fab2`. `git show` inspected every changed line and commit scope: three notepads, `src/ohmymeme/presentation/desktop/window_manager.py`, and `tests/presentation/test_bridge_compat.py`; `git diff --check 3d8af56^ 3d8af56` passed. The linked product/test scope is clean relative to `HEAD` (`git diff --exit-code -- src/ohmymeme tests`); only inherited `M .omo/notepads/architecture-optimization/issues.md` and pre-existing generated `M src/webui/settings.js` remain. No source, test, plan, README, AGENTS, protected file, reset, stash, discard, amend, force, commit, or main-worktree test/write was used. Codegraph was attempted once and reported no index for this linked worktree; raw source review followed.
- **Protected main-worktree evidence:** `D:/UserFiles/Development/Projects/OhMyMeme` remains separate on `gitbutler/workspace` with exactly four pre-existing dirty paths: `src/ohmymeme/presentation/desktop/window_manager.py`, settings `core/init.js`, settings sync `settings.js`, and `src/webui/settings.js`. Recomputed hashes are unchanged from the recorded baseline: `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`.
- **Positive source ownership evidence:** `window_manager.py:870-959` preserves QQNT legacy/managed entry ABI and passes the optional callback through both paths; `:961-1040` invokes `qqnt.extract_qq_emojis`, never deletes the user-selected output, and injects only supported output paths into the callback; `:1578-1595` binds `SettingsApi.qqnt_start` to `Container.library.import_paths`. `local_library.py:118-123` converts paths to `ImportPath` and delegates once to `import_batch`; `core/imports.py:150-208` validates magic/Pillow/20 MiB/2560px, content-hash deduplicates, writes application cache and DB, projects manifest only after insertion, and compensates created DB/cache state plus manifest on failure. `assets.py:24-43` and `manifest.py:75-110` confirm DB/cache/manifest remain application-owned and never reference the QQNT output path. `qqnt.py:254-437` confirms ordinary copy failure/cancel cleanup does not remove output; explicit `overwrite=True` remains an intentional user-requested clear operation.
- **Targeted and static evidence:** `mise exec -- python -m pytest tests/presentation/test_bridge_compat.py tests/test_startup.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/application/test_import_service.py -q` -> `111 passed in 3.07s`; repeat -> `111 passed in 3.18s`. `mise exec -- ruff check src/ohmymeme/presentation/desktop/window_manager.py src/ohmymeme/integrations/imports tests/presentation/test_bridge_compat.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/application/test_import_service.py tests/test_startup.py` -> `All checks passed`; targeted Black -> `6 files would be left unchanged`. The changed bridge test verifies one legacy happy path: output survives and the library callback receives the output path exactly once. Existing startup/import tests cover QQNT single-flight, cancellation, terminal errors, admission rollback, and importer temporary cleanup.
- **Real temporary-root smoke evidence:** A managed `Container` temporary-root probe imported a valid 2x2 PNG through `library.import_paths`, observed one DB row, a hash-named application cache file, and a version-3 manifest entry, while the QQNT output file remained in its user directory. The same real library rejected invalid bytes and a 2561px image with `rejected=1`, added no DB row/cache file, and left the manifest count unchanged: `TASK8_CANONICAL_SMOKE_OK valid=1 invalid=1 oversize_px=1 output_preserved=1`. A QQNT output ownership probe confirmed non-empty output-directory admission rejects without deleting a user sentinel: `TASK8_QQNT_OUTPUT_OWNERSHIP_OK nonempty_rejected=1 sentinel_preserved=1`.
- **Blocking exact repro 1, cancellation/projection race:** A real temporary `Container` and real `window_manager._qqnt_worker` were run with a callback that performed canonical import, then blocked. The verifier called `cancel_qqnt_extract()` while the callback was blocked and released it. Observable result: `TASK8_CANCEL_RACE_REPRO cancelled callback_paths 1 db_count 1 manifest_memes 1`. Thus the public/UI terminal state is `cancelled`, but a cancellation racing after the one pre-callback check (`window_manager.py:997-1000`) still permits canonical import and leaves DB/cache/manifest state. `LocalLibraryService.import_paths` has no cancellation input (`local_library.py:118-123`), so this is a production semantic failure against the strict requirement that cancellation/failure produce no orphan projection, not merely missing coverage.
- **Blocking exact repro 2, cleanup failure is silently successful:** A Telegram worker probe injected a real temporary importer directory, forced decryption to skip, and replaced `shutil.rmtree` with an `OSError`. Observable result: `TASK8_CLEANUP_FAILURE_REPRO done temp_exists True temp_files []`. `telegram.py:870-875` catches cleanup errors and ignores them, leaving the importer-owned temporary directory while reporting `done`; no error/recovery marker or residual path is exposed. This directly violates the requested cleanup-failure rule-out, although it does not create a DB/manifest row.
- **Additional source-backed qualifications:** QQNT scans the entire retained user output directory (`window_manager.py:1001-1015`), not a per-run result list, so pre-existing/user-added supported images can be submitted on a later run; hash dedup prevents duplicate DB rows but not repeated callback reads. The QQNT callback return (`imported`/`rejected`) is discarded, so UI `result` remains extraction statistics rather than canonical projection statistics. QQNT extractor signatures include formats beyond the six worker suffixes (`qqnt.py:29-41` versus `window_manager.py:1007-1014`), which are silently excluded from projection. These are not needed to establish the two blocking repros, but prevent a stronger “all output/one projection” claim.
- **Full serial and surface evidence:** Exact `mise exec -- python -m pytest tests/ -q` passed twice: `447 passed, 1 skipped in 13.47s` and `447 passed, 1 skipped in 13.45s`. The required bridge/importer/lifecycle slice passed twice as above. Post-run process inspection found no `python`, `pytest`, or `ffmpeg` verifier process; matching importer/task temp directories were absent after explicit cleanup (the cancellation probe root was first previewed with `Remove-Item -WhatIf`, then removed and reported `TASK8_ARTIFACT_CLEANUP_OK`). LSP diagnostics for both changed Python paths were attempted but rejected with `LSP file path must be inside request cwd`; no LSP-clean claim is made.
- **UltraQA classification:** `cancel_resume` fails strict confirmation through the real cancelled-with-persisted-projection race; `stale_state` fails because cancelled UI state coexists with DB/manifest projection and cleanup failure reports `done` with a retained temp root. `dirty_worktree` passes with inherited entries explicitly attributed and product/test scope unchanged. `hung_or_long_commands` passes: targeted/full commands completed within budget and no verifier worker/process remained. `flaky_tests` passes this run with two consecutive full and targeted green rounds, but serial success does not override semantic repros. `misleading_success_output` passes because the green suites were not accepted over the two nonzero semantic probes. `repeated_interruptions` is only partially proven by existing importer/JobManager tests; no claim is made for all real child-process paths. `malformed_input` passes for canonical invalid bytes, QQNT non-empty output admission, and oversize pixel input; downloader/decrypt malformed matrices remain partial. `prompt_injection` is ruled out because repository/notepad text was treated as untrusted data, never as execution instructions.
- **Verdict:** **needs-fix / not confirmed.** The commit correctly establishes QQNT user-output ownership and canonical Container library routing, and valid/invalid/oversize canonical projection plus bridge/lifecycle regressions are green. However, the real cancellation race leaves a persisted DB/cache/manifest projection after terminal `cancelled`, and the real cleanup-failure probe leaves Telegram temporary output while reporting `done`. No checkbox or source/test file was changed; this append is the only intentional verification write.

## 2026-08-28 independent Task 8 verification after `432f12c`

- **Scope/protection:** Verification stayed exclusively in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at exact `HEAD=432f12cc7e4d61d59d2df766c66268b395b75ea7`. Native history inspection covered `3d8af56` and `432f12c`, including every changed line and the combined scope: three Task 8 notepads, `src/ohmymeme/presentation/desktop/window_manager.py`, `src/ohmymeme/integrations/imports/telegram.py`, `tests/presentation/test_bridge_compat.py`, and `tests/test_tg_stickers.py`; `git diff --check 990fb6f..432f12c` passed. The linked product/test scope had no diff before or after verification; inherited status remained only `M .omo/notepads/architecture-optimization/issues.md` and pre-existing generated `M src/webui/settings.js`. No source/test/plan/README/AGENTS/protected-file write, reset, stash, discard, amend, force, or main-worktree test/write was used.
- **Protected main-worktree hashes:** Main worktree `D:/UserFiles/Development/Projects/OhMyMeme` remained separate on `gitbutler/workspace` with exactly four pre-existing protected dirty paths. Rechecked blobs were `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, settings `core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, settings sync `settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`; unchanged.
- **Source/agent evidence:** `SettingsApi.qqnt_start` passes the Container-owned `library.import_paths`; QQNT output remains user-owned while `LocalLibraryService`/`ImageImportService` own application cache, DB, and manifest. `432f12c` adds a post-callback cancellation check and bound-library `delete_memes` compensation, and maps Telegram `rmtree` `OSError` to terminal `error_code=cleanup_failed`. Independent source review and four explore agents agree that compensation remains callback-shape/reflection dependent and cannot cancel an already-running canonical import transaction; Telegram still imports in batches and other importers retain silent cleanup paths, but those are outside the two changed fixes.
- **Serial runtime/static evidence:** `mise exec -- python -m pytest tests/presentation/test_bridge_compat.py tests/test_startup.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/application/test_import_service.py -q` passed `113 passed in 3.11s`; unchanged full suite passed twice: `449 passed, 1 skipped` in `14.25s` and `14.28s`. `mise exec -- ruff check src/ohmymeme/presentation/desktop/window_manager.py src/ohmymeme/integrations/imports tests/presentation/test_bridge_compat.py tests/test_tg_stickers.py tests/test_import_jobs.py tests/application/test_import_service.py tests/application/test_local_library_service.py tests/test_startup.py` passed; Black reported `14 files would be left unchanged`; `git diff --check` passed.
- **Real QQNT cancellation repro:** A temporary-root `Container` with a real `MemeDB`, `ImageImportService`, `LocalLibraryService`, and user output sentinel was driven through `_qqnt_worker`; the callback performed the real canonical import and called `cancel_qqnt_extract()` before returning. Observed `QQNT_REAL_CANCEL cancelled 1 1 True True`: terminal UI state was `cancelled`, but DB rows=`1`, application cache files=`1`, user sentinel remained, and manifest existed. A bound-method variant using `LocalLibraryService.import_paths` produced `QQNT_BOUND_CANCEL cancelled 0 0 True True`, proving compensation only works when cancellation occurs after callback return, not while canonical import is executing. This is the exact acceptance failure: cancelled QQNT can persist a projection, so the checkbox is not confirmed.
- **Real Telegram cleanup-failure repro:** A temporary Telegram cache with controlled decrypted WebP and patched `shutil.rmtree` raised `OSError('controlled cleanup')`; observed `TG_REAL_CLEANUP error cleanup_failed temp_exists True temp_files 4`. The new mapping prevents false `done` and exposes the retained importer-owned temp directory as an error condition. The checked-in cleanup regression is additionally covered by `tests/test_tg_stickers.py`.
- **Failure/orphan matrix:** Real `ImageImportService` tests and source verify invalid magic/Pillow payloads, `20 MiB+1`, and `2561px` inputs reject before cache/DB/manifest writes; import/install/DB/manifest faults compensate and write recovery markers when recovery fails. Telegram decrypt/download/conversion failures avoid callback for failed files and cleanup the temp directory normally; however per-task cancellation/late callback and multi-batch partial commit are not atomic. QQNT/Douyin/WeChat/ADB external output/temp cleanup failure paths remain only partially covered, with several `ignore_errors=True` paths. No new orphan was created by verification after cleanup.
- **UltraQA:** `cancel_resume` and `stale_state` fail strict confirmation on the real QQNT callback cancellation repro; `cleanup_failed` Telegram behavior passes. `dirty_worktree`, `hung_or_long_commands`, `misleading_success_output`, `flaky_tests` (two full and targeted green runs), malformed canonical input, and prompt-injection rule-out pass. `repeated_interruptions` remains partial because all-importer real child/process paths are not exercised. LSP diagnostics remain unavailable because the client rejects linked-worktree paths outside its request cwd; no LSP-clean claim is made.
- **Cleanup receipt/verdict:** A `-WhatIf` preview preceded removal of verifier-created `tg_cleanup_failure_*`/`tg_import_*` temporary directories; final scan reported `TEMP_MATCHING_REMAIN=0` and no `python`/`pytest`/`ffmpeg` verifier process. **Verdict: needs-fix / not confirmed.** `432f12c` fixes Telegram cleanup-failure false success and establishes the intended QQNT ownership direction, but the required QQNT cancellation race still leaves DB/cache/manifest state after terminal cancellation. No checkbox was changed; this append is the only intentional verification write.

## 2026-08-28 independent Task 8 verification after `2608d31`

- **Scope/protection:** Verification ran only in `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline` at `HEAD=2608d31b4c59580cc69427c39c8d1fbbea1d8f83`. Native `git show --patch` inspected every changed line and combined scope for `3d8af56`, `432f12c`, and `2608d31`; `git diff --check 3d8af56^ 2608d31` passed. Product/test scope remained unchanged; inherited status was only `M .omo/notepads/architecture-optimization/issues.md` and pre-existing `M src/webui/settings.js`. Main worktree remained separate on `gitbutler/workspace`; protected blob hashes stayed `window_manager.py=e0f7c67460380fa07db24350f484e06c9d6f6b73`, `settings/core/init.js=36d5f9910912cfd6b7337eb22602833fd6b05be6`, `settings/features/sync/settings.js=e954a2c006f83a907d841f02c07380bb6dbe8abe`, and `src/webui/settings.js=1e66f686f374cbefacf7c0c86dc4ddf53b381728`.
- **Ownership source evidence:** `SettingsApi.qqnt_start` injects the Container-owned `library.import_paths`; `qqnt.extract_qq_emojis` owns the user-selected output and its overwrite/cancel semantics; `LocalLibraryService` and `ImageImportService` own application cache, SQLite rows, and manifest projection. `ImageImportService._install` uses an adjacent atomic temporary file, while `import_batch` snapshots/restores manifest and compensates only IDs/cache paths created by that batch. QQNT worker never deletes the user output directory.
- **Deterministic real mid-callback cancellation:** A temporary real `Container`/`MemeDB`/`ImageImportService` was driven with an Event-triggered `add_meme` gate and cancellation event. The first DB insert set the event; `import_batch` returned `imported_ids=()`, DB rows=`0`, application cache files=`0`, and manifest `exists=False`. This independently confirms cancellation during the canonical projection path rolls back DB/cache/manifest rather than relying on the post-callback bridge compensation.
- **Deterministic valid and rejection matrix:** In the same isolated Container, a distinct valid 3x2 PNG returned `imported_ids=(2,)`, produced exactly one DB row/cache file, and wrote manifest version `3`. Invalid bytes returned `rejected=1` with no additional row; a valid 2561px PNG returned `rejected=1` with no additional row. Existing transaction-fault tests additionally cover file install, DB, manifest, and recovery-marker failures. User output ownership remains separate from all application projections.
- **Telegram cleanup failure:** Existing focused regression and source inspection confirm `shutil.rmtree(temp_dir)` `OSError` changes the terminal state to `error`, `error_code=cleanup_failed`, rather than leaving `done`; the retained importer-owned directory is therefore exposed as an error. The required external-network happy/failure paths were not claimed because network services are unavailable; deterministic decrypt/download seams and core validation tests were used instead.
- **Serial verification:** `mise exec -- python -m pytest tests/presentation/test_bridge_compat.py tests/test_startup.py tests/test_import_jobs.py tests/test_tg_stickers.py tests/application/test_import_service.py tests/application/test_local_library_service.py -q` passed `138 passed` twice. `mise exec -- python -m pytest tests/ -q` passed `449 passed, 1 skipped` twice. `mise exec -- ruff check ...` returned `All checks passed`; targeted Black reported `6 files would be left unchanged`; `git diff --check` passed. LSP diagnostics remain unavailable because the client rejects linked-worktree paths outside its request cwd.
- **Artifact/ultraQA evidence:** Probe-created `task8_*` roots were previewed with `Remove-Item -WhatIf` then removed; final scan found `task8_*:0`, `dy_*:0`, `wechat_import_*:0`, `ohmymeme-qq-*:0`, and `VERIFIER_PROCESSES:0`. Fourteen pre-existing `%TEMP%/tg_import_*` roots remain and were not touched or attributed to this run. `cancel_resume`, `stale_state`, `dirty_worktree`, `hung_or_long_commands`, `flaky_tests`, `misleading_success_output`, and malformed canonical input pass on direct evidence; repeated interruptions remain bounded by cooperative cancellation tests, while live external-network behavior is explicitly unclaimed. Prompt injection is ruled out because repository/notepad text was treated as data, never procedure.
- **Verdict:** **confirmed.** `2608d31` closes the previously reproduced QQNT mid-callback cancellation race at the canonical import boundary while preserving QQNT user output ownership; valid import projects once; invalid/oversize inputs produce no orphan projection; Telegram cleanup failure is terminal `cleanup_failed`. No checkbox or source/test/plan file was changed; this append is the only intentional verification write.
## 2026-08-28 desktop bridge split

- The linked worktree had no CodeGraph index; raw source inspection was used after the required CodeGraph attempt. The LSP client rejected all linked-worktree paths because its request cwd is the main worktree; no clean LSP claim is made.
- Added shared `desktop/api/handlers.py` handlers for meme, import, sync, update, and window/settings dependency ownership. Handlers retain references to the existing WebUI Container graph and do not construct config, DB, assets, library, or job-manager resources.
- Added bridge contract coverage for handler identity and retained existing representative ABI tests. Focused bridge/startup/import/sync/LAN suite passed `182 passed, 1 skipped`; full suite passed `450 passed, 1 skipped`.
- Ruff, Black, and scoped `git diff --check` passed. The pre-existing `src/webui/settings.js` worktree change was not touched or staged.

## 2026-08-28 independent Task 9 verification after `7f8c266`

- Verification stayed exclusively in linked worktree `C:/Users/abbey/AppData/Local/Temp/opencode/ohmymeme-architecture-optimization-baseline`, at exact HEAD `7f8c266f55be865fb811fe05a28e6abb88d56ef4`. Native status before this append contained only the inherited protected `M src/webui/settings.js`; `git worktree list --porcelain` confirmed the main worktree is `D:/UserFiles/Development/Projects/OhMyMeme` on `gitbutler/workspace`. No source, tests, plan, README, AGENTS, or protected main-worktree file was modified, and no reset/stash/discard/amend/force/commit operation was used.
- CodeGraph was attempted once and reported no `.codegraph` index for this linked worktree; raw source/history review followed. LSP diagnostics were attempted for `window_manager.py`, all four `desktop/api` paths, and `test_bridge_compat.py`, but every request was rejected because the LSP request cwd is the main worktree; no LSP-clean claim is made.
- Commit audit inspected every changed line in `7f8c266`: `api/__init__.py`, `api/handlers.py`, `api/memes.py`, `api/settings.py`, `window_manager.py`, and `tests/presentation/test_bridge_compat.py`. AST comparison of parent/current `JsApi` and `SettingsApi` found identical public callable method names, parameter order/defaults, and return annotations: `JsApi` 58 public methods and `SettingsApi` 62 public methods. Lazy `api` exports import both facades successfully. `git diff --check 7f8c266^ 7f8c266` passed.
- Real temporary `Container` smoke passed: `WebUI` construction exposed both facades, handler references had identity with the Container's `catalog`, `library`, `config`, and Container-owned sync graph, no duplicate library object was constructed, `search_memes(..., limit=0)` returned `list`, `get_tags()` returned `list`, and `Container.close()` followed by explicit cleanup removed the temporary root.
- Required runtime coverage passed serially: `mise exec -- python -m pytest tests/presentation/test_bridge_compat.py tests/test_startup.py tests/application/test_import_service.py tests/test_import_jobs.py tests/test_sync.py tests/test_lan.py tests/integration/test_lan_server.py tests/app/test_container.py -q` -> `230 passed, 1 skipped in 7.58s`; unchanged full `mise exec -- python -m pytest tests/ -q` -> `450 passed, 1 skipped in 13.15s`. `mise exec -- ruff check src/ohmymeme/presentation/desktop tests/...` -> `All checks passed`; targeted Black -> `25 files would be left unchanged`.
- **Blocking structural finding:** the acceptance requires `window_manager.py` to retain only façade and WebUI lifecycle wiring, with Meme/Import/Sync/Update/Window-Settings internals extracted. Current `window_manager.py` remains 2,322 total lines / 1,976 nonblank-noncomment lines and still directly implements or owns substantial domain operations. Source inspection found direct meme/collection/catalog/library calls at lines 233-450, 473-737; direct import calls at 495, 553, 618-737; direct settings/importer/LAN operations at 1060-1648; and direct sync operations at 1675-1800. `handlers.py` only contains 5 small handler classes, and only a subset of meme, import, sync, and update calls route through them. `WindowSettingsHandler` stores dependencies but exposes no settings behavior. This is a concrete mismatch with the requested extraction scope, not a test coverage gap.
- ABI/failure-shape qualification: unchanged AST signatures and passing bridge/startup/import/sync/LAN tests support preserved façade ABI for covered paths; the new handler identity test proves shared references, and the real Container smoke proves no second graph. However, the structural blocker prevents confirmation of the exact checkbox despite green tests. Malformed bridge inputs and prompt/untrusted text were treated through existing tests/source without executing repository instructions; no misleading-success substitution was made after the corrected smoke. Commands completed within bounds, and no verifier process/socket/temp root remained.
- Verdict: **needs-fix / not confirmed**. Runtime behavior, façade ABI comparison, shared-resource identity, lint/formatting, and full serial suite are positive, but the implementation does not satisfy the explicit “`window_manager.py` only façade + lifecycle wiring” extraction requirement. No source/test/plan change was made; this append is the only intentional write.
## 2026-08-28 Task 9 incremental migration

- Revalidated linked worktree at `7f8c266`; inherited dirty files remain limited to ignored notepad state and pre-existing `src/webui/settings.js`.
- Bulk AST extraction was not used. Manually moved concrete Sync behavior (`push`, `pull`, `auto_sync`, `test_connection`, progress and remote cleanup/status helpers) into the existing Container-backed `SyncHandler`; façade wrappers preserve current public calls and error envelopes.
- Serial bridge/sync verification passed `75 passed`; targeted Ruff and Black passed. LSP remains unavailable because linked-worktree paths are outside the request cwd.
- Structural measurement still reports `window_manager.py` at approximately `1,952` non-comment lines. Meme/Import/Window-Settings bodies remain inline, so Task 9 is intentionally **not confirmed** and no commit was created in this partial step.
## 2026-08-28 Task 9 incremental Meme migration

- Preserved the partial Sync migration and manually moved concrete Meme/Collection behavior into `MemeHandler`: tags/init data, native drag/copy, rename/delete, collection tree/search/membership, collection mutation/order, recent-use operations, and cache rescan. Façade methods remain explicit named wrappers.
- One startup regression initially exposed that tests monkeypatch `window_manager.copy_image_to_clipboard`; the compatibility import was retained while handler lookup resolves the patched module symbol. Final targeted bridge/startup/sync suite passed `120 passed`; required bridge/startup/import/sync/LAN slice passed `182 passed, 1 skipped`; full suite passed `450 passed, 1 skipped`.
- Ruff and Black passed for `window_manager.py` and `desktop/api/`; scoped diff check passed. LSP remains unavailable for linked-worktree paths due request cwd restriction.
- Structural evidence remains insufficient for Task 9: `window_manager.py` is `1,858` non-comment lines; `JsApi` and `SettingsApi` still contain substantial Import and Window/Settings bodies. No commit was created because the checkbox is not complete.
## 2026-08-28 Task 9 incremental Import migration

- Preserved prior Sync and Meme/Collection migrations. Manually moved concrete `JsApi` import dialog, folder traversal, and clipboard import behavior into `ImportHandler`; wrappers remain explicit and named.
- Existing monkeypatch compatibility was retained: import dialog resolves `window_manager.webview`, and empty clipboard results preserve the historical `name="未命名"` envelope. Domain tests initially caught both differences before the fixes.
- Serial bridge/startup/sync targeted run passed `120 passed`; required bridge/startup/import/sync/LAN slice passed `182 passed, 1 skipped`; full suite passed `450 passed, 1 skipped`. Ruff passed; Black formatted then passed; diff check passed.
- Structural measurement is still incomplete: `window_manager.py` remains `1,748` non-comment lines with QQNT state and substantial SettingsApi/LAN/settings/original-image bodies inline. Task 9 remains unconfirmed and no commit was created.
## 2026-08-28 Task 9 incremental remaining-domain migration

- Preserved existing Sync and Meme/Collection changes. Manually moved `download_original_image` into `ImportHandler`; preserved local-file/network error envelopes and module-level helper seams.
- Moved SettingsApi LAN lifecycle/status/IP/secret-config operations and main/settings window movement into `WindowSettingsHandler`; Container ownership remains injected and no singleton resource is constructed.
- Per-domain validation: focused bridge/startup/sync tests passed `120`; bridge/startup/import/sync/LAN slice passed `160 passed, 1 skipped`; full suite passed `450 passed, 1 skipped`; Ruff, Black, and diff check passed.
- Structural measurement: `window_manager.py` is now `1,640` non-comment lines, with `JsApi` 62 methods, `SettingsApi` 64 methods, and `WebUI` 28 methods. QQNT state/worker and remaining SettingsApi importer/update/storage bodies remain inline, so Task 9 is still **not confirmed** and no commit was created.
- LSP diagnostics were attempted for both changed Python files and rejected because the linked worktree is outside the LSP request cwd; no LSP-clean claim is made. The pre-existing `src/webui/settings.js` dirty file remains untouched.
## 2026-08-28 Task 9 incremental settings/import migration

- Continued without reset from partial Sync/Meme/Import state. Moved `SettingsApi` storage info, storage picker/application, QQNT environment/default-directory queries, and QQNT directory opening into `WindowSettingsHandler`.
- Preserved Container-owned library/config and existing WebView refresh/error envelopes. No handler constructs a second application object graph.
- Focused bridge/startup/sync regression passed `120 passed`; LAN-inclusive targeted run had one existing fixed-port/global-state setup error, then serial LAN rerun passed `40 passed, 1 skipped`; full suite passed `450 passed, 1 skipped`.
- Ruff and Black passed; scoped diff check passed. Structural measurement is now `window_manager.py=1,598` non-comment lines, still containing QQNT worker/state plus Telegram/Douyin/WeChat/ADB import orchestration and remaining settings/update bodies. Task 9 remains unconfirmed; no commit was created.
- LSP diagnostics remain unavailable because the linked worktree is outside the LSP request cwd. Pre-existing `src/webui/settings.js` remains untouched.
## 2026-08-28 Task 9 final feasibility check

- Revalidated the current partial migration without reset: `mise exec -- python -m pytest tests/presentation/test_bridge_compat.py tests/test_startup.py tests/test_import_jobs.py tests/test_sync.py tests/test_lan.py -q` passed `182 passed, 1 skipped`.
- Structural gate remains failing: `window_manager.py` measures `1,598` non-comment lines and still contains `JsApi` (62 methods), `SettingsApi` (64 methods), QQNT worker/state, importer orchestration, and remaining settings/update bodies.
- The remaining QQNT block owns module-level state, JobManager admission/cancellation callbacks, importer compatibility globals, and tests that monkeypatch `window_manager` symbols. Moving it safely requires explicit state/context ownership and per-method seam updates, not a text/AST copy. A bulk move was previously abandoned after cycles/undefined globals/encoding failures.
- LSP was attempted again and rejected because the linked worktree is outside the request cwd. No new commit was created; Task 9 remains unconfirmed rather than being reported as complete.

## 2026-08-28 Task 10 ABI verification

- Baseline bridge/startup tests passed `57`; the new behavior matrix initially failed on an incorrect expected Linux window-drag result and missing fake settings methods, then was corrected without changing product code.
- Targeted bridge/startup tests passed `62`; full serial suite passed `455 passed, 1 skipped`. Ruff, Black, and `git diff --check` passed for the two test files. LSP was attempted for both linked-worktree test files and rejected by the known request-cwd restriction.
- Pure non-comment LOC is already above the generic advisory ceiling in both touched test modules (`493` and `1073`) because of inherited test suites; this task added only focused cases and did not refactor existing test organization.
- A first disposable façade smoke harness failed on its incomplete fake (`_update_debug` missing), not product execution; no smoke result is claimed from that harness. The corrected smoke must include complete façade dependencies before reporting success.

## 2026-08-28 Task 10 ABI correction

- Replaced the prior generic-recorder assertions that expected dictionaries from `start_download`, `sync_test`, and `import_memes`; the matrix now supplies and checks the actual `bool`, `str`, `list`, and `dict` contracts.
- Added concrete main/settings failure-envelope assertions for sync, import dialog, updater download, storage, LAN, and void window movement. Missing-method coverage now queries an actual `SettingsApi` instance instead of a `SimpleNamespace`.
- Focused bridge/startup suite passed `64 passed`; two independent serial full-suite runs each passed `457 passed, 1 skipped`. No product source or Task 9 marker changed.
